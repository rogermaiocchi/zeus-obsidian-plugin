# Runtime cache — gerado por reindex
data/

# v1.5 — bin/ZeusDaemonMac é commitado (autonomia drop-in). Plugin pré-construído
# inclui o daemon arm64 codesigned adhoc; usuário final não roda swift build.
# Maintainer regenera com `node scripts/build-release.mjs` (Apple Silicon Mac).

# macOS noise
.DS_Store

# Editor noise
.vscode/
*.swp

# Swift daemon build artifacts
daemon/.build/

# Dependencias de build JS (esbuild)
node_modules/

# Backups locais (nao versionar)
*.bak
main.js.backup-*
