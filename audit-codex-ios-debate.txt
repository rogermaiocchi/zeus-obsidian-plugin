2026-05-21T15:04:50.397691Z ERROR codex_core::session: failed to load skill /Users/rogermaiocchi/dev/Apple-notes/SKILL.md: invalid description: exceeds maximum length of 1024 characters
2026-05-21T15:04:50.397946Z ERROR codex_core::session: failed to load skill /Users/rogermaiocchi/dotfiles/shared/skills/lead-conversao/SKILL.md: invalid YAML: mapping values are not allowed in this context at line 2 column 277
2026-05-21T15:04:55.404659Z ERROR codex_models_manager::manager: failed to refresh available models: timeout waiting for child process to exit
OpenAI Codex v0.130.0
--------
workdir: /Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91
model: gpt-5.5
provider: openai
approval: never
sandbox: read-only
reasoning effort: high
reasoning summaries: none
session id: 019e4b11-7370-7a21-bb23-c162c354cfd2
--------
user
MESA DE DEBATE — arquitetura para 100% de funcionamento do Zeus no iOS.
Você é o engenheiro adversarial. Objetivo: decidir o caminho REALISTA para o plugin Zeus
(Obsidian) funcionar 100% no iPhone e iPad do Roger, AGORA, com o mínimo de risco. Seja
cético, aponte furos, e recomende UMA arquitetura. Conciso.

FATOS VERIFICADOS (não são hipóteses):
- Devices: iPhone 15 (A16, iOS 26 build 23F77) + iPad Air 4 (A14, iOS 26). NENHUM dos dois
  é elegível a Apple Intelligence (requer A17 Pro+/M-series). Logo FoundationModels
  (.available) é FALSO neles → endpoints generativos retornariam .unavailable.
- O stack Apple-NÃO-generativo (NLContextualEmbedding/embed, Vision/OCR, NLTagger,
  NLLanguageRecognizer) NÃO depende de Apple Intelligence — roda em iOS 16+ qualquer.
- NÃO há host app rodando o AegisDaemon (HTTP 2223) instalado nos devices. MetassistemaApp
  não está instalado; Pequi está instalado mas não expõe o daemon HTTP. Logo HOJE o iOS
  não tem daemon on-device: só rodam as lanes JS puras do plugin (bm25, leiden, multiplex,
  cosine sobre embeddings.jsonl sincronizado via iCloud).
- Mac mini: ZeusDaemonMac v1.15 RODANDO em 127.0.0.1:2223, com Apple Intelligence + todo o
  stack Apple-native. 18/18 endpoints verificados OK hoje. Hardening v1.14/v1.15: bind
  loopback por padrão; peers não-loopback exigem header X-Zeus-Token == env
  ZEUS_DAEMON_TOKEN (compare constant-time); /v1/health aberto p/ discovery; /v1/cmd
  travado. Privacy gate central: conteúdo de Clientes/** (sigiloso) é RECUSADO para
  qualquer daemon não-loopback (remoto).
- Tailscale instalado no iPhone (e no Mac). O plugin já suporta mesh peers
  (localStorage per-device, default vazio) + allowRemoteDaemonFallback (default false) +
  envio do token.
- O daemon iOS CANÔNICO é outro repo (meta-repos/ProjetoAegis + pacote aegis-intelligence
  = Gemma 4 via MLX-Swift). A cópia de AegisDaemon DENTRO deste repo zeus-obsidian-plugin
  é divergente/vestigial: falta dep MLX, e wireUpIfNeeded() do Twin nunca é chamado. Ou
  seja, "ligar o Twin" de verdade exige trabalho no outro repo + app host + pesos Gemma
  (~1.4–2.6GB) como ODR + assinatura/deploy via Xcode.

OPÇÕES:
  A) Deploy de host app iOS com Twin MLX (aegis-intelligence/Gemma 4). Generativo 100%
     offline on-device. Custo: build+assinatura Xcode em 2 devices, dep MLX, pesos multi-GB
     como ODR, limites térmicos A14/A16, trabalho em 2-3 repos. Tempo alto, risco alto.
  B) Relay Tailscale → Mac. iOS plugin manda embed/ocr/generativo para o ZeusDaemonMac via
     Tailscale (Mac bind no IP tailscale + ZEUS_DAEMON_TOKEN; iOS configura mesh peer +
     token + allowRemoteDaemonFallback=on). Lanes JS locais. Privacy gate mantém Clientes/**
     só-local (JS). Custo: baixo, sem app novo. Risco: superfície de rede do Mac reaberta
     (mitigada pelo token + Tailscale ACL); depende de conectividade.
  C) Híbrido: B agora (online) + A depois (offline).

PERGUNTAS A RESPONDER:
1. Qual opção entrega "100% funcional no iOS" com menor risco AGORA? Justifique.
2. Reabrir o daemon Mac no Tailscale logo após o hardening v1.14/v1.15 é coerente, ou
   reintroduz o vetor que acabamos de fechar? O token gate + Tailscale ACL bastam?
3. No relay (B), o que QUEBRA? (ex.: Clientes/** bloqueado pelo gate → como o usuário
   percebe? embed de nota sigilosa no iOS simplesmente não acontece?). Liste os limites
   honestos do "100%".
4. Há mudança de CÓDIGO necessária no plugin (este repo) para o relay ser sólido — ou já
   está tudo lá (mesh peers + token + fallback)? Aponte file:line se faltar algo.
5. Para "100%" honesto, o que é INERENTEMENTE impossível sem o app host (ex.: iOS offline
   sem rede + nota sigilosa = sem embed)? Diga claramente.

Leia o código do plugin (main.source.js discovery/mesh/token, lib/zeus-http-client.js
gate/token) para fundamentar a resposta 4. Recomende a arquitetura final.

codex
Vou usar `context-orchestrator` para manter a leitura estreita e `graphify` só na parte de consulta arquitetural/código, porque a pergunta pede fundamento em arquivos do repo. Em seguida vou ler os pontos citados (`main.source.js`, `lib/zeus-http-client.js`) e o relatório Graphify se existir.
exec
/bin/zsh -lc "sed -n '1,180p' /Users/rogermaiocchi/dotfiles/shared/skills/context-orchestrator/SKILL.md" in /Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91
 succeeded in 0ms:
---
name: context-orchestrator
description: >
  Use at the START of every session to minimize context window usage.
  Analyzes the user's task, selects a context profile, and loads ONLY relevant
  memory files and tool schemas. Trigger: /orchestrate or automatically when
  the first user message arrives. Use when context overflow occurs, when
  starting a new task, or when "prompt is too long" errors appear.
---

# Context Orchestrator v2

Orquestrador de contexto baseado em retrieval seletivo por tags, decay scoring,
attention sinks, e retrieval progressivo (SwiftMem + MemGPT + A-MEM + MemoryBank +
StreamingLLM + Stanford Generative Agents + ByteRover + ITR + WISE).

---

## Protocolo de Boot (executar na primeira mensagem)

### Passo 1 — Classificar a tarefa

Ler a primeira mensagem do usuario e classificar em um ou mais perfis:

| Perfil | Palavras-chave de match | Tags MEMORY.md |
|--------|------------------------|----------------|
| `juridico` | processo, caso, cliente, CNJ, OAB, peticao, audiencia, prazo | `#juridico` |
| `vault` | vault, obsidian, nota, pasta, template, frontmatter, diario, governanca | `#vault #governanca` |
| `bot` | capivara, whatsapp, webhook, evolution, piper, tts, telefone | `#bot` |
| `infra` | vps, docker, container, deploy, gcp, nginx, traefik, ssh, servidor | `#infraestrutura` |
| `rag` | embedding, semantica, neo4j, colbert, ner, busca, indexacao | `#rag` |
| `codigo` | bug, feature, refactor, teste, api, endpoint, funcao, modulo | (nenhuma memoria especifica) |
| `geral` | (nenhum match acima) | somente Core |

### Passo 2 — Retrieval com Decay Scoring (Tsinghua MemoryBank + Stanford)

Abrir `MEMORY.md` (ja esta no contexto). Filtrar por tags do perfil detectado.
Ranquear usando formula composta:

```
score = (0.4 * tag_relevance) + (0.3 * imp/5) + (0.3 * recency_score)

Onde:
  tag_relevance = 1.0 se tag faz match direto, 0.5 se tag pai faz match
  imp = campo imp: do MEMORY.md (1-5), normalizado para 0-1
  recency_score = max(0, 1 - dias_desde_modified/90)
    → arquivo acessado hoje = 1.0
    → arquivo de 30 dias atras = 0.67
    → arquivo de 90+ dias = 0.0 (candidato a archival/prune)
```

```
REGRA: Carregar NO MAXIMO 3-5 arquivos por sessao (score > 0.4).
REGRA: Itens [CORE] so carregam se NENHUM perfil especifico fizer match.
REGRA: Itens #desativado NUNCA carregam automaticamente.
REGRA: Ao carregar um arquivo, atualizar seu frontmatter:
       modified: (data de hoje)
       Isso renova a recency e evita decay excessivo.
```

Exemplo de filtragem (SQL-like com scoring):
```sql
SELECT file, (0.4*tag_match + 0.3*imp/5 + 0.3*recency) AS score
FROM MEMORY
WHERE tags INTERSECT (#bot)
  AND tier != 'archival'
ORDER BY score DESC
LIMIT 3;
```

### Passo 3 — Carregar tools por ITR per-step (95% token reduction)

NAO fazer ToolSearch de todos os tools. Aplicar **Instruction-Tool Retrieval (ITR)**:
a cada PASSO da tarefa, carregar SOMENTE os tools necessarios para aquele passo.

```
REGRA CRITICA: Nunca fazer ToolSearch("computer-use") a menos que
o usuario EXPLICITAMENTE peca para controlar a tela.
28+ tools de computer-use = ~3000 tokens desperdicados.

REGRA: Nunca fazer ToolSearch em lote ("*" ou queries amplas).
REGRA: Um servidor MCP por vez, sob demanda, quando o passo exigir.
REGRA: Antes de fazer ToolSearch, perguntar: "Este passo PRECISA
       deste tool, ou posso resolver com Read/Write/Edit/Bash/Grep/Glob?"
```

| Perfil | Tools permitidos via ToolSearch | Bloqueados por padrao |
|--------|-------------------------------|----------------------|
| `juridico` | Docling, Calendar, Gmail | computer-use, Chrome, Preview, Drive |
| `vault` | nenhum extra | TODOS os MCPs |
| `bot` | nenhum extra | TODOS os MCPs |
| `infra` | nenhum extra | TODOS os MCPs |
| `rag` | nenhum extra | computer-use, Chrome, Calendar |
| `codigo` | nenhum extra | computer-use, Chrome, Docling |
| `geral` | sob demanda | TODOS por padrao |

**Tool Usage Log** (Berkeley BFCL V4): manter mentalmente uma lista de tools
ja carregados nesta sessao. Nunca fazer ToolSearch de um tool ja carregado.

### Passo 4 — Orcamento de contexto com Attention Sinks (MIT StreamingLLM)

**Principio dos Attention Sinks (MIT Han Lab):** LLMs ancoram atencao
desproporcional nos tokens iniciais do contexto. Ao compactar:

```
REGRA ATTENTION SINK: Nunca descartar o system prompt, CLAUDE.md,
e o MEMORY.md durante compactacao. Eles sao os "attention sinks" —
ancoras que orientam todo o comportamento subsequente.

REGRA: Ao compactar conversa, preservar:
  1. System prompt + CLAUDE.md (cabeca — attention sink)
  2. Ultimos 5-8 turnos (cauda — contexto imediato)
  3. Descartar turnos intermediarios primeiro (meio)
  Isso segue o padrao head+tail do StreamingLLM.
```

Budget estimado:

| Componente | Tokens | Tier (WISE) | Compactavel? |
|------------|--------|-------------|-------------|
| System prompt | ~5000 | Imutavel | Nao |
| CLAUDE.md + MEMORY.md | ~3000 | Imutavel | Nao |
| Deferred tool names | ~2000 | Imutavel | Nao |
| Skills list | ~2000 | Imutavel | Parcial |
| Tool schemas carregados | ~500-3000 | Mutavel | Sim (evitar carregar) |
| Memorias carregadas | ~1000-5000 | Mutavel | Sim (max 3-5 arquivos) |
| Conversa | cresce | Mutavel | Sim (compactacao auto) |
| **Budget total** | ~200000 | — | — |

---

## Tiers de Memoria (Zhejiang WISE + USTC MemoryLLM)

Cada arquivo de memoria pertence a um tier:

| Tier | Descricao | Carregamento | Exemplos |
|------|-----------|-------------|----------|
| **Imutavel** | Regras e identidade que nunca mudam | Sempre (via CLAUDE.md) | Estilo, perfil, vault precedencia |
| **Core** | Estado ativo dos projetos principais | Sob demanda por tag match | project_whatsapp_bot, project_vault |
| **Recall** | Projetos secundarios e feedback especifico | Sob demanda se score > 0.4 | feedback_mermaid, project_gcp |
| **Archival** | Projetos desativados, historico | Nunca automatico | project_qwen4b, project_pdf_annotator |

```
REGRA: Tiers sao determinados pelo campo imp: no MEMORY.md:
  imp:5 = Imutavel ou Core (sempre/quase-sempre relevante)
  imp:4 = Core (carrega quando perfil faz match)
  imp:3 = Recall (carrega se score > 0.4)
  imp:2 = Recall (carrega sob demanda explicita)
  imp:1 = Archival (nunca carrega automaticamente)
```

---

## Retrieval Progressivo em 3 Niveis (ByteRover Context Tree)

Resolver queries no nivel mais raso possivel antes de carregar mais contexto:

```
Nivel 1 — MEMORY.md index (ja no contexto, ~70 linhas)
  → Responde: "que projetos existem?", "qual tag usar?", "onde fica X?"
  → Se resolve aqui, NAO carregar nenhum arquivo.

Nivel 2 — Carregar 1-3 arquivos por tag match (score > 0.5)
  → Responde: "qual o estado do bot?", "como funciona o vault?"
  → Ler SOMENTE os arquivos top-ranked.

Nivel 3 — Carregar arquivos auxiliares + ponteiros para vault
  → Responde: "preciso dos detalhes da arquitetura X"
  → Ler ponteiros em project_vault_state.md para arquivos do vault
  → Ou usar Agent tool para pesquisa profunda (nao infla contexto principal)
```

```
REGRA: Maioria das queries resolve no Nivel 1 ou 2.
REGRA: Nivel 3 so com necessidade confirmada.
REGRA: Se Nivel 3 envolver leitura de >3 arquivos, usar Agent tool
       (subagente tem contexto proprio, protege o principal).
```

---

exec
/bin/zsh -lc "sed -n '1,180p' /Users/rogermaiocchi/dotfiles/shared/skills/graphify/SKILL.md" in /Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91
 succeeded in 0ms:
---
name: graphify
description: "any input (code, docs, papers, images, videos) to knowledge graph. Use when user asks any question about a codebase, documents, or project content - especially if graphify-out/ exists, treat the question as a /graphify query."
trigger: /graphify
---

# /graphify

Turn any folder of files into a navigable knowledge graph with community detection, an honest audit trail, and three outputs: interactive HTML, GraphRAG-ready JSON, and a plain-language GRAPH_REPORT.md.

## Usage

```
/graphify                                             # full pipeline on current directory → Obsidian vault
/graphify <path>                                      # full pipeline on specific path
/graphify https://github.com/<owner>/<repo>           # clone repo then run full pipeline on it
/graphify https://github.com/<owner>/<repo> --branch <branch>  # clone a specific branch
/graphify <url1> <url2> ...                           # clone multiple repos, build each, merge into one cross-repo graph
/graphify <path> --mode deep                          # thorough extraction, richer INFERRED edges
/graphify <path> --update                             # incremental - re-extract only new/changed files
/graphify <path> --directed                            # build directed graph (preserves edge direction: source→target)
/graphify <path> --whisper-model medium                # use a larger Whisper model for better transcription accuracy
/graphify <path> --cluster-only                       # rerun clustering on existing graph
/graphify <path> --no-viz                             # skip visualization, just report + JSON
/graphify <path> --html                               # (HTML is generated by default - this flag is a no-op)
/graphify <path> --svg                                # also export graph.svg (embeds in Notion, GitHub)
/graphify <path> --graphml                            # export graph.graphml (Gephi, yEd)
/graphify <path> --neo4j                              # generate graphify-out/cypher.txt for Neo4j
/graphify <path> --neo4j-push bolt://localhost:7687   # push directly to Neo4j
/graphify <path> --mcp                                # start MCP stdio server for agent access
/graphify <path> --watch                              # watch folder, auto-rebuild on code changes (no LLM needed)
/graphify <path> --wiki                               # build agent-crawlable wiki (index.md + one article per community)
/graphify <path> --obsidian --obsidian-dir ~/vaults/my-project  # write vault to custom path (e.g. existing vault)
/graphify add <url>                                   # fetch URL, save to ./raw, update graph
/graphify add <url> --author "Name"                   # tag who wrote it
/graphify add <url> --contributor "Name"              # tag who added it to the corpus
/graphify query "<question>"                          # BFS traversal - broad context
/graphify query "<question>" --dfs                    # DFS - trace a specific path
/graphify query "<question>" --budget 1500            # cap answer at N tokens
/graphify path "AuthModule" "Database"                # shortest path between two concepts
/graphify explain "SwinTransformer"                   # plain-language explanation of a node
```

## What graphify is for

Drop any folder of code, docs, papers, images, or video into graphify and get a queryable knowledge graph. Persistent across sessions, honest audit trail (EXTRACTED/INFERRED/AMBIGUOUS), community detection surfaces cross-document connections you wouldn't think to ask about.

## What You Must Do When Invoked

If no path was given, use `.` (current directory). Do not ask the user for a path.

If the path argument starts with `https://github.com/` or `http://github.com/`, treat it as a GitHub URL — run Step 0 before anything else, then continue with the resolved local path.

Follow these steps in order. Do not skip steps.

### Step 0 - Clone GitHub repo(s) (only if a GitHub URL was given)

**Single repo:**
```bash
LOCAL_PATH=$(graphify clone <github-url> [--branch <branch>])
# Use LOCAL_PATH as the target for all subsequent steps
```

**Multiple repos (cross-repo graph):**
```bash
# Clone each repo, run the full pipeline on each, then merge
graphify clone <url1>   # → ~/.graphify/repos/<owner1>/<repo1>
graphify clone <url2>   # → ~/.graphify/repos/<owner2>/<repo2>
# Run /graphify on each local path to produce their graph.json files
# Then merge:
graphify merge-graphs \
  ~/.graphify/repos/<owner1>/<repo1>/graphify-out/graph.json \
  ~/.graphify/repos/<owner2>/<repo2>/graphify-out/graph.json \
  --out graphify-out/cross-repo-graph.json
```

Graphify clones into `~/.graphify/repos/<owner>/<repo>` and reuses existing clones on repeat runs. Each node in the merged graph carries a `repo` attribute so you can filter by origin.

### Step 1 - Ensure graphify is installed

```bash
# Detect the correct Python interpreter (handles uv tool, pipx, venv, system installs)
PYTHON=""
GRAPHIFY_BIN=$(which graphify 2>/dev/null)
# 1. uv tool installs — most reliable on modern Mac/Linux
if [ -z "$PYTHON" ] && command -v uv >/dev/null 2>&1; then
    _UV_PY=$(uv tool run graphifyy python -c "import sys; print(sys.executable)" 2>/dev/null)
    if [ -n "$_UV_PY" ]; then PYTHON="$_UV_PY"; fi
fi
# 2. Read shebang from graphify binary (pipx and direct pip installs)
if [ -z "$PYTHON" ] && [ -n "$GRAPHIFY_BIN" ]; then
    _SHEBANG=$(head -1 "$GRAPHIFY_BIN" | tr -d '#!')
    case "$_SHEBANG" in
        *[!a-zA-Z0-9/_.-]*) ;;
        *) "$_SHEBANG" -c "import graphify" 2>/dev/null && PYTHON="$_SHEBANG" ;;
    esac
fi
# 3. Fall back to python3
if [ -z "$PYTHON" ]; then PYTHON="python3"; fi
"$PYTHON" -c "import graphify" 2>/dev/null || "$PYTHON" -m pip install graphifyy -q 2>/dev/null || "$PYTHON" -m pip install graphifyy -q --break-system-packages 2>&1 | tail -3
# Write interpreter path for all subsequent steps (persists across invocations)
mkdir -p graphify-out
"$PYTHON" -c "import sys; open('graphify-out/.graphify_python', 'w').write(sys.executable)"
# Save scan root so `graphify update` (no args) knows where to look next time
echo "$(cd INPUT_PATH && pwd)" > graphify-out/.graphify_root
```

If the import succeeds, print nothing and move straight to Step 2.

**In every subsequent bash block, replace `python3` with `$(cat graphify-out/.graphify_python)` to use the correct interpreter.**

### Step 2 - Detect files

```bash
$(cat graphify-out/.graphify_python) -c "
import json
from graphify.detect import detect
from pathlib import Path
result = detect(Path('INPUT_PATH'))
print(json.dumps(result))
" > graphify-out/.graphify_detect.json
```

Replace INPUT_PATH with the actual path the user provided. Do NOT cat or print the JSON - read it silently and present a clean summary instead:

```
Corpus: X files · ~Y words
  code:     N files (.py .ts .go ...)
  docs:     N files (.md .txt ...)
  papers:   N files (.pdf ...)
  images:   N files
  video:    N files (.mp4 .mp3 ...)
```

Omit any category with 0 files from the summary.

Then act on it:
- If `total_files` is 0: stop with "No supported files found in [path]."
- If `skipped_sensitive` is non-empty: mention file count skipped, not the file names.
- If `total_words` > 2,000,000 OR `total_files` > 200: show the warning and the top 5 subdirectories by file count, then ask which subfolder to run on. Wait for the user's answer before proceeding.
- Otherwise: proceed directly to Step 2.5 if video files were detected, or Step 3 if not.

### Step 2.5 - Transcribe video / audio files (only if video files detected)

Skip this step entirely if `detect` returned zero `video` files.

Video and audio files cannot be read directly. Transcribe them to text first, then treat the transcripts as doc files in Step 3.

**Strategy:** Read the god nodes from `graphify-out/.graphify_detect.json` (or the analysis file if it exists from a previous run). You are already a language model — write a one-sentence domain hint yourself from those labels. Then pass it to Whisper as the initial prompt. No separate API call needed.

**However**, if the corpus has *only* video files and no other docs/code, use the generic fallback prompt: `"Use proper punctuation and paragraph breaks."`

**Step 1 - Write the Whisper prompt yourself.**

Read the top god node labels from detect output or analysis, then compose a short domain hint sentence, for example:

- Labels: `transformer, attention, encoder, decoder` → `"Machine learning research on transformer architectures and attention mechanisms. Use proper punctuation and paragraph breaks."`
- Labels: `kubernetes, deployment, pod, helm` → `"DevOps discussion about Kubernetes deployments and Helm charts. Use proper punctuation and paragraph breaks."`

Set it as `WHISPER_PROMPT` to use in the next command.

**Step 2 - Transcribe:**

```bash
GRAPHIFY_WHISPER_MODEL=base  # or whatever --whisper-model the user passed
$(cat graphify-out/.graphify_python) -c "
import json, os
from pathlib import Path
from graphify.transcribe import transcribe_all

detect = json.loads(Path('graphify-out/.graphify_detect.json').read_text())
video_files = detect.get('files', {}).get('video', [])
prompt = os.environ.get('GRAPHIFY_WHISPER_PROMPT', 'Use proper punctuation and paragraph breaks.')

transcript_paths = transcribe_all(video_files, initial_prompt=prompt)
print(json.dumps(transcript_paths))
" > graphify-out/.graphify_transcripts.json
```

After transcription:

exec
/bin/zsh -lc "pwd && rg --files | rg '("'^|/)(main'"\\.source\\.js|zeus-http-client\\.js|AGENTS\\.md|graphify-out|Relatorio\\.md)"'$'"'" in /Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91
 succeeded in 0ms:
/Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91
lib/zeus-http-client.js
main.source.js

codex
A tarefa cai em `codigo` + `rag/obsidian`; não vou carregar memória operacional porque os fatos fornecidos e os arquivos-alvo bastam. Agora vou buscar linhas exatas de discovery, peers, token, fallback e privacy gate.
exec
/bin/zsh -lc "find /Users/rogermaiocchi/Metassistema/_Sistema/Graphify/Mapas -path '*Documents-raiz/Relatorio.md' -maxdepth 5 -type f -print 2>/dev/null | head -20" in /Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91
exec
/bin/zsh -lc "find . -maxdepth 3 -iname '*Relatorio.md' -o -path './graphify-out/*' | head -50" in /Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91
 succeeded in 0ms:
 succeeded in 0ms:
exec
/bin/zsh -lc "rg -n \"mesh|peer|allowRemoteDaemonFallback|ZEUS|token|X-Zeus-Token|health|127\\.0\\.0\\.1|localhost|daemon|fallback|Clientes|privacy|sigiloso|remote\" lib/zeus-http-client.js" in /Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91
exec
/bin/zsh -lc "rg -n \"mesh|peer|allowRemoteDaemonFallback|ZEUS|token|X-Zeus-Token|health|127\\.0\\.0\\.1|localhost|daemon|fallback|Clientes|privacy|sigiloso|remote\" main.source.js" in /Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91
 succeeded in 0ms:
2: * ZeusHttpClient — daemon HTTP transport (v0.9 padrão Aegis — full Apple ecosystem + PIA)
4: * Cliente HTTP que fala com daemon Swift nativo rodando em 127.0.0.1:2223
6: * quando o daemon está disponível; faz fallback gracioso para spawn caso contrário.
28: *   POST /v1/prompt                 body: {instruction, max_tokens, deterministic}
42: *   GET  /v1/health
54:    this.baseUrl = (baseUrl || 'http://127.0.0.1:2223').replace(/\/$/, '');
55:    this.healthCache = null;
56:    this.healthCheckedAt = 0;
57:    this.HEALTH_TTL_MS = 30000;   // re-probe daemon health every 30s
62:      bytesIn: 0,        // bytes received from daemon (response payload)
63:      bytesOut: 0,       // bytes sent to daemon (request payload)
70:    // a PERMISSÃO enviada no header; o daemon sinaliza POSSIBILIDADE de cloud.
74:    //   'auto'   → header X-Zeus-Allow-Pcc:auto; daemon flags possibility when load exceeds on-device
75:    // v1.15 — codex #5: "used" virou "possible". O daemon NÃO consegue medir uso
97:   * Snapshot of current metrics + estimated token cost.
98:   * Heuristic: 1 token ~= 4 bytes for English/Portuguese text (very rough).
142:    this.baseUrl = (url || 'http://127.0.0.1:2223').replace(/\/$/, '');
143:    this.healthCache = null;
147:    return /^https?:\/\/(127\.0\.0\.1|localhost|\[::1\])(:\d+)?(\/|$)/i.test(this.baseUrl || '');
152:      // vault-relative (Clientes/...) OU absoluto/aninhado (/Users/.../Clientes/...)
153:      if (/^Clientes\//i.test(path.replace(/^\/+/, ''))) return true;
154:      if (/(^|\/)Clientes\//i.test(path)) return true;
156:    if (payload && payload.privacy && /sigiloso/i.test(String(payload.privacy))) return true;
161:    const privacyPath = options._privacyPath || options.privacyPath || options.path || null;
162:    if (!this._isLoopbackBaseUrl() && this._isPrivatePath(privacyPath, options)) {
164:        `privacy-gate: ${endpoint} recusou enviar conteúdo sigiloso para daemon remoto (${this.baseUrl})`,
169:  // Lazy health check — cached for HEALTH_TTL_MS
174:    if (this.healthCache !== null && (now - this.healthCheckedAt) < this.HEALTH_TTL_MS) {
175:      return this.healthCache;
181:        url: `${this.baseUrl}/v1/health`,
190:      this.healthCache = ok;
192:      this.healthCache = false;
194:    this.healthCheckedAt = Date.now();
195:    return this.healthCache;
206:  // v1.15 — codex #5: o daemon sinaliza `X-Zeus-Pcc-Possible: 1` quando cloud era
221:  // Try to load Obsidian's requestUrl from the obsidian module; fallback to fetch on Node
240:    // Node fallback (testing, scripts)
258:  async _post(endpoint, body, timeoutMs = 60000, privacyCtx = null) {
259:    // v1.14 — privacy gate CENTRALIZADO (codex HIGH #4). Cobre TODOS os endpoints,
261:    // ou de privacyCtx explícito (caso /v1/embed, cujo body é texto já extraído).
262:    const privacyPath = (privacyCtx && (privacyCtx._privacyPath || privacyCtx.privacyPath))
266:    const privacyTag = (privacyCtx && privacyCtx.privacy)
267:      || (body && typeof body === 'object' ? body.privacy : null);
268:    this._assertRawContentAllowed(endpoint, { _privacyPath: privacyPath, privacy: privacyTag });
306:    const { _privacyPath, privacyPath, privacy, ...wireOptions } = options || {};
307:    // privacy gate aplicado centralmente em _post via privacyCtx (body é texto, sem path)
310:      { _privacyPath: _privacyPath || privacyPath, privacy });
315:  // v1.15 — codex round 5: aceita _privacyPath/privacy no options para o gate ver a
318:    const { _privacyPath, privacyPath, privacy, ...wireOptions } = options || {};
320:      { _privacyPath: _privacyPath || privacyPath, privacy });
323:  // v1.3.0 — asp transcribe (SpeechAnalyzer macOS 26+ ou SFSpeechRecognizer fallback)
338:    // v1.5 — daemon /v1/embed só aceita single `text`. Antes da v1.5 isso caía
339:    // no fallback `afm embed` CLI que recebia stdin batch; removido o CLI,
342:    // BATCH=20 já. Eventualmente daemon ganha `{texts:[...]}` nativo.
346:    const { _privacyPath, privacyPath, privacy, ...wireOptions } = options || {};
347:    const privacyCtx = { _privacyPath: _privacyPath || privacyPath, privacy };
351:      const r = await this._post('/v1/embed', { text: t, ...wireOptions }, 30000, privacyCtx);
353:      if (!Array.isArray(v)) throw new Error('embedBatch: daemon não retornou vetor para "' + (t || '').slice(0, 40) + '..."');
373:    // v1.5 — daemon /v1/ocr handler exige `image_path` (ou `image_base64`).
374:    // Pré-v1.5 cliente enviava `path` que caía no CLI fallback; agora precisa
387:  // v1.15 — codex round 5: privacyCtx ({_privacyPath, privacy}) propaga a
389:  // texto vindo de Clientes/** escaparia por endpoints content-only (sem path no body).
390:  async summarize(text, privacyCtx = null) {
391:    return await this._post('/v1/summarize', { text }, 60000, privacyCtx);
394:  async graphExtract(text, maxNodes = 20, maxEdges = 30, privacyCtx = null) {
395:    return await this._post('/v1/graph/extract', { text, max_nodes: maxNodes, max_edges: maxEdges }, 60000, privacyCtx);
398:  async classify(text, options, privacyCtx = null) {
399:    return await this._post('/v1/classify', { text, options }, 60000, privacyCtx);
403:    // options: { max_tokens, deterministic, prewarm, _privacyPath, privacy }
406:      max_tokens: options.max_tokens || 300,
411:      { _privacyPath: options._privacyPath || options.privacyPath, privacy: options.privacy });
468:  // v1.7 — Spotlight via CSSearchableIndex programático (requer daemon ≥ v1.7,
470:  // mas faz fallback gracioso para `/v1/spotlight/search` (mdfind shell) quando
471:  // o daemon bundled ainda for v1.0.0 (HTTP 404).
473:  // mode: 'spotlight' | 'stale' | 'unindexed' | 'mdfind-fallback' | 'error'
475:  //  — fallback honesto declarado em vez de scores silenciosos vazios)
491:      if (/domain_hint obrigat[oó]rio|unsupported_on_aegis_daemon/i.test(nativeMessage)) {
496:      // tentar matchar a forma exata da mensagem, fallback gracioso pra mdfind
500:        return { ...r, mode: 'mdfind-fallback', native_error: nativeMessage.slice(0, 200) };
532:   * Batch extract passports for many notes in one daemon call.
546:   * token-efficient first probe for agents.
580:  async health() {
582:      url: `${this.baseUrl}/v1/health`,
601:  // o daemon retorna 501 com `hint` acionável apontando para o comando do plugin

 succeeded in 0ms:
3: * v0.7.0 — Full Apple ecosystem coverage via Aegis daemon. Novos endpoints consumidos:
12: * v0.6.0 — Aegis-pattern HTTP daemon transport (ADR-018): plugin agnóstico de plataforma,
13: *           mesmo código TS chama daemon Swift nativo via requestUrl em Mac/iPhone/iPad/MacBook.
14: *           v0.5: modular extensions (afm-daemon, hierarchical, multi-vector).
32: * Camadas de reasoning (opcionais, default OFF — janela 4096 tokens com
39: * Binary `afm` em bin/ ou fallback ~/.local/bin/metafm
47:// informativo passa por dbg(), que só imprime quando ZEUS_DEBUG está ligado
49:// sempre visíveis. ZEUS_DEBUG começa false — nenhum spam antes de loadSettings.
50:let ZEUS_DEBUG = false;
51:function dbg(...args) { if (ZEUS_DEBUG) { try { console.log(...args); } catch (_) {} } }
138:  const fallback = [
144:  for (const c of fallback) {
154:  throw new Error('Zeus pluginRequire: cannot locate plugin dir (procurei __dirname, stack, iOS sandbox, glob iCloud, fallbacks)');
156:const _ZEUS_PLUGIN_DIR = _zeusFindPluginDir();
162:    ? _zeusPath.join(_ZEUS_PLUGIN_DIR, rel)
163:    : (_ZEUS_PLUGIN_DIR + '/' + rel).replace(/\/+/g, '/');
166:dbg('[zeus] pluginRequire base:', _ZEUS_PLUGIN_DIR);
187:const ZeusHttpClient = require('./lib/zeus-http-client');    // v0.6: Aegis-pattern daemon HTTP transport (ADR-018)
193:const DaemonLifecycle = require('./lib/daemon-lifecycle');               // v1.5: auto-spawn bin/ZeusDaemonMac (autonomia total Mac)
201:const LexicalIosIndex = require('./lib/lexical-ios');                    // v1.11: BM25 persistido com stems pt-BR para iOS sem daemon (Feature I)
213:// superfície de execução. iOS degrada gracioso quando daemon não é alcançável.
227:  audioEngine: 'auto',                   // sa|sf|auto — daemon escolhe melhor disponível
235:  // FoundationModels reasoning layer (janela 4096 tokens; chunking hierárquico NexusSum
254:  // v0.6.0 — Aegis-pattern HTTP daemon (ADR-018)
255:  zeusDaemonUrl: 'http://127.0.0.1:2223',   // local daemon loopback (neutro; mesh peers ficam em localStorage per-device)
256:  daemonPreferredOverSpawn: true,            // ADR-018 fase E++: HTTP-first em todos hot paths; spawn é fallback no Mac
257:  // v1.4.1 — On-device-first: cada device Apple roda seu próprio daemon nativo
259:  // Tailscale mesh apenas se o daemon local não responder. Quando OFF, Tailscale
260:  // mesh nunca é tentado — modo strict on-device (melhor privacidade + latência).
261:  allowRemoteDaemonFallback: false,
276:  // v1.1 — Status bar: token-saved metrics
278:  statusBarRefreshIntervalMs: 30000,        // 30s refresh para tokens metrics
279:  rawTokenBaseline: 1250,                   // tokens médios sem PIA por request (~5KB/4)
282:  // PERMISSÃO que o client envia, e o daemon sinaliza POSSIBILIDADE de cloud.
283:  // 'off'    = nenhum header de permissão; só on-device (default, privacy máximo)
285:  // 'auto'   = client permite PCC; daemon sinaliza possibilidade quando a carga excede on-device
309:  // CAMADA 1 (default ON): relay HTTP daemon via Tailscale/loopback. iOS chama
310:  //   daemon Mac, persiste em embeddings.jsonl 512-dim NLContextualEmbedding.
327:// Sync sha256 using Node crypto when available; rolling-hash fallback on iOS
353:  // process.platform doesn't exist on iOS (Capacitor) — fallback to UA detection.
358:// HTTP-only dispatcher (ADR-018 Aegis pattern, v1.5 — autonomous daemon)
361:// passa pelo daemon HTTP local (bin/ZeusDaemonMac auto-spawned no Mac via
365:async function tryDaemonOrSpawn(plugin, daemonMethod, daemonArgs /* spawnArgs, stdinText, timeoutMs ignorados */) {
366:  if (!plugin.httpClient || typeof plugin.httpClient[daemonMethod] !== 'function') {
367:    throw new Error(`Daemon method indisponível: ${daemonMethod}`);
371:    throw new Error(`Daemon HTTP fora do ar (${plugin.httpClient.baseUrl}) — ${daemonMethod} não pôde rodar`);
373:  const result = await plugin.httpClient[daemonMethod](...daemonArgs);
374:  return { source: 'daemon', result };
377:// v0.6.1 — Adaptive daemon discovery: tries local loopback, then optional mesh peers.
378:// Per device, prefers: 127.0.0.1 (same device daemon) > user-configured mesh peers.
382:// quebrava em qualquer outra rede. Os peers de mesh agora vêm de localStorage
383:// (per-device, NÃO sincronizado via iCloud), default vazio — sem mesh out-of-the-box.
384:// Configurável em Settings → Zeus → "Daemon mesh peers (local)". Cada device escolhe
385:// seus próprios peers; nada de topologia em data.json (que sincroniza).
386:const ZEUS_MESH_PEERS_KEY = 'zeus.mesh.peers';   // JSON array de URLs http://host:port, per-device
391:    const raw = window.localStorage.getItem(ZEUS_MESH_PEERS_KEY);
404:    if (clean.length === 0) window.localStorage.removeItem(ZEUS_MESH_PEERS_KEY);
405:    else window.localStorage.setItem(ZEUS_MESH_PEERS_KEY, JSON.stringify(clean));
413://   Cada device (Mac mini, MacBook Air, iPad, iPhone) roda seu próprio daemon nativo
414://   em 127.0.0.1:2223. macOS: ZeusDaemonMac (LaunchAgent). iOS: AegisDaemon embedado
415://   no app Aegis. Tailscale mesh é apenas FALLBACK degradado quando o daemon local
418://   Por isso o cache só persiste 127.0.0.1: se cachearmos um peer Tailscale, o device
419://   ficaria preso ali mesmo quando o daemon local subir. Toda boot reavalia o local.
420:const ZEUS_LOCAL_DAEMON_KEY = 'zeus.daemon.url';        // cached URL string (sempre 127.0.0.1:* quando cacheado)
421:const ZEUS_LOCAL_DAEMON_TS_KEY = 'zeus.daemon.ts';      // cached at (ms)
422:const ZEUS_LOCAL_DAEMON_TTL_MS = 12 * 60 * 60 * 1000;   // 12h — re-probe periodicamente
426:  return /^https?:\/\/(127\.0\.0\.1|localhost|\[::1\])(:\d+)?(\/|$)/i.test(url);
432:    const url = window.localStorage.getItem(ZEUS_LOCAL_DAEMON_KEY);
433:    const ts = parseInt(window.localStorage.getItem(ZEUS_LOCAL_DAEMON_TS_KEY) || '0', 10);
435:    if (Date.now() - ts > ZEUS_LOCAL_DAEMON_TTL_MS) return null;
444:      window.localStorage.removeItem(ZEUS_LOCAL_DAEMON_KEY);
445:      window.localStorage.removeItem(ZEUS_LOCAL_DAEMON_TS_KEY);
453:    window.localStorage.setItem(ZEUS_LOCAL_DAEMON_KEY, url);
454:    window.localStorage.setItem(ZEUS_LOCAL_DAEMON_TS_KEY, String(Date.now()));
458:// v1.4.1 — On-device-first daemon discovery.
461://   1. 127.0.0.1:2223  — daemon LOCAL Apple-nativo (ZeusDaemonMac no Mac, AegisDaemon no iOS)
462://   2. localhost:2223  — alternativa de hostname para o mesmo loopback
463://   3. settings.zeusDaemonUrl — override sincronizado via iCloud (geralmente também 127.0.0.1)
464://   4. mesh peers (localStorage, per-device) — fallback degradado SE allowRemoteDaemonFallback estiver ON
470:  const allowRemote = plugin.settings.allowRemoteDaemonFallback === true;  // default false (strict on-device)
479:    // 1+2: loopback (sempre primeiro — daemon local Apple-nativo daquele device)
480:    push('http://127.0.0.1:2223');
481:    push('http://localhost:2223');
484:    // 4: mesh peers remotos (per-device localStorage, só se fallback permitido)
506:    console.warn('[zeus] adaptive daemon discovery → NENHUM daemon respondeu (nem local nem mesh)');
510:  // Prioriza loopback (on-device); se nenhum loopback, usa o de menor idx (mesh ordenado por proximidade).
514:  dbg('[zeus] adaptive daemon discovery → using', chosen.url,
515:    chosen.loopback ? '(LOCAL on-device daemon ✓)' : '(REMOTE Tailscale fallback ⚠)');
587:      // aocr (text in image) — HTTP-first via daemon ocr()
596:        if (r.source === 'daemon') {
603:      // av classify (top-N categories) — HTTP-first via daemon visionClassify()
612:        if (r.source === 'daemon') {
619:      // av landmarks (face detection) — HTTP-first via daemon visionLandmarks()
628:        if (r.source === 'daemon') {
637:      // acs metadata (Spotlight: EXIF, GPS, dates, camera, dimensions) — Mac only, no daemon path
669:        // fallback if not JSON
772:    // Plain aocr path (PDF or fallback for images without av features) — HTTP-first
777:      if (r.source === 'daemon') {
800:      // If --structured fails (macOS <26 or experimental issue), retry without (spawn-only fallback)
815:        } catch (e2) { console.warn('[zeus] aocr fallback also failed', absPath, e2.message); }
893:    if (r.source === 'daemon') {
1069:  // the very rare "no qVec available" fallback, which can't happen on iOS since
1070:  // daemon embed must succeed for search to work).
1102:      if (r.source === 'daemon') {
1130:      // body só se sem qVec (fallback iOS).
1184:// Fits FoundationModels 4096-token window easily (short generation, no tools).
1202:        [instruction, { max_tokens: 300, deterministic: true, prewarm: true, timeoutMs: 90000 }],
1203:        ['prompt', instruction, '--deterministic', '--max-tokens', '300', '--prewarm'],
1208:      if (r.source === 'daemon') {
1234:    // HTTP-first: daemon graphExtract() works on iOS too. v0.11 uses vault.adapter
1239:      // v1.15 — codex round 5: passa filePath como privacyCtx para o gate central
1244:        [stripped, 20, 30, { _privacyPath: filePath }],
1249:      if (r.source === 'daemon') {
1591:  // Size limit: FoundationModels janela é 4096 tokens (~10K chars com safety margin).
1597:    // ADR-018 fase E++: enrich agora roteia via daemon HTTP (works on iOS).
1639:        // Read note content for daemon path — vault.adapter works on Mac and iOS.
1655:        if (r.source === 'daemon') {
1669:          ? 'FoundationModels janela 4096 tokens insuficiente para esta nota + vault context. Tente sub-folder menor ou nota mais curta.'
1710:    if (r.source === 'daemon') {
1782:      text: 'Retorna passports (concepts + summary + domain + difficulty) sem conteúdo bruto — token-eficiente.',
2185:    // ADR-018 fase E++: enrich agora suporta iOS via daemon HTTP — gate só pelo setting.
2285:    const daemonSection = container.createDiv({ cls: 'zeus-status-section' });
2286:    daemonSection.createDiv({ cls: 'zeus-status-section-title', text: 'Daemon HTTP' });
2288:      const health = await this.plugin.httpClient.health();
2290:      this._addStatusRow(daemonSection, 'URL', this.plugin.settings.zeusDaemonUrl);
2291:      this._addStatusRow(daemonSection, 'Status', health.status || 'unreachable', health.status === 'ok' ? 'ok' : 'err');
2292:      this._addStatusRow(daemonSection, 'Platform', health.platform || '?');
2293:      this._addStatusRow(daemonSection, 'Endpoints', String((health.endpoints || []).length));
2294:      this._addStatusRow(daemonSection, 'Tools', String(tools.length));
2295:      this._addStatusRow(daemonSection, 'NLContextualEmbedding', health.nl_available ? '✓' : '✗', health.nl_available ? 'ok' : 'err');
2296:      this._addStatusRow(daemonSection, 'Vision', health.vision_available ? '✓' : '✗', health.vision_available ? 'ok' : 'err');
2297:      this._addStatusRow(daemonSection, 'FoundationModels', health.fm_available ? '✓' : '✗', health.fm_available ? 'ok' : 'warn');
2298:      if (health.translation_available !== undefined) {
2299:        this._addStatusRow(daemonSection, 'Translation', health.translation_available ? '✓' : '✗', health.translation_available ? 'ok' : 'warn');
2302:      this._addStatusRow(daemonSection, 'Status', 'UNREACHABLE: ' + e.message.slice(0, 60), 'err');
2349:    const probeBtn = actions.createEl('button', { text: '⚡ Probe daemon' });
2380:        ZEUS_DEBUG = v;
2388:    desc.appendText(' para PDFs/imagens. Sem BM25 próprio, sem tokenizer próprio, sem bge-micro-v2.');
2391:    const _lcStatus = (this.plugin.daemonLifecycle && this.plugin.daemonLifecycle.lastStatus) || null;
2393:      ? `${_lcStatus.running ? 'ALIVE' : 'DEAD'} (${_lcStatus.source}) — ${this.plugin.daemonLifecycle.url}`
2397:      .setDesc(`Auto-spawn no Mac quando 127.0.0.1:2223 não responde. iOS consome via Tailscale/iCloud read-only. Estado: ${_lcLabel}`);
2416:    new Setting(containerEl).setName('Aegis-pattern HTTP daemon (v0.6, ADR-018)').setHeading();
2419:      .setName('Zeus daemon URL (sincronizado via iCloud)')
2420:      .setDesc('Setting compartilhada. Mantenha em http://127.0.0.1:2223 — cada device Apple roda seu PRÓPRIO daemon nativo (ZeusDaemonMac no macOS, AegisDaemon no iOS) e o discovery sempre tenta o loopback primeiro. Mesh peers (configuráveis abaixo, por device) ficam só como fallback quando o daemon local não está rodando.')
2421:      .addText(t => t.setValue(this.plugin.settings.zeusDaemonUrl).setPlaceholder('http://127.0.0.1:2223').onChange(async v => {
2430:      .setName('Permitir fallback remoto para mesh peers')
2431:      .setDesc('Default OFF (strict on-device): nunca conecta a outro device — exige daemon Apple-nativo local funcionando. ON: se o daemon local 127.0.0.1:2223 não responde, tenta os mesh peers configurados abaixo. Os peers ficam só neste device (localStorage, não sincroniza via iCloud).')
2432:      .addToggle(t => t.setValue(this.plugin.settings.allowRemoteDaemonFallback === true).onChange(async v => {
2433:        this.plugin.settings.allowRemoteDaemonFallback = v;
2440:      .setName('Daemon mesh peers (local, não sincronizado)')
2441:      .setDesc('URLs de daemons remotos para fallback, uma por linha (ex: http://host-ou-ip:2223). Guardado só neste device (localStorage) — nunca vai para data.json sincronizado. Cada device escolhe seus próprios peers; sem topologia hardcoded. Requer o toggle acima ligado e um token de auth válido no daemon remoto.')
2447:            _zeusSetLocalDaemonUrl(null);  // invalida cache para reavaliar com os novos peers
2453:      .setName('Forçar redescoberta de daemon agora')
2454:      .setDesc('Limpa cache localStorage e probe 127.0.0.1 + settings + mesh peers (local) em paralelo. Loopback (daemon local Apple-nativo) sempre ganha quando responde. Use após instalar o daemon local ou mover entre redes.')
2457:        const n = new Notice('Zeus: redescobrindo daemon…', 0);
2465:            new Notice(`Zeus: daemon ${isLocal ? 'LOCAL on-device ✓' : 'REMOTE (fallback Tailscale) ⚠'} em ${url}`, 6000);
2467:            const macHint = 'macOS: rode `bash daemon/scripts/install-mac-daemon.sh` para subir o ZeusDaemonMac via LaunchAgent.';
2469:            new Notice(`Zeus: nenhum daemon respondeu.\n${isMac() ? macHint : iosHint}`, 12000);
2479:      .setName('Prefer daemon over child_process (EXPERIMENTAL)')
2480:      .setDesc('Quando ON: hot path tenta HTTP daemon primeiro, fallback child_process se daemon indisponível. Requer daemon rodando no device. Em iOS, esta é a ÚNICA forma de embed/enrich novos (Capacitor bloqueia spawn). Default OFF até daemon estar deployado.')
2481:      .addToggle(t => t.setValue(this.plugin.settings.daemonPreferredOverSpawn).onChange(async v => {
2482:        this.plugin.settings.daemonPreferredOverSpawn = v;
2527:      .setDesc('Roda `metafm enrich` na nota ativa: FoundationModels lê notas relacionadas via tool-calling e sugere links + conexões. LIMITAÇÃO: FoundationModels tem janela 4096 tokens — system prompt + tool descrições consomem ~1500, sobrando ~2500 para o conteúdo da nota + tool responses. Notas >~2KB ou vault com muitos folders pode estourar (skip silencioso). Default off; ligue se trabalhar com notas curtas. Cache por SHA.')
2550:      .setDesc('Quando ON, comandos de indexação populam data/image-features.jsonl com vetor 768-dim por imagem. Habilita o comando "encontrar imagens similares à atual". Requer daemon Zeus rodando (Mac).')
2560:      .setDesc('Permite o comando "Zeus: buscar via Spotlight nativo" consultar o índice macOS via CSSearchQuery exposto pelo daemon. Funciona apenas no Mac.')
2646:    metricsDesc.appendText('Status bar exibe contagem de docs indexados e, opcionalmente, tokens economizados via ');
2651:      .setName('Mostrar tokens economizados no status bar')
2661:      .setDesc('Atualiza tokens economizados periodicamente. Default 30000 (30s). Aumente para reduzir overhead, diminua para feedback mais responsivo.')
2685:      .setDesc('Zera contadores de tokens, bytes e requests do HTTP client. Útil após mudar baseline ou debugar.')
2699:    pccDesc.appendText(' (notas grandes, agent multi-step com janela 4096 estourada). Requer macOS 26+ Apple Intelligence ativo no device do daemon.');
2703:      .setDesc('off = só on-device (privacy máximo, default). opt-in = client envia header X-Zeus-Allow-Pcc:1; Apple decide caso a caso. auto = client permite PCC; o daemon apenas sinaliza POSSIBILIDADE quando a carga excede on-device — o routing real é decidido pela Apple (sem API pública de controle/inspeção).')
2707:        .addOption('auto', 'auto — daemon decide')
2727:      .setDesc('Inspeciona o modo atual e a contagem de requests onde cloud (PCC) era POSSÍVEL. O daemon não consegue medir uso real de cloud — Apple não expõe API de inspeção; este número é heurístico.')
2893:    ZEUS_DEBUG = !!this.settings.debug;
2935:    // caem para o daemon HTTP via `plugin.httpClient` no caminho moderno.
2938:    // v0.6.0 — ADR-018 Aegis-pattern HTTP daemon client (works on ALL devices: Mac+iOS uniform)
2944:      dbg('[zeus] using per-device cached daemon URL:', _initialDaemonUrl, '(settings:', this.settings.zeusDaemonUrl, ')');
2948:    // v1.5 — Daemon lifecycle: se 127.0.0.1:2223 não responder, plugin sobe
2952:    this.daemonLifecycle = new DaemonLifecycle(this);
2964:        const status = await this.daemonLifecycle.ensureRunning();
2965:        dbg('[zeus] daemon lifecycle:', status);
2967:        // um daemon local em 127.0.0.1:2223, redireciona httpClient pra ele,
2968:        // sobrescrevendo qualquer cache de URL remota (Tailscale peer). Sem
2969:        // esse rebase, plugin spawna localmente mas continua falando com peer
2977:        console.warn('[zeus] daemon lifecycle ensureRunning failed:', e.message);
2988:    // geradas em iOS sem daemon. Precisa estar disponível ANTES do AutoIndexer
3026:    const ZEUS_LOCAL_DEVICE_ID_KEY = 'zeus.device.id';
3030:        _localDeviceId = window.localStorage.getItem(ZEUS_LOCAL_DEVICE_ID_KEY);
3037:          window.localStorage.setItem(ZEUS_LOCAL_DEVICE_ID_KEY, _localDeviceId);
3074:    // verifica se há tasks deixadas por iOS via iCloud e processa via daemon.
3075:    // Boot consume após 20s (deixa daemon estabilizar + iCloud sync inicial).
3088:                // Mac processa com daemon FM via buildOne (que prefere daemon).
3152:    // v0.6.1 — Adaptive daemon discovery (async, doesn't block onload)
3162:          // URL atual morta — varre loopback + settings + mesh peers (local) em paralelo.
3165:            dbg('[zeus] adapting daemon URL from', activeUrl, 'to', discovered);
3173:        // Probe daemon capabilities to log what's available
3174:        const health = await this.httpClient.health();
3176:        dbg('[zeus] daemon health:', health.status, '| platform:', health.platform, '| endpoints:', (health.endpoints || []).length, '| tools:', tools.length, '| url:', activeUrl);
3177:        if (health.status === 'ok') {
3179:          new Notice(`Zeus: daemon ${health.platform || '?'} ${isLocal ? 'LOCAL ✓' : 'REMOTE (Tailscale) ⚠'} · ${(health.endpoints || []).length} endpoints`);
3181:          // v1.4.1 — Instruções per-platform: cada device tem infra Apple nativa para rodar seu daemon.
3182:          const macHint = 'macOS: rode `bash daemon/scripts/install-mac-daemon.sh` para subir o ZeusDaemonMac via LaunchAgent (~/Library/LaunchAgents/com.maiocchi.zeusdaemon.plist).';
3183:          const iosHint = 'iOS: abra o app Aegis no device para iniciar o AegisDaemon (HTTP NIO em 127.0.0.1:2223, paridade total com macOS).';
3185:          new Notice(`Zeus: daemon UNREACHABLE em ${activeUrl}.\n${platformHint}\nOu desative "Permitir fallback remoto" para forçar modo strict on-device.`, 15000);
3307:      id: 'zeus-daemon-status',
3308:      name: 'Zeus: status do daemon HTTP (lifecycle)',
3310:        const lc = this.daemonLifecycle;
3318:      id: 'zeus-http-daemon-probe',
3319:      name: 'Zeus: probe HTTP daemon (Aegis-pattern, ADR-018)',
3323:          const health = await this.httpClient.health();
3326:          new Notice(`Daemon: ${health.status || 'unknown'} · platform: ${health.platform || '?'} · ${tools.length} tools`);
3327:          dbg('[zeus] HTTP daemon health:', health, 'tools:', tools);
3418:          const tags = (r && (r.tags || r.tokens)) || [];
3419:          const preview = Array.isArray(tags) ? tags.slice(0, 20).map(t => (t.lemma || t.tag || t.token)).join(' ') : JSON.stringify(r).slice(0, 200);
3421:          new Notice(`Lemma: ${tags.length || '?'} tokens (preview no console; JSON no clipboard)`);
3528:          // nativo com fallback gracioso, declara mode.
3533:          if (r && r.mode === 'mdfind-fallback') {
3534:            new Notice(`Spotlight: ${results.length} results via mdfind fallback (CSSearchQuery não disponível)`, 4000);
3664:        if (!isMac()) { new Notice('PIA build: requer daemon no Mac'); return; }
3795:    // v1.1 — refresh periódico do status bar com token-saved + PCC indicator.
3862:          // Re-embed single file: read content, call daemon /v1/embed, update searcher.embeddings
3866:          const remote = !_zeusIsLoopback(this.httpClient.baseUrl);
3867:          if (remote && IoQueue.isPrivatePath(rel)) {
3868:            console.warn('[zeus] real-time embed blocked by privacy gate:', rel, this.httpClient.baseUrl);
3871:          const resp = await this.httpClient.embed(content.slice(0, 4000), { _privacyPath: rel });
3899:          // Resolver path absoluto (daemon precisa de path absoluto)
3929:          const remote = !_zeusIsLoopback(this.httpClient.baseUrl);
3930:          if (remote && IoQueue.isPrivatePath(rel)) {
3931:            console.warn('[zeus] audio embed blocked by privacy gate:', rel, this.httpClient.baseUrl);
3934:          const resp = await this.httpClient.embed(tr.text.slice(0, 4000), { _privacyPath: rel });
4219:    // codex audit MED A/F: computa domain_hint no JS para que daemon não caia
4221:    // vaultRoot via universal.sha256Hex (Web Crypto fallback no iOS).
4307:              new Notice('Zeus Spotlight: daemon bundled não suporta /v1/spotlight/index. Rebuild via `node scripts/build-release.mjs`.', 9000);
4349:              new Notice('Zeus Spotlight purge: daemon bundled não suporta. Rebuild necessário.', 7000);
4563:          new Notice(`Zeus lexical-ios: ${r.N} notas, ${r.vocab} tokens (${r.elapsedMs}ms)`);
4587:            `${h.path} (score ${h.score.toFixed(2)}, ${h.matched_tokens.length} match)`,
4608:            lines.push(`  Camada 1 (relay daemon): ${probe.ok ? '✓ OK' : '✗ ' + probe.reason}`);
4637:            '  2. iOS Capacitor + Tailscale instalado e mesh ativa',
4638:            '  3. Settings → Zeus → "Permitir fallback remoto via Tailscale" ON',
4641:            'iOS chama daemon Mac via http://<tailscale-ip>:2223/v1/embed',
4647:            '  Quando indisponível Tailscale Mac, fallback local 384-dim.',
4724:    if (this.daemonLifecycle) {
4725:      try { await this.daemonLifecycle.stop(); } catch (e) { console.warn('[zeus] daemon lifecycle stop:', e.message); }
4800:    } else if (state === 'daemon-down') {
4801:      text = `⚠️ Zeus daemon offline`;
4804:      // v1.1 — token-saved metrics
4821:  // Cada request via daemon devolve um passport compacto (~300B) ao invés do
4822:  // conteúdo bruto (~5KB médio). Estimativa: tokens_saved = (raw_baseline -
4823:  // actual_tokens) por request, somado ao longo da sessão.

codex
Não encontrei `Documents-raiz/Relatorio.md` no caminho canônico informado, então vou fundamentar pela leitura direta dos arquivos. A primeira varredura confirma que o repo já tem discovery local→mesh, fallback remoto opt-in e gate centralizado no client; falta checar se o token realmente é enviado em todo POST/health.
exec
/bin/zsh -lc "nl -ba lib/zeus-http-client.js | sed -n '50,170p'" in /Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91
exec
/bin/zsh -lc "nl -ba lib/zeus-http-client.js | sed -n '206,320p'" in /Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91
 succeeded in 0ms:
    50	const universal = require('./universal-fs');
    51	
    52	class ZeusHttpClient {
    53	  constructor(baseUrl) {
    54	    this.baseUrl = (baseUrl || 'http://127.0.0.1:2223').replace(/\/$/, '');
    55	    this.healthCache = null;
    56	    this.healthCheckedAt = 0;
    57	    this.HEALTH_TTL_MS = 30000;   // re-probe daemon health every 30s
    58	
    59	    // v0.9 — Token economics instrumentation
    60	    this.metrics = {
    61	      requests: 0,
    62	      bytesIn: 0,        // bytes received from daemon (response payload)
    63	      bytesOut: 0,       // bytes sent to daemon (request payload)
    64	      byEndpoint: new Map(),   // endpoint → { count, bytesIn, bytesOut }
    65	      startedAt: Date.now(),
    66	    };
    67	
    68	    // v2.0 — Apple Cloud Private (PCC) routing. v1.15: o routing real é decidido
    69	    // pela Apple (sem API pública de controle/inspeção). Estes modos só controlam
    70	    // a PERMISSÃO enviada no header; o daemon sinaliza POSSIBILIDADE de cloud.
    71	    // pccMode: 'off' | 'opt-in' | 'auto'
    72	    //   'off'    → no PCC permission header sent; on-device only
    73	    //   'opt-in' → header X-Zeus-Allow-Pcc:1; Apple decides per request
    74	    //   'auto'   → header X-Zeus-Allow-Pcc:auto; daemon flags possibility when load exceeds on-device
    75	    // v1.15 — codex #5: "used" virou "possible". O daemon NÃO consegue medir uso
    76	    // real de PCC (Apple não expõe API de inspeção); só estima possibilidade. Os
    77	    // campos refletem honestamente "cloud era possível", não "cloud foi usado".
    78	    this.pccMode = 'off';
    79	    this.lastPccPossible = false;      // updated from response header X-Zeus-Pcc-Possible
    80	    this.pccPossibleCount = 0;         // total requests onde cloud era possível (heurística)
    81	  }
    82	
    83	  setPccMode(mode) {
    84	    const valid = ['off', 'opt-in', 'auto'];
    85	    this.pccMode = valid.includes(mode) ? mode : 'off';
    86	  }
    87	
    88	  getPccStatus() {
    89	    return {
    90	      mode: this.pccMode,
    91	      lastPossible: this.lastPccPossible,
    92	      possibleCount: this.pccPossibleCount,
    93	    };
    94	  }
    95	
    96	  /**
    97	   * Snapshot of current metrics + estimated token cost.
    98	   * Heuristic: 1 token ~= 4 bytes for English/Portuguese text (very rough).
    99	   */
   100	  getMetrics() {
   101	    const byEndpoint = {};
   102	    for (const [k, v] of this.metrics.byEndpoint) {
   103	      byEndpoint[k] = {
   104	        count: v.count,
   105	        bytesIn: v.bytesIn,
   106	        bytesOut: v.bytesOut,
   107	        estimatedTokens: Math.round((v.bytesIn + v.bytesOut) / 4),
   108	      };
   109	    }
   110	    return {
   111	      requests: this.metrics.requests,
   112	      bytesIn: this.metrics.bytesIn,
   113	      bytesOut: this.metrics.bytesOut,
   114	      estimatedTokens: Math.round((this.metrics.bytesIn + this.metrics.bytesOut) / 4),
   115	      byEndpoint,
   116	      sinceMs: Date.now() - this.metrics.startedAt,
   117	    };
   118	  }
   119	
   120	  resetMetrics() {
   121	    this.metrics = {
   122	      requests: 0,
   123	      bytesIn: 0,
   124	      bytesOut: 0,
   125	      byEndpoint: new Map(),
   126	      startedAt: Date.now(),
   127	    };
   128	  }
   129	
   130	  _recordMetric(endpoint, bytesOut, bytesIn) {
   131	    this.metrics.requests++;
   132	    this.metrics.bytesOut += bytesOut;
   133	    this.metrics.bytesIn += bytesIn;
   134	    let row = this.metrics.byEndpoint.get(endpoint);
   135	    if (!row) { row = { count: 0, bytesIn: 0, bytesOut: 0 }; this.metrics.byEndpoint.set(endpoint, row); }
   136	    row.count++;
   137	    row.bytesIn += bytesIn;
   138	    row.bytesOut += bytesOut;
   139	  }
   140	
   141	  setBaseUrl(url) {
   142	    this.baseUrl = (url || 'http://127.0.0.1:2223').replace(/\/$/, '');
   143	    this.healthCache = null;
   144	  }
   145	
   146	  _isLoopbackBaseUrl() {
   147	    return /^https?:\/\/(127\.0\.0\.1|localhost|\[::1\])(:\d+)?(\/|$)/i.test(this.baseUrl || '');
   148	  }
   149	
   150	  _isPrivatePath(path, payload = {}) {
   151	    if (typeof path === 'string' && path) {
   152	      // vault-relative (Clientes/...) OU absoluto/aninhado (/Users/.../Clientes/...)
   153	      if (/^Clientes\//i.test(path.replace(/^\/+/, ''))) return true;
   154	      if (/(^|\/)Clientes\//i.test(path)) return true;
   155	    }
   156	    if (payload && payload.privacy && /sigiloso/i.test(String(payload.privacy))) return true;
   157	    return false;
   158	  }
   159	
   160	  _assertRawContentAllowed(endpoint, options = {}) {
   161	    const privacyPath = options._privacyPath || options.privacyPath || options.path || null;
   162	    if (!this._isLoopbackBaseUrl() && this._isPrivatePath(privacyPath, options)) {
   163	      throw new Error(
   164	        `privacy-gate: ${endpoint} recusou enviar conteúdo sigiloso para daemon remoto (${this.baseUrl})`,
   165	      );
   166	    }
   167	  }
   168	
   169	  // Lazy health check — cached for HEALTH_TTL_MS
   170	  // v1.4.1 — timeoutMs (default 1500ms): probes precisam falhar rápido em URLs mortas

 succeeded in 0ms:
   206	  // v1.15 — codex #5: o daemon sinaliza `X-Zeus-Pcc-Possible: 1` quando cloud era
   207	  // POSSÍVEL (heurística), não quando foi comprovadamente usado — Apple não expõe
   208	  // API de inspeção de routing.
   209	  _readPccPossible(headers) {
   210	    if (!headers) return false;
   211	    // Obsidian requestUrl returns headers as plain object; fetch returns Headers instance
   212	    const val = typeof headers.get === 'function'
   213	      ? headers.get('x-zeus-pcc-possible') || headers.get('X-Zeus-Pcc-Possible')
   214	      : (headers['x-zeus-pcc-possible'] || headers['X-Zeus-Pcc-Possible']);
   215	    const possible = val === '1' || val === 'true';
   216	    if (possible) this.pccPossibleCount++;
   217	    this.lastPccPossible = possible;
   218	    return possible;
   219	  }
   220	
   221	  // Try to load Obsidian's requestUrl from the obsidian module; fallback to fetch on Node
   222	  async _requestUrl({ url, method = 'GET', body, contentType = 'application/json', throw: throwOnError = true, signal = null }) {
   223	    let obsidian;
   224	    try { obsidian = require('obsidian'); } catch { obsidian = null; }
   225	
   226	    const pccHeaders = this._pccHeaders();
   227	
   228	    if (obsidian && obsidian.requestUrl) {
   229	      // Obsidian environment — preferred
   230	      const resp = await obsidian.requestUrl({
   231	        url, method, contentType,
   232	        headers: { 'Content-Type': contentType, ...pccHeaders },
   233	        body: typeof body === 'string' ? body : (body ? JSON.stringify(body) : undefined),
   234	        throw: throwOnError,
   235	      });
   236	      this._readPccPossible(resp.headers);
   237	      return resp;
   238	    }
   239	
   240	    // Node fallback (testing, scripts)
   241	    if (typeof fetch === 'function') {
   242	      const resp = await fetch(url, {
   243	        method,
   244	        headers: { 'Content-Type': contentType, ...pccHeaders },
   245	        body: body ? (typeof body === 'string' ? body : JSON.stringify(body)) : undefined,
   246	        signal,
   247	      });
   248	      this._readPccPossible(resp.headers);
   249	      const text = await resp.text();
   250	      let json = null;
   251	      try { json = JSON.parse(text); } catch {}
   252	      return { status: resp.status, text, json, headers: resp.headers };
   253	    }
   254	
   255	    throw new Error('Nenhum transporte HTTP disponível (sem obsidian.requestUrl, sem fetch)');
   256	  }
   257	
   258	  async _post(endpoint, body, timeoutMs = 60000, privacyCtx = null) {
   259	    // v1.14 — privacy gate CENTRALIZADO (codex HIGH #4). Cobre TODOS os endpoints,
   260	    // não só /v1/embed. Deriva o path sensível do body (path/note_path/image_path)
   261	    // ou de privacyCtx explícito (caso /v1/embed, cujo body é texto já extraído).
   262	    const privacyPath = (privacyCtx && (privacyCtx._privacyPath || privacyCtx.privacyPath))
   263	      || (body && typeof body === 'object'
   264	        ? (body.path || body.note_path || body.notePath || body.image_path || null)
   265	        : null);
   266	    const privacyTag = (privacyCtx && privacyCtx.privacy)
   267	      || (body && typeof body === 'object' ? body.privacy : null);
   268	    this._assertRawContentAllowed(endpoint, { _privacyPath: privacyPath, privacy: privacyTag });
   269	
   270	    const ctrl = new (typeof AbortController !== 'undefined' ? AbortController : class { constructor(){this.signal=null;} abort(){} })();
   271	    let timer = null;
   272	    // v0.9 — instrument bytes-out (request payload)
   273	    const bodyStr = typeof body === 'string' ? body : (body ? JSON.stringify(body) : '');
   274	    const bytesOut = bodyStr ? universal.byteLength(bodyStr) : 0;
   275	    try {
   276	      const request = this._requestUrl({
   277	        url: `${this.baseUrl}${endpoint}`,
   278	        method: 'POST',
   279	        body: bodyStr,
   280	        signal: ctrl.signal,
   281	      });
   282	      const timeout = new Promise((_, reject) => {
   283	        timer = setTimeout(() => {
   284	          try { if (ctrl.abort) ctrl.abort(); } catch {}
   285	          reject(new Error(`Daemon ${endpoint}: timeout após ${timeoutMs}ms`));
   286	        }, timeoutMs);
   287	      });
   288	      const resp = await Promise.race([request, timeout]);
   289	      // v0.9 — instrument bytes-in (response payload)
   290	      const respText = resp.text || (resp.json ? JSON.stringify(resp.json) : '');
   291	      const bytesIn = respText ? universal.byteLength(respText) : 0;
   292	      this._recordMetric(endpoint, bytesOut, bytesIn);
   293	
   294	      if (resp.status >= 400) {
   295	        const err = (resp.json && resp.json.error) || resp.text || `HTTP ${resp.status}`;
   296	        throw new Error(`Daemon ${endpoint}: ${err}`);
   297	      }
   298	      return resp.json || JSON.parse(resp.text);
   299	    } finally {
   300	      clearTimeout(timer);
   301	    }
   302	  }
   303	
   304	  // High-level API mirroring afm CLI subcommands
   305	  async embed(text, options = {}) {
   306	    const { _privacyPath, privacyPath, privacy, ...wireOptions } = options || {};
   307	    // privacy gate aplicado centralmente em _post via privacyCtx (body é texto, sem path)
   308	    // Returns { vectors: [[...]], dim, model, count }
   309	    return await this._post('/v1/embed', { text, ...wireOptions }, 60000,
   310	      { _privacyPath: _privacyPath || privacyPath, privacy });
   311	  }
   312	
   313	  // v1.3.0 — afm refine (Writing Tools nativo)
   314	  // mode: "proofread|rewrite|simplify"; tone para rewrite: "academic|professional|casual"
   315	  // v1.15 — codex round 5: aceita _privacyPath/privacy no options para o gate ver a
   316	  // proveniência do `text` (conteúdo já extraído, sem path no body).
   317	  async refine(text, mode = 'proofread', options = {}) {
   318	    const { _privacyPath, privacyPath, privacy, ...wireOptions } = options || {};
   319	    return await this._post('/v1/afm/refine', { text, mode, ...wireOptions }, 90000,
   320	      { _privacyPath: _privacyPath || privacyPath, privacy });

exec
/bin/zsh -lc "nl -ba main.source.js | sed -n '377,520p'" in /Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91
exec
/bin/zsh -lc "nl -ba main.source.js | sed -n '254,320p'" in /Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91
 succeeded in 0ms:
   254	  // v0.6.0 — Aegis-pattern HTTP daemon (ADR-018)
   255	  zeusDaemonUrl: 'http://127.0.0.1:2223',   // local daemon loopback (neutro; mesh peers ficam em localStorage per-device)
   256	  daemonPreferredOverSpawn: true,            // ADR-018 fase E++: HTTP-first em todos hot paths; spawn é fallback no Mac
   257	  // v1.4.1 — On-device-first: cada device Apple roda seu próprio daemon nativo
   258	  // (ZeusDaemonMac no macOS, AegisDaemon no iOS). Quando ON, discovery cai para
   259	  // Tailscale mesh apenas se o daemon local não responder. Quando OFF, Tailscale
   260	  // mesh nunca é tentado — modo strict on-device (melhor privacidade + latência).
   261	  allowRemoteDaemonFallback: false,
   262	  // v0.7.0 — full Apple ecosystem coverage
   263	  imagesIndexFeaturePrint: false,           // se ON, comandos de indexação de imagens populam data/image-features.jsonl
   264	  autoLanguageDetectOnSave: false,          // detecta língua na nota ativa ao salvar e adiciona ao frontmatter (`lang:`)
   265	  spotlightQueryEnabled: false,             // permite Zeus consultar Spotlight nativo macOS via CSSearchQuery
   266	  // v0.8.0 — native Obsidian Graph integration
   267	  nativeGraphIntegration: false,            // opt-in: auto-write zeus_related: in frontmatter (modifica TODAS as notas)
   268	  nativeGraphTopN: 5,                       // top N neighbors per note
   269	  nativeGraphMinScore: 0.3,                 // skip edges below this cosine score
   270	  nativeGraphSyncOnSave: true,              // resync neighbor after file modify
   271	  // v0.10.0 — cross-device coordination + scheduler
   272	  deviceId: '',                             // v1.15: NÃO persistido em data.json (filtrado em saveSettings); vive em localStorage per-device
   273	  schedulerEnabled: true,                   // default ON — background coordinator sweeps stale passports
   274	  schedulerIntervalMs: 15 * 60 * 1000,      // 15 min default
   275	  coordTtlMs: 60 * 1000,                    // 60s default; iCloud sync delay (5-30s) << TTL
   276	  // v1.1 — Status bar: token-saved metrics
   277	  showTokenSavedInStatusBar: true,          // exibe "k tok saved" via PIA no status bar
   278	  statusBarRefreshIntervalMs: 30000,        // 30s refresh para tokens metrics
   279	  rawTokenBaseline: 1250,                   // tokens médios sem PIA por request (~5KB/4)
   280	  // v2.0 — Apple Cloud Private (ACP / PCC). v1.15: routing real é decidido pela
   281	  // Apple (sem API pública de controle/inspeção); estes modos só controlam a
   282	  // PERMISSÃO que o client envia, e o daemon sinaliza POSSIBILIDADE de cloud.
   283	  // 'off'    = nenhum header de permissão; só on-device (default, privacy máximo)
   284	  // 'opt-in' = client envia X-Zeus-Allow-Pcc:1; Apple decide caso a caso
   285	  // 'auto'   = client permite PCC; daemon sinaliza possibilidade quando a carga excede on-device
   286	  pccMode: 'off',
   287	  pccVisualIndicator: true,                 // exibe ☁️PCC? no status bar quando cloud era possível (heurístico)
   288	  // v1.15 — logging gate (Obsidian guideline): default OFF mostra só warn/error.
   289	  debug: false,                             // ON → dbg() imprime logs informativos no console
   290	  // v1.8 — hybrid search MMR diversify + multiplex graph
   291	  hybridDiversityLambda: 0.5,               // λ ∈ [0,1] da MMR — 1=só relevância, 0=só diversidade
   292	  hybridDiversifyDefault: false,            // se ON, query() aplica MMR por padrão (lambda configurável acima)
   293	  // codex LOW #9: v1.8 mudou baseline do query() incluindo BM25 default-on. Pra
   294	  // compat estrita com v1.7.1, user pode desligar via setting. Default ON é a
   295	  // recomendação — BM25 complementa semantic em casos lexicais (siglas, IDs,
   296	  // nomes próprios) onde cosine sozinho falha.
   297	  hybridBm25Enabled: true,
   298	  multiplexAutoBuild: false,                // se ON, build inicial roda no onload em background
   299	  // v1.11 Feature I — lexical-ios (BM25 persistido com stems pt-BR). Default OFF
   300	  // porque vault grande pode levar 8-12s no iPad. Comando manual "Zeus: rebuild
   301	  // lexical-ios index" disponível pra trigger inicial. Incrementals rodam
   302	  // automaticamente via AutoIndexer ~30s após cada modify.
   303	  lexicalIosAutoBuild: false,
   304	  // v1.10 — AutoIndexer: indexação automática nativa Apple (FSEvents Mac /
   305	  // vault.adapter iOS) orquestrando todas as camadas (passport/base/spotlight/
   306	  // multiplex/leiden) com debounce + cooldown.
   307	  autoIndexEnabled: true,
   308	  // v1.12 — embed iOS two-tier (codex audit C+B aprovado)
   309	  // CAMADA 1 (default ON): relay HTTP daemon via Tailscale/loopback. iOS chama
   310	  //   daemon Mac, persiste em embeddings.jsonl 512-dim NLContextualEmbedding.
   311	  // CAMADA 2 (default OFF, labs): transformers.js + multilingual-e5-small ~118MB
   312	  //   fetch lazy primeiro use. Persiste embeddings-ios.jsonl 384-dim.
   313	  //   v1.12 ENTREGA stub apenas; runtime completo em v1.13 ADR-011 labs.
   314	  iosEmbedRelayEnabled: true,
   315	  iosEmbedTransformersEnabled: false,
   316	  // v1.9 — Leiden communities (escopo enxuto: local move + connectivity split + agregação)
   317	  // Vide docs/ADR-008-Leiden-Communities-JS-Port.md
   318	  leidenResolution: 1.0,                    // γ na modularidade; >1 favorece comunidades menores
   319	  leidenAutoRun: false,                     // se ON, dispara detectCommunities após buildFromVault multiplex
   320	  leidenPropagateFM: false,                 // se ON, escreve zeus_community: NN no frontmatter de cada nota

 succeeded in 0ms:
   377	// v0.6.1 — Adaptive daemon discovery: tries local loopback, then optional mesh peers.
   378	// Per device, prefers: 127.0.0.1 (same device daemon) > user-configured mesh peers.
   379	//
   380	// v1.15 — codex #7 / S8 Check 2: a topologia de rede (IPs Tailscale, hostnames mDNS)
   381	// NÃO é mais hardcoded no fonte. Vazava os devices/IPs pessoais do maintainer e
   382	// quebrava em qualquer outra rede. Os peers de mesh agora vêm de localStorage
   383	// (per-device, NÃO sincronizado via iCloud), default vazio — sem mesh out-of-the-box.
   384	// Configurável em Settings → Zeus → "Daemon mesh peers (local)". Cada device escolhe
   385	// seus próprios peers; nada de topologia em data.json (que sincroniza).
   386	const ZEUS_MESH_PEERS_KEY = 'zeus.mesh.peers';   // JSON array de URLs http://host:port, per-device
   387	
   388	function _zeusGetMeshPeers() {
   389	  try {
   390	    if (typeof window === 'undefined' || !window.localStorage) return [];
   391	    const raw = window.localStorage.getItem(ZEUS_MESH_PEERS_KEY);
   392	    if (!raw) return [];
   393	    const arr = JSON.parse(raw);
   394	    return Array.isArray(arr) ? arr.filter(u => typeof u === 'string' && /^https?:\/\//.test(u)) : [];
   395	  } catch { return []; }
   396	}
   397	
   398	function _zeusSetMeshPeers(urls) {
   399	  try {
   400	    if (typeof window === 'undefined' || !window.localStorage) return;
   401	    const clean = (Array.isArray(urls) ? urls : [])
   402	      .map(u => String(u).trim())
   403	      .filter(u => /^https?:\/\//.test(u));
   404	    if (clean.length === 0) window.localStorage.removeItem(ZEUS_MESH_PEERS_KEY);
   405	    else window.localStorage.setItem(ZEUS_MESH_PEERS_KEY, JSON.stringify(clean));
   406	  } catch {}
   407	}
   408	
   409	// v1.4.1 — Per-device cache: localStorage não é sincronizado via iCloud, então cada device
   410	// memoriza sua própria URL funcional. Evita re-discovery em todo startup.
   411	//
   412	// IMPORTANTE — arquitetura on-device-first (Apple-native em TODOS os devices):
   413	//   Cada device (Mac mini, MacBook Air, iPad, iPhone) roda seu próprio daemon nativo
   414	//   em 127.0.0.1:2223. macOS: ZeusDaemonMac (LaunchAgent). iOS: AegisDaemon embedado
   415	//   no app Aegis. Tailscale mesh é apenas FALLBACK degradado quando o daemon local
   416	//   não está rodando — não a rota normal.
   417	//
   418	//   Por isso o cache só persiste 127.0.0.1: se cachearmos um peer Tailscale, o device
   419	//   ficaria preso ali mesmo quando o daemon local subir. Toda boot reavalia o local.
   420	const ZEUS_LOCAL_DAEMON_KEY = 'zeus.daemon.url';        // cached URL string (sempre 127.0.0.1:* quando cacheado)
   421	const ZEUS_LOCAL_DAEMON_TS_KEY = 'zeus.daemon.ts';      // cached at (ms)
   422	const ZEUS_LOCAL_DAEMON_TTL_MS = 12 * 60 * 60 * 1000;   // 12h — re-probe periodicamente
   423	
   424	function _zeusIsLoopback(url) {
   425	  if (!url) return false;
   426	  return /^https?:\/\/(127\.0\.0\.1|localhost|\[::1\])(:\d+)?(\/|$)/i.test(url);
   427	}
   428	
   429	function _zeusGetLocalDaemonUrl() {
   430	  try {
   431	    if (typeof window === 'undefined' || !window.localStorage) return null;
   432	    const url = window.localStorage.getItem(ZEUS_LOCAL_DAEMON_KEY);
   433	    const ts = parseInt(window.localStorage.getItem(ZEUS_LOCAL_DAEMON_TS_KEY) || '0', 10);
   434	    if (!url || !ts) return null;
   435	    if (Date.now() - ts > ZEUS_LOCAL_DAEMON_TTL_MS) return null;
   436	    return url;
   437	  } catch { return null; }
   438	}
   439	
   440	function _zeusSetLocalDaemonUrl(url) {
   441	  try {
   442	    if (typeof window === 'undefined' || !window.localStorage) return;
   443	    if (!url) {
   444	      window.localStorage.removeItem(ZEUS_LOCAL_DAEMON_KEY);
   445	      window.localStorage.removeItem(ZEUS_LOCAL_DAEMON_TS_KEY);
   446	      return;
   447	    }
   448	    // Só cacheia loopback — URL remota não é "estado estável" do device.
   449	    if (!_zeusIsLoopback(url)) {
   450	      dbg('[zeus] cache skip — URL não é loopback (sempre re-probe local primeiro):', url);
   451	      return;
   452	    }
   453	    window.localStorage.setItem(ZEUS_LOCAL_DAEMON_KEY, url);
   454	    window.localStorage.setItem(ZEUS_LOCAL_DAEMON_TS_KEY, String(Date.now()));
   455	  } catch {}
   456	}
   457	
   458	// v1.4.1 — On-device-first daemon discovery.
   459	//
   460	// Ordem fixa de candidatos:
   461	//   1. 127.0.0.1:2223  — daemon LOCAL Apple-nativo (ZeusDaemonMac no Mac, AegisDaemon no iOS)
   462	//   2. localhost:2223  — alternativa de hostname para o mesmo loopback
   463	//   3. settings.zeusDaemonUrl — override sincronizado via iCloud (geralmente também 127.0.0.1)
   464	//   4. mesh peers (localStorage, per-device) — fallback degradado SE allowRemoteDaemonFallback estiver ON
   465	//
   466	// Probes em paralelo com timeout curto (1500ms). MAS o resultado é PRIORIZADO por
   467	// ordem de preferência: se loopback responde, vence sobre Tailscale mesmo que Tailscale
   468	// tenha respondido primeiro. Isso garante "local sempre que possível".
   469	async function discoverDaemonUrl(plugin, candidates = null, probeTimeoutMs = 1500) {
   470	  const allowRemote = plugin.settings.allowRemoteDaemonFallback === true;  // default false (strict on-device)
   471	
   472	  const ordered = [];
   473	  const seen = new Set();
   474	  const push = (u) => { if (u && !seen.has(u)) { seen.add(u); ordered.push(u); } };
   475	
   476	  if (candidates) {
   477	    for (const u of candidates) push(u);
   478	  } else {
   479	    // 1+2: loopback (sempre primeiro — daemon local Apple-nativo daquele device)
   480	    push('http://127.0.0.1:2223');
   481	    push('http://localhost:2223');
   482	    // 3: settings (sincronizado via iCloud — geralmente loopback, mas pode ser custom)
   483	    push(plugin.settings.zeusDaemonUrl);
   484	    // 4: mesh peers remotos (per-device localStorage, só se fallback permitido)
   485	    if (allowRemote) {
   486	      for (const u of _zeusGetMeshPeers()) push(u);
   487	    }
   488	  }
   489	
   490	  const ZeusHttpClientLocal = require('./lib/zeus-http-client');
   491	  // Probe em paralelo — mas pondera resultado pela posição (loopback ganha de remoto).
   492	  const probes = ordered.map((url, idx) => (async () => {
   493	    try {
   494	      const client = new ZeusHttpClientLocal(url);
   495	      const ok = await client.isAvailable(probeTimeoutMs);
   496	      return ok ? { url, idx, loopback: _zeusIsLoopback(url) } : null;
   497	    } catch { return null; }
   498	  })());
   499	
   500	  const results = await Promise.allSettled(probes);
   501	  const winners = results
   502	    .filter(r => r.status === 'fulfilled' && r.value)
   503	    .map(r => r.value);
   504	
   505	  if (winners.length === 0) {
   506	    console.warn('[zeus] adaptive daemon discovery → NENHUM daemon respondeu (nem local nem mesh)');
   507	    return plugin.settings.zeusDaemonUrl;  // mantém o default para tentar de novo depois
   508	  }
   509	
   510	  // Prioriza loopback (on-device); se nenhum loopback, usa o de menor idx (mesh ordenado por proximidade).
   511	  const loopback = winners.find(w => w.loopback);
   512	  const chosen = loopback || winners.sort((a, b) => a.idx - b.idx)[0];
   513	
   514	  dbg('[zeus] adaptive daemon discovery → using', chosen.url,
   515	    chosen.loopback ? '(LOCAL on-device daemon ✓)' : '(REMOTE Tailscale fallback ⚠)');
   516	
   517	  _zeusSetLocalDaemonUrl(chosen.url);   // só cacheia se for loopback (helper filtra)
   518	  return chosen.url;
   519	}
   520	

exec
/bin/zsh -lc "nl -ba main.source.js | sed -n '2416,2468p'" in /Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91
exec
/bin/zsh -lc "nl -ba main.source.js | sed -n '2928,3188p'" in /Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91
 succeeded in 0ms:
  2416	    new Setting(containerEl).setName('Aegis-pattern HTTP daemon (v0.6, ADR-018)').setHeading();
  2417	
  2418	    new Setting(containerEl)
  2419	      .setName('Zeus daemon URL (sincronizado via iCloud)')
  2420	      .setDesc('Setting compartilhada. Mantenha em http://127.0.0.1:2223 — cada device Apple roda seu PRÓPRIO daemon nativo (ZeusDaemonMac no macOS, AegisDaemon no iOS) e o discovery sempre tenta o loopback primeiro. Mesh peers (configuráveis abaixo, por device) ficam só como fallback quando o daemon local não está rodando.')
  2421	      .addText(t => t.setValue(this.plugin.settings.zeusDaemonUrl).setPlaceholder('http://127.0.0.1:2223').onChange(async v => {
  2422	        this.plugin.settings.zeusDaemonUrl = v;
  2423	        await this.plugin.saveSettings();
  2424	        this.plugin.httpClient.setBaseUrl(v);
  2425	        // v1.4.1 — Limpa cache localStorage para que o novo valor seja reprobado.
  2426	        _zeusSetLocalDaemonUrl(null);
  2427	      }));
  2428	
  2429	    new Setting(containerEl)
  2430	      .setName('Permitir fallback remoto para mesh peers')
  2431	      .setDesc('Default OFF (strict on-device): nunca conecta a outro device — exige daemon Apple-nativo local funcionando. ON: se o daemon local 127.0.0.1:2223 não responde, tenta os mesh peers configurados abaixo. Os peers ficam só neste device (localStorage, não sincroniza via iCloud).')
  2432	      .addToggle(t => t.setValue(this.plugin.settings.allowRemoteDaemonFallback === true).onChange(async v => {
  2433	        this.plugin.settings.allowRemoteDaemonFallback = v;
  2434	        await this.plugin.saveSettings();
  2435	        // Invalida cache para forçar reavaliação na próxima discovery
  2436	        _zeusSetLocalDaemonUrl(null);
  2437	      }));
  2438	
  2439	    new Setting(containerEl)
  2440	      .setName('Daemon mesh peers (local, não sincronizado)')
  2441	      .setDesc('URLs de daemons remotos para fallback, uma por linha (ex: http://host-ou-ip:2223). Guardado só neste device (localStorage) — nunca vai para data.json sincronizado. Cada device escolhe seus próprios peers; sem topologia hardcoded. Requer o toggle acima ligado e um token de auth válido no daemon remoto.')
  2442	      .addTextArea(t => {
  2443	        t.setValue(_zeusGetMeshPeers().join('\n'))
  2444	          .setPlaceholder('http://192.168.1.50:2223\nhttp://meu-host.local:2223')
  2445	          .onChange(v => {
  2446	            _zeusSetMeshPeers(String(v).split('\n').map(s => s.trim()).filter(Boolean));
  2447	            _zeusSetLocalDaemonUrl(null);  // invalida cache para reavaliar com os novos peers
  2448	          });
  2449	        t.inputEl.rows = 3;
  2450	      });
  2451	
  2452	    new Setting(containerEl)
  2453	      .setName('Forçar redescoberta de daemon agora')
  2454	      .setDesc('Limpa cache localStorage e probe 127.0.0.1 + settings + mesh peers (local) em paralelo. Loopback (daemon local Apple-nativo) sempre ganha quando responde. Use após instalar o daemon local ou mover entre redes.')
  2455	      .addButton(b => b.setButtonText('Redescobrir').setCta().onClick(async () => {
  2456	        _zeusSetLocalDaemonUrl(null);
  2457	        const n = new Notice('Zeus: redescobrindo daemon…', 0);
  2458	        try {
  2459	          const url = await discoverDaemonUrl(this.plugin);
  2460	          this.plugin.httpClient.setBaseUrl(url);
  2461	          const ok = await this.plugin.httpClient.isAvailable(1500);
  2462	          n.hide();
  2463	          if (ok) {
  2464	            const isLocal = _zeusIsLoopback(url);
  2465	            new Notice(`Zeus: daemon ${isLocal ? 'LOCAL on-device ✓' : 'REMOTE (fallback Tailscale) ⚠'} em ${url}`, 6000);
  2466	          } else {
  2467	            const macHint = 'macOS: rode `bash daemon/scripts/install-mac-daemon.sh` para subir o ZeusDaemonMac via LaunchAgent.';
  2468	            const iosHint = 'iOS: abra o app Aegis para iniciar o AegisDaemon (porta 2223 embedada).';

 succeeded in 0ms:
  2928	    // is Mac-only anyway — gated by isMac() before invocation).
  2929	    const pluginDataPath = (path && this.vaultRoot)
  2930	      ? path.join(this.vaultRoot, this.manifest.dir, DATA_DIR_NAME)
  2931	      : universal.joinPath(this.manifest.dir, DATA_DIR_NAME);
  2932	    // v1.5 — HierarchicalProcessor e MultiVectorEmbedder herdaram dependência
  2933	    // de `afmBin` (spawn child_process). Mantemos a assinatura por compat —
  2934	    // passamos null: ambos os módulos verificam o binário antes de invocar e
  2935	    // caem para o daemon HTTP via `plugin.httpClient` no caminho moderno.
  2936	    this.hierarchical = new HierarchicalProcessor(null, this.settings.hierarchicalThreshold);
  2937	    this.multiVector = new MultiVectorEmbedder(null, pluginDataPath);
  2938	    // v0.6.0 — ADR-018 Aegis-pattern HTTP daemon client (works on ALL devices: Mac+iOS uniform)
  2939	    // v1.4.1 — Prefer per-device cached URL (localStorage, NÃO sincronizado via iCloud).
  2940	    // Isso garante que cada device (Mac mini, MacBook, iPad, iPhone) use a URL que
  2941	    // funciona para ELE, mesmo que settings.zeusDaemonUrl (sincronizado) aponte para outro device.
  2942	    const _initialDaemonUrl = _zeusGetLocalDaemonUrl() || this.settings.zeusDaemonUrl;
  2943	    if (_initialDaemonUrl !== this.settings.zeusDaemonUrl) {
  2944	      dbg('[zeus] using per-device cached daemon URL:', _initialDaemonUrl, '(settings:', this.settings.zeusDaemonUrl, ')');
  2945	    }
  2946	    this.httpClient = new ZeusHttpClient(_initialDaemonUrl);
  2947	
  2948	    // v1.5 — Daemon lifecycle: se 127.0.0.1:2223 não responder, plugin sobe
  2949	    // bin/ZeusDaemonMac sozinho (autonomia "drop-in" sem launchctl manual).
  2950	    // iOS: child_process indisponível → status `no-spawn`, plugin segue em modo
  2951	    // degradado read-only consumindo data/embeddings.jsonl syncado do Mac.
  2952	    this.daemonLifecycle = new DaemonLifecycle(this);
  2953	    if (isMac()) {
  2954	      // v1.6 — inicia FSEvents watcher para observability de iCloud sync. NÃO
  2955	      // dispara re-embed (codex HIGH #3 — vault.on já cobre); só mede latência
  2956	      // do adapter Obsidian vs FSEvents pra detectar quando o adapter perdeu sync.
  2957	      try {
  2958	        const ws = this.nativeWatcher.start();
  2959	        dbg('[zeus] native-watcher:', ws);
  2960	      } catch (e) {
  2961	        console.warn('[zeus] native-watcher start failed:', e.message);
  2962	      }
  2963	      try {
  2964	        const status = await this.daemonLifecycle.ensureRunning();
  2965	        dbg('[zeus] daemon lifecycle:', status);
  2966	        // v1.5.1 — fix P2 codex review: quando o lifecycle sobe (ou reaproveita)
  2967	        // um daemon local em 127.0.0.1:2223, redireciona httpClient pra ele,
  2968	        // sobrescrevendo qualquer cache de URL remota (Tailscale peer). Sem
  2969	        // esse rebase, plugin spawna localmente mas continua falando com peer
  2970	        // remoto — "drop-in/on-device" não cumprido.
  2971	        if (status && status.running && status.url && this.httpClient.baseUrl !== status.url) {
  2972	          dbg('[zeus] httpClient rebase:', this.httpClient.baseUrl, '→', status.url);
  2973	          this.httpClient.setBaseUrl(status.url);
  2974	          _zeusSetLocalDaemonUrl(status.url);
  2975	        }
  2976	      } catch (e) {
  2977	        console.warn('[zeus] daemon lifecycle ensureRunning failed:', e.message);
  2978	      }
  2979	    }
  2980	
  2981	    // v0.7.0 — image similarity search via feature-print cache
  2982	    this.imageSimilarity = new ImageSimilaritySearch(this);
  2983	    // v0.9.0 — Passport Index Architecture (PIA): MCP-first agent surface
  2984	    this.passport = new PassportIndex(this);
  2985	    this.basesGen = new BasesGenerator(this);
  2986	
  2987	    // v1.11 Feature H — IoQueue: fila iCloud-mediada para Mac consumir tarefas
  2988	    // geradas em iOS sem daemon. Precisa estar disponível ANTES do AutoIndexer
  2989	    // (que enfileira quando passport.buildOne falha em iOS).
  2990	    this.ioQueue = new IoQueue(this);
  2991	
  2992	    // v1.11 Feature I — LexicalIosIndex (BM25 persistido com stems pt-BR).
  2993	    // Build full é opt-in via settings.lexicalIosAutoBuild OU comando manual.
  2994	    // Incrementals rodam via AutoIndexer ~30s após modify.
  2995	    this.lexicalIos = new LexicalIosIndex(this);
  2996	    // v1.12 — embed iOS two-tier wiring (codex C+B aprovado)
  2997	    this.embedIos = new EmbedIosLib(this);
  2998	    this.embedRelay = new EmbedIosLib.EmbedRelay(this);
  2999	    if (this.settings.lexicalIosAutoBuild) {
  3000	      setTimeout(async () => {
  3001	        try {
  3002	          dbg('[zeus.lexical-ios] auto-build starting…');
  3003	          const r = await this.lexicalIos.build((msg, pct) => {
  3004	            if (pct % 25 === 0) dbg(`[zeus.lexical-ios] ${pct}%`, msg);
  3005	          });
  3006	          dbg('[zeus.lexical-ios] auto-build done:', r);
  3007	        } catch (e) {
  3008	          console.warn('[zeus.lexical-ios] auto-build failed:', e.message);
  3009	        }
  3010	      }, 8000);
  3011	    }
  3012	
  3013	    // v1.11.1 — codex HIGH #1: AutoIndexer movido para DEPOIS do coordinator setup.
  3014	    // Antes, AutoIndexer.start() rodava sem coordinator.deviceId disponível —
  3015	    // ioQueue.enqueue iOS detection (`/ios|ipad/i.test(deviceId)`) falhava silente.
  3016	
  3017	    // v0.10.0 — Cross-device coordination (claim/release via iCloud-synced locks)
  3018	    this.coordinator = new DistributedCoordinator(this, {
  3019	      deviceId: this.settings.deviceId || undefined,
  3020	      ttlMs: this.settings.coordTtlMs || 60_000,
  3021	    });
  3022	    // v1.4.2 — deviceId é PER-DEVICE: armazenado em localStorage (NÃO sincronizado via iCloud).
  3023	    // data.json sincronizado entre Mac mini, MacBook, iPad, iPhone — persistir deviceId lá
  3024	    // contaminaria todos os devices fazendo eles assumirem a identidade do último que escreveu.
  3025	    // localStorage é per-Obsidian-install, isolado por device.
  3026	    const ZEUS_LOCAL_DEVICE_ID_KEY = 'zeus.device.id';
  3027	    let _localDeviceId = null;
  3028	    try {
  3029	      if (typeof window !== 'undefined' && window.localStorage) {
  3030	        _localDeviceId = window.localStorage.getItem(ZEUS_LOCAL_DEVICE_ID_KEY);
  3031	      }
  3032	    } catch (_) {}
  3033	    if (!_localDeviceId) {
  3034	      _localDeviceId = this.coordinator.deviceId;  // generated by DistributedCoordinator
  3035	      try {
  3036	        if (typeof window !== 'undefined' && window.localStorage) {
  3037	          window.localStorage.setItem(ZEUS_LOCAL_DEVICE_ID_KEY, _localDeviceId);
  3038	        }
  3039	      } catch (_) {}
  3040	      dbg('[zeus] generated per-device deviceId (localStorage):', _localDeviceId);
  3041	    }
  3042	    // settings.deviceId fica em memória apenas — overrides do coordinator com o per-device.
  3043	    const _persistedDeviceId = this.settings.deviceId;  // v1.14 — valor vindo do data.json (pode ser legado)
  3044	    this.settings.deviceId = _localDeviceId;
  3045	    this.coordinator.deviceId = _localDeviceId;
  3046	
  3047	    // v1.11.1 — AutoIndexer (movido para cá após coordinator pronto, codex HIGH #1).
  3048	    this.autoIndexer = new AutoIndexer(this);
  3049	    if (this.settings.autoIndexEnabled !== false) {
  3050	      try {
  3051	        const ai = this.autoIndexer.start();
  3052	        dbg('[zeus] auto-indexer:', ai);
  3053	      } catch (e) {
  3054	        console.warn('[zeus] auto-indexer start failed:', e.message);
  3055	      }
  3056	    }
  3057	    // v1.14 — codex #6 / S8 Check 3: flush de deviceId legado. Se o data.json
  3058	    // sincronizado (iCloud) ainda tinha deviceId persistido, regrava SEM ele
  3059	    // (saveSettings já filtra deviceId via destructuring). Guard evita write
  3060	    // em todo boot: só flusha quando havia valor stale no disco.
  3061	    if (_persistedDeviceId) {
  3062	      dbg('[zeus] flush deviceId legado do data.json sincronizado');
  3063	      try { await this.saveSettings(); } catch (e) { console.warn('[zeus] flush deviceId falhou:', e.message); }
  3064	    }
  3065	    // v0.10.0 — Background scheduler for stale-passport detection + claim-coordinated re-extract
  3066	    this.scheduler = new PassportScheduler(this, {
  3067	      intervalMs: this.settings.schedulerIntervalMs || 15 * 60 * 1000,
  3068	    });
  3069	    if (this.settings.schedulerEnabled) {
  3070	      this.scheduler.start();
  3071	    }
  3072	
  3073	    // v1.11 Feature H — Mac-side ioQueue consumer: periodicamente (15min)
  3074	    // verifica se há tasks deixadas por iOS via iCloud e processa via daemon.
  3075	    // Boot consume após 20s (deixa daemon estabilizar + iCloud sync inicial).
  3076	    if (isMac() && this.ioQueue) {
  3077	      const consumeAllPending = async () => {
  3078	        try {
  3079	          const tasks = await this.ioQueue.list();
  3080	          if (tasks.length === 0) return;
  3081	          dbg('[zeus.io-queue] consumindo', tasks.length, 'tasks pendentes');
  3082	          for (const task of tasks) {
  3083	            await this.ioQueue.consume(task, async (t) => {
  3084	              if (t.type === 'passport') {
  3085	                if (!this.passport || typeof this.passport.buildOne !== 'function') {
  3086	                  return { ok: false, error: 'passport API indisponível' };
  3087	                }
  3088	                // Mac processa com daemon FM via buildOne (que prefere daemon).
  3089	                let absPath = t.path;
  3090	                if (t.path && !t.path.startsWith('/') && this.vaultRoot) {
  3091	                  absPath = this.vaultRoot.replace(/\/$/, '') + '/' + t.path;
  3092	                }
  3093	                try {
  3094	                  // Verifica se passport já existe E sha bate — alreadyDone idempotente.
  3095	                  const existing = await this.passport.getPassport(t.path);
  3096	                  if (existing && existing.sha === t.sha && existing.source !== 'ios-local') {
  3097	                    return { ok: true, alreadyDone: true };
  3098	                  }
  3099	                  await this.passport.buildOne(absPath, []);
  3100	                  return { ok: true };
  3101	                } catch (e) {
  3102	                  return { ok: false, error: e.message };
  3103	                }
  3104	              }
  3105	              // Outros types (embed, spotlight) — Mac-side TODO; por enquanto,
  3106	              // marca alreadyDone pra remover da fila (Mac auto-indexer já cobre).
  3107	              return { ok: true, alreadyDone: true };
  3108	            });
  3109	          }
  3110	        } catch (e) {
  3111	          console.warn('[zeus.io-queue] consume loop failed:', e.message);
  3112	        }
  3113	      };
  3114	      setTimeout(consumeAllPending, 20000);
  3115	      // Periódico — 15min, mesmo intervalo do PassportScheduler para alinhar.
  3116	      this._ioQueueIntervalId = setInterval(consumeAllPending, 15 * 60 * 1000);
  3117	    }
  3118	
  3119	    // v1.8 — Multiplex auto-build (opt-in via settings). Roda em background
  3120	    // após onLayoutReady para não bloquear o startup; falha silenciosa.
  3121	    if (this.settings.multiplexAutoBuild) {
  3122	      setTimeout(async () => {
  3123	        try {
  3124	          dbg('[zeus.multiplex] auto-build starting…');
  3125	          await this.multiplex.buildFromVault((msg, pct) => {
  3126	            if (pct % 25 === 0) dbg(`[zeus.multiplex] ${pct}%`, msg);
  3127	          });
  3128	          await this.multiplex.persist();
  3129	          this._multiplexLoaded = true;
  3130	          const s = this.multiplex.stats();
  3131	          dbg('[zeus.multiplex] auto-build done:', s.total, 'edges');
  3132	          // v1.9 — Leiden auto-run após multiplex pronto. Roda em sequência (não
  3133	          // em paralelo) — Leiden depende do grafo congelado em memória.
  3134	          if (this.settings.leidenAutoRun) {
  3135	            try {
  3136	              dbg('[zeus.leiden] auto-detect starting…');
  3137	              const r = await this.leiden.detectCommunities({
  3138	                resolution: this.settings.leidenResolution || 1.0,
  3139	              });
  3140	              await this.leiden.persist(r);
  3141	              dbg('[zeus.leiden] auto-detect done:', r.stats.communityCount, 'comunidades, Q=', r.modularity.toFixed(3));
  3142	            } catch (e2) {
  3143	              console.warn('[zeus.leiden] auto-detect failed:', e2.message);
  3144	            }
  3145	          }
  3146	        } catch (e) {
  3147	          console.warn('[zeus.multiplex] auto-build failed:', e.message);
  3148	        }
  3149	      }, 5000);
  3150	    }
  3151	
  3152	    // v0.6.1 — Adaptive daemon discovery (async, doesn't block onload)
  3153	    // v1.4.1 — Discovery agora valida/refresh em paralelo: se cache localStorage
  3154	    // está OK, isAvailable() responde rápido; se cache está stale, probes paralelos
  3155	    // encontram URL viável em <2s e atualizam o cache + httpClient para os próximos calls.
  3156	    this.app.workspace.onLayoutReady(async () => {
  3157	      try {
  3158	        // Primeiro: confirma se a URL atual (cache ou settings) responde. Se sim, evita re-probe.
  3159	        const currentOk = await this.httpClient.isAvailable(1500);
  3160	        let activeUrl = this.httpClient.baseUrl;
  3161	        if (!currentOk) {
  3162	          // URL atual morta — varre loopback + settings + mesh peers (local) em paralelo.
  3163	          const discovered = await discoverDaemonUrl(this);
  3164	          if (discovered && discovered !== activeUrl) {
  3165	            dbg('[zeus] adapting daemon URL from', activeUrl, 'to', discovered);
  3166	            this.httpClient.setBaseUrl(discovered);
  3167	            activeUrl = discovered;
  3168	          }
  3169	        } else {
  3170	          // URL atual funciona — refresh do timestamp do cache (sliding TTL).
  3171	          _zeusSetLocalDaemonUrl(activeUrl);
  3172	        }
  3173	        // Probe daemon capabilities to log what's available
  3174	        const health = await this.httpClient.health();
  3175	        const tools = await this.httpClient.tools();
  3176	        dbg('[zeus] daemon health:', health.status, '| platform:', health.platform, '| endpoints:', (health.endpoints || []).length, '| tools:', tools.length, '| url:', activeUrl);
  3177	        if (health.status === 'ok') {
  3178	          const isLocal = _zeusIsLoopback(activeUrl);
  3179	          new Notice(`Zeus: daemon ${health.platform || '?'} ${isLocal ? 'LOCAL ✓' : 'REMOTE (Tailscale) ⚠'} · ${(health.endpoints || []).length} endpoints`);
  3180	        } else {
  3181	          // v1.4.1 — Instruções per-platform: cada device tem infra Apple nativa para rodar seu daemon.
  3182	          const macHint = 'macOS: rode `bash daemon/scripts/install-mac-daemon.sh` para subir o ZeusDaemonMac via LaunchAgent (~/Library/LaunchAgents/com.maiocchi.zeusdaemon.plist).';
  3183	          const iosHint = 'iOS: abra o app Aegis no device para iniciar o AegisDaemon (HTTP NIO em 127.0.0.1:2223, paridade total com macOS).';
  3184	          const platformHint = isMac() ? macHint : iosHint;
  3185	          new Notice(`Zeus: daemon UNREACHABLE em ${activeUrl}.\n${platformHint}\nOu desative "Permitir fallback remoto" para forçar modo strict on-device.`, 15000);
  3186	        }
  3187	      } catch (e) {
  3188	        console.warn('[zeus] adaptive discovery skipped:', e.message);

exec
/bin/zsh -lc "nl -ba main.source.js | sed -n '3858,3890p;3926,3938p;4596,4648p'" in /Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91
 succeeded in 0ms:
  3858	      clearTimeout(this._embedTimers.get(rel));
  3859	      this._embedTimers.set(rel, setTimeout(async () => {
  3860	        this._embedTimers.delete(rel);
  3861	        try {
  3862	          // Re-embed single file: read content, call daemon /v1/embed, update searcher.embeddings
  3863	          const content = await this.app.vault.read(file);
  3864	          const reachable = await this.httpClient.isAvailable();
  3865	          if (!reachable) return;
  3866	          const remote = !_zeusIsLoopback(this.httpClient.baseUrl);
  3867	          if (remote && IoQueue.isPrivatePath(rel)) {
  3868	            console.warn('[zeus] real-time embed blocked by privacy gate:', rel, this.httpClient.baseUrl);
  3869	            return;
  3870	          }
  3871	          const resp = await this.httpClient.embed(content.slice(0, 4000), { _privacyPath: rel });
  3872	          if (resp && resp.vectors && resp.vectors[0]) {
  3873	            // Update in-memory + on-disk
  3874	            const sha = await universal.sha256Hex(content);
  3875	            const entry = { path: rel, sha, mtime: Date.now(), title: file.basename, vec: resp.vectors[0] };
  3876	            this.searcher.embeddings.set(rel, entry);
  3877	            this.indexer.saveEmbeddings(this.searcher.embeddings);
  3878	            this.refreshSmartView();
  3879	            dbg('[zeus] real-time embed:', rel, `dim=${resp.dim}`);
  3880	          }
  3881	        } catch (e) {
  3882	          console.warn('[zeus] real-time embed failed for', rel, e.message);
  3883	        }
  3884	      }, 500));   // 500ms debounce — Apple Notes-style instant
  3885	    };
  3886	
  3887	    // v1.3.3 — real-time audio: VAD pre-filter → transcribe → embed
  3888	    // Debounce 2s (audio writes às vezes não são atômicos)
  3889	    const scheduleAudioTranscribe = (rel, file) => {
  3890	      if (!this.settings.indexOnSave) return;
  3926	          }
  3927	
  3928	          // Embed transcript
  3929	          const remote = !_zeusIsLoopback(this.httpClient.baseUrl);
  3930	          if (remote && IoQueue.isPrivatePath(rel)) {
  3931	            console.warn('[zeus] audio embed blocked by privacy gate:', rel, this.httpClient.baseUrl);
  3932	            return;
  3933	          }
  3934	          const resp = await this.httpClient.embed(tr.text.slice(0, 4000), { _privacyPath: rel });
  3935	          if (resp && resp.vectors && resp.vectors[0]) {
  3936	            const sha = await universal.sha256Hex(tr.text);
  3937	            const entry = {
  3938	              path: rel,
  4596	    });
  4597	
  4598	    // v1.12 — embed iOS two-tier: relay status + install model stub
  4599	    this.addCommand({
  4600	      id: 'zeus-ios-embed-status',
  4601	      name: 'Zeus: status embed iOS (relay Mac + transformers.js)',
  4602	      callback: async () => {
  4603	        try {
  4604	          const lines = ['Zeus iOS embed two-tier:'];
  4605	          // Camada 1: relay status
  4606	          if (this.embedRelay) {
  4607	            const probe = await this.embedRelay.tryEmbed('zeus iOS embed probe');
  4608	            lines.push(`  Camada 1 (relay daemon): ${probe.ok ? '✓ OK' : '✗ ' + probe.reason}`);
  4609	            if (probe.ok) lines.push(`    dim=${probe.dim} · model=${probe.model} · source=${probe.source}`);
  4610	          }
  4611	          // Camada 2: transformers.js
  4612	          if (this.embedIos) {
  4613	            const s = await this.embedIos.stats();
  4614	            lines.push(`  Camada 2 (transformers.js labs): ${s.runtime_installed ? '✓ instalado' : '✗ não instalado'}`);
  4615	            lines.push(`    model=${s.model_id} · dim=${s.dim} · entries=${s.count}`);
  4616	          }
  4617	          new Notice(lines.join('\n'), 12000);
  4618	          dbg('[zeus] iOS embed status:', lines.join(' | '));
  4619	        } catch (e) {
  4620	          new Notice('Zeus iOS embed status falhou: ' + e.message.slice(0, 150));
  4621	        }
  4622	      },
  4623	    });
  4624	
  4625	    this.addCommand({
  4626	      id: 'zeus-ios-embed-install',
  4627	      name: 'Zeus: instalar modelo embed iOS (multilingual-e5-small, labs)',
  4628	      callback: async () => {
  4629	        try {
  4630	          // v1.12 ENTREGA stub: copia instruções pro clipboard (mesmo padrão
  4631	          // MobileCLIP v1.9). Runtime transformers.js full em v1.13 ADR-011.
  4632	          const msg = [
  4633	            'Embed iOS v1.12 — STUB labs. Runtime transformers.js full em v1.13 (ADR-011).',
  4634	            '',
  4635	            'Para v1.12, USE Camada 1 (relay Mac via Tailscale):',
  4636	            '  1. Mac mini / MacBook deve ter ZeusDaemonMac rodando (default)',
  4637	            '  2. iOS Capacitor + Tailscale instalado e mesh ativa',
  4638	            '  3. Settings → Zeus → "Permitir fallback remoto via Tailscale" ON',
  4639	            '  4. iosEmbedRelayEnabled: true (default)',
  4640	            '',
  4641	            'iOS chama daemon Mac via http://<tailscale-ip>:2223/v1/embed',
  4642	            '→ persiste em data/embeddings.jsonl 512-dim NLContextualEmbedding',
  4643	            '→ qualidade Apple-native pt-BR otimizada (Mac-canônico).',
  4644	            '',
  4645	            'Camada 2 (v1.13 labs): transformers.js + Xenova/multilingual-e5-small',
  4646	            '  ~118MB INT8 ONNX cached via Browser Cache API.',
  4647	            '  Quando indisponível Tailscale Mac, fallback local 384-dim.',
  4648	            '  Schema versionado: embeddings-ios.jsonl separado.',

exec
/bin/zsh -lc "rg -n \"X-Zeus-Token|Zeus-Token|daemon.*token|token\" main.source.js lib/zeus-http-client.js lib -g '*.js'" in /Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91
 succeeded in 0ms:
lib/zeus-http-client.js:28: *   POST /v1/prompt                 body: {instruction, max_tokens, deterministic}
lib/zeus-http-client.js:97:   * Snapshot of current metrics + estimated token cost.
lib/zeus-http-client.js:98:   * Heuristic: 1 token ~= 4 bytes for English/Portuguese text (very rough).
lib/zeus-http-client.js:403:    // options: { max_tokens, deterministic, prewarm, _privacyPath, privacy }
lib/zeus-http-client.js:406:      max_tokens: options.max_tokens || 300,
lib/zeus-http-client.js:546:   * token-efficient first probe for agents.
main.source.js:32: * Camadas de reasoning (opcionais, default OFF — janela 4096 tokens com
main.source.js:235:  // FoundationModels reasoning layer (janela 4096 tokens; chunking hierárquico NexusSum
main.source.js:276:  // v1.1 — Status bar: token-saved metrics
main.source.js:278:  statusBarRefreshIntervalMs: 30000,        // 30s refresh para tokens metrics
main.source.js:279:  rawTokenBaseline: 1250,                   // tokens médios sem PIA por request (~5KB/4)
main.source.js:1184:// Fits FoundationModels 4096-token window easily (short generation, no tools).
main.source.js:1202:        [instruction, { max_tokens: 300, deterministic: true, prewarm: true, timeoutMs: 90000 }],
main.source.js:1203:        ['prompt', instruction, '--deterministic', '--max-tokens', '300', '--prewarm'],
main.source.js:1591:  // Size limit: FoundationModels janela é 4096 tokens (~10K chars com safety margin).
main.source.js:1669:          ? 'FoundationModels janela 4096 tokens insuficiente para esta nota + vault context. Tente sub-folder menor ou nota mais curta.'
main.source.js:1782:      text: 'Retorna passports (concepts + summary + domain + difficulty) sem conteúdo bruto — token-eficiente.',
main.source.js:2388:    desc.appendText(' para PDFs/imagens. Sem BM25 próprio, sem tokenizer próprio, sem bge-micro-v2.');
main.source.js:2441:      .setDesc('URLs de daemons remotos para fallback, uma por linha (ex: http://host-ou-ip:2223). Guardado só neste device (localStorage) — nunca vai para data.json sincronizado. Cada device escolhe seus próprios peers; sem topologia hardcoded. Requer o toggle acima ligado e um token de auth válido no daemon remoto.')
main.source.js:2527:      .setDesc('Roda `metafm enrich` na nota ativa: FoundationModels lê notas relacionadas via tool-calling e sugere links + conexões. LIMITAÇÃO: FoundationModels tem janela 4096 tokens — system prompt + tool descrições consomem ~1500, sobrando ~2500 para o conteúdo da nota + tool responses. Notas >~2KB ou vault com muitos folders pode estourar (skip silencioso). Default off; ligue se trabalhar com notas curtas. Cache por SHA.')
main.source.js:2646:    metricsDesc.appendText('Status bar exibe contagem de docs indexados e, opcionalmente, tokens economizados via ');
main.source.js:2651:      .setName('Mostrar tokens economizados no status bar')
main.source.js:2661:      .setDesc('Atualiza tokens economizados periodicamente. Default 30000 (30s). Aumente para reduzir overhead, diminua para feedback mais responsivo.')
main.source.js:2685:      .setDesc('Zera contadores de tokens, bytes e requests do HTTP client. Útil após mudar baseline ou debugar.')
main.source.js:3418:          const tags = (r && (r.tags || r.tokens)) || [];
main.source.js:3419:          const preview = Array.isArray(tags) ? tags.slice(0, 20).map(t => (t.lemma || t.tag || t.token)).join(' ') : JSON.stringify(r).slice(0, 200);
main.source.js:3421:          new Notice(`Lemma: ${tags.length || '?'} tokens (preview no console; JSON no clipboard)`);
main.source.js:3795:    // v1.1 — refresh periódico do status bar com token-saved + PCC indicator.
main.source.js:4563:          new Notice(`Zeus lexical-ios: ${r.N} notas, ${r.vocab} tokens (${r.elapsedMs}ms)`);
main.source.js:4587:            `${h.path} (score ${h.score.toFixed(2)}, ${h.matched_tokens.length} match)`,
main.source.js:4804:      // v1.1 — token-saved metrics
main.source.js:4822:  // conteúdo bruto (~5KB médio). Estimativa: tokens_saved = (raw_baseline -
main.source.js:4823:  // actual_tokens) por request, somado ao longo da sessão.
lib/zeus-http-client.js:28: *   POST /v1/prompt                 body: {instruction, max_tokens, deterministic}
lib/zeus-http-client.js:97:   * Snapshot of current metrics + estimated token cost.
lib/zeus-http-client.js:98:   * Heuristic: 1 token ~= 4 bytes for English/Portuguese text (very rough).
lib/zeus-http-client.js:403:    // options: { max_tokens, deterministic, prewarm, _privacyPath, privacy }
lib/zeus-http-client.js:406:      max_tokens: options.max_tokens || 300,
lib/zeus-http-client.js:546:   * token-efficient first probe for agents.
lib/bm25.js:11: *   - tokenize(text) → list<string>           (lower, [0-9a-zà-ÿ_-]{2,})
lib/bm25.js:13: *   - rankNotes(notes, query)                  → list<{path, score, tokens}>
lib/bm25.js:18: *     [{path: 'a.md', text: 'corpus tokens'}, ...],
lib/bm25.js:21: *   // ranked: [{path: 'a.md', score: 1.23, tokens: ['corpus','tokens']}, ...]
lib/bm25.js:36: *   - tokenize() faz regex global em string lowercased — ~3M tokens/s em laptop M1.
lib/bm25.js:43:// stacks (ex: query "habeas corpus" tokeniza igual no JS e no Py).
lib/bm25.js:46:// chinês não são tokenizados (consideramos vault PT-BR/EN como caso comum).
lib/bm25.js:53: * Quebra `text` em tokens léxicos: lowercase, 2+ letras/dígitos/_/-.
lib/bm25.js:54: * Espelha tokenização do bm25.py canônico; tokens de uma letra são ruído.
lib/bm25.js:59:function tokenize(text) {
lib/bm25.js:72: * @param {string[][]} corpus — lista de docs JÁ tokenizados
lib/bm25.js:73: * @param {string[]} queryTokens — tokens da consulta
lib/bm25.js:110:  // querySet: dedup tokens da query (BM25 não pondera repetição da query)
lib/bm25.js:145: * @returns {Array<{path: string, score: number, tokens: string[]}>}
lib/bm25.js:149:  const queryTokens = tokenize(query);
lib/bm25.js:159:    const tokens = tokenize(notes[i].text || '');
lib/bm25.js:160:    corpus[i] = tokens;
lib/bm25.js:161:    docTokens[i] = tokens;
lib/bm25.js:168:    ranked.push({ path: notes[i].path, score: scores[i], tokens: docTokens[i] });
lib/bm25.js:175:  tokenize,
lib/bm25.js:183:// Smoke test do tokenize + bm25Scores com corpus sintético. Útil pra debug
lib/bm25.js:196:  console.log(`[bm25 demo] tokenize("Habeas Corpus, Lei 14.133"):`, tokenize('Habeas Corpus, Lei 14.133'));
lib/passport-index.js:16: *   - Savings:  60-80% em tokens para queries típicas.
lib/passport-index.js:403:   * Score = concept_overlap(query_tokens, p.concepts) +
lib/passport-index.js:404:   *         bm25Score(query_tokens, p.one_line_summary || basename(p.path))
lib/passport-index.js:406:   * Reusa lib/bm25 — tokenize idêntica, garante interop léxica com o retriever
lib/passport-index.js:407:   * principal. concept_overlap conta tokens da query que aparecem como substring
lib/passport-index.js:427:    const queryTokens = bm25.tokenize(query);
lib/passport-index.js:436:      return bm25.tokenize(summary + ' ' + basename);
lib/passport-index.js:453:      // Concept overlap: tokens da query que aparecem como substring em concept
lib/multiplex-graph.js:16: *   7. spotlight_token_bm25 — placeholder: BM25 sobre tokens spotlight do daemon.
lib/multiplex-graph.js:58:  'spotlight_token_bm25',
lib/multiplex-graph.js:72:  spotlight_token_bm25: 0.6,
lib/multiplex-graph.js:314:    onProgress('build: spotlight_token_bm25 (best-effort)', 80);
lib/multiplex-graph.js:315:    // 7) spotlight_token_bm25 — placeholder/best-effort. Pula se daemon down OU
lib/multiplex-graph.js:321:        onProgress('spotlight_token_bm25: skip (daemon ou vaultRoot indisponíveis)', 82);
lib/multiplex-graph.js:324:      // expor /v1/spotlight/tokens. Por ora não criamos edges deste tipo —
lib/multiplex-graph.js:327:      console.warn('[zeus.multiplex] spotlight_token_bm25 skip:', e.message);
lib/bases-generator.js:96:    // density: heurística "tokens únicos ~ file.size / 6" (aproximação 1 token
lib/bases-generator.js:144:      '    displayName: Density ~tokens',
lib/hierarchical.js:8: * 4096-token context window (~10KB chars, ~2KB after system prompt + tool
lib/hierarchical.js:37:const FINAL_SUMMARY_TARGET_CHARS = 2000; // ~500 tokens
lib/hierarchical.js:208:          ['summarize', '--max-tokens', '100', '--deterministic'],
lib/hierarchical.js:238:      // Most plausible signature: `afm batch summarize <files...> --max-tokens N`
lib/hierarchical.js:243:        ['batch', 'summarize', '--max-tokens', '100', '--deterministic', ...filePaths],
lib/hierarchical.js:368:  // note exceeds the 4096-token FM window. Returns an enrich-shaped object
lib/lexical-ios.js:12: *   - PERSISTÊNCIA: header line + 1 linha por nota com {tokens:[{token,tf}], dl}
lib/lexical-ios.js:17: *   - INCREMENTAL: upsert por path (re-tokeniza só a nota tocada).
lib/lexical-ios.js:22: *      "idf":{token:idf_value, ...}, "last_built":"<ISO>"}
lib/lexical-ios.js:25: *      "tokens":[{"token":"...", "tf":<int>}, ...], "dl":<int>}
lib/lexical-ios.js:27: * Performance esperada (vault ~5k notas, ~500k tokens distintos):
lib/lexical-ios.js:30: *   - incremental: ~5-20ms por nota (re-tokeniza + reescreve)
lib/lexical-ios.js:32: * Reusa tokenize() do bm25.js para garantir interop léxica.
lib/lexical-ios.js:51:// token resultante tiver >= 3 chars (evita over-stem).
lib/lexical-ios.js:54:// tokenize), só adiciona uma camada de canonicalização. Para queries puramente
lib/lexical-ios.js:76: * Normaliza um token: NFD strip-acentos + lowercase + stem pt-BR leve.
lib/lexical-ios.js:77: * @param {string} token
lib/lexical-ios.js:78: * @returns {string|null} token canonicalizado, ou null se < 2 chars
lib/lexical-ios.js:80:function normalizeAndStem(token) {
lib/lexical-ios.js:81:  if (!token || typeof token !== 'string') return null;
lib/lexical-ios.js:83:  let t = token.normalize('NFD').replace(/[̀-ͯ]/g, '').toLowerCase();
lib/lexical-ios.js:101:      break; // só 1 stem por token — evita cascata over-eager
lib/lexical-ios.js:109: * Tokeniza + stem. Reusa bm25Lib.tokenize para a regex base, depois aplica
lib/lexical-ios.js:110: * normalize+stem em cada token.
lib/lexical-ios.js:112: * @returns {string[]} tokens canonicalizados (com repetição para TF)
lib/lexical-ios.js:114:function tokenizeAndStem(text) {
lib/lexical-ios.js:115:  const raw = bm25Lib.tokenize(text);
lib/lexical-ios.js:125: * TF-array compacto: [{token, tf}] ordenado por tf desc (top-200 para limitar
lib/lexical-ios.js:127: * @param {string[]} tokens
lib/lexical-ios.js:128: * @returns {Array<{token:string, tf:number}>}
lib/lexical-ios.js:130:function buildTokenArray(tokens) {
lib/lexical-ios.js:132:  for (const t of tokens) counts.set(t, (counts.get(t) || 0) + 1);
lib/lexical-ios.js:133:  const arr = Array.from(counts.entries()).map(([token, tf]) => ({ token, tf }));
lib/lexical-ios.js:142:    this._docs = new Map(); // path -> { sha, tokens:[{token,tf}], dl }
lib/lexical-ios.js:224:   * Build full index: itera todas notas .md, tokeniza+stem, recomputa header
lib/lexical-ios.js:243:    onProgress(`tokenizando ${notes.length} notas…`, 5);
lib/lexical-ios.js:247:    const df = new Map(); // token → doc frequency
lib/lexical-ios.js:252:        onProgress(`tokenize ${i}/${notes.length}`, Math.round(5 + (90 * i) / notes.length));
lib/lexical-ios.js:261:      const tokens = tokenizeAndStem(content);
lib/lexical-ios.js:262:      if (tokens.length === 0) continue;
lib/lexical-ios.js:263:      const tokArr = buildTokenArray(tokens);
lib/lexical-ios.js:265:      this._docs.set(path, { path, sha, tokens: tokArr, dl: tokens.length });
lib/lexical-ios.js:266:      totalLen += tokens.length;
lib/lexical-ios.js:267:      // df: cada token único nesta nota
lib/lexical-ios.js:269:      for (const t of tokens) seen.add(t);
lib/lexical-ios.js:276:    for (const [token, freq] of df.entries()) {
lib/lexical-ios.js:278:      idf[token] = Math.log(1 + (N - freq + 0.5) / (freq + 0.5));
lib/lexical-ios.js:290:    onProgress(`done — ${N} notas, ${Object.keys(idf).length} tokens únicos (${elapsed}ms)`, 100);
lib/lexical-ios.js:299:   * @returns {Promise<Array<{path:string, score:number, matched_tokens:string[]}>>}
lib/lexical-ios.js:305:    const qTokens = tokenizeAndStem(query);
lib/lexical-ios.js:315:      for (const { token, tf } of (doc.tokens || [])) {
lib/lexical-ios.js:316:        if (qSet.has(token)) tfMap.set(token, tf);
lib/lexical-ios.js:334:        results.push({ path: doc.path, score, matched_tokens: matched });
lib/lexical-ios.js:342:   * Atualização incremental: re-tokeniza UMA nota, atualiza posting list e
lib/lexical-ios.js:380:    const tokens = tokenizeAndStem(content);
lib/lexical-ios.js:381:    const tokArr = buildTokenArray(tokens);
lib/lexical-ios.js:382:    this._docs.set(path, { path, sha: currentSha, tokens: tokArr, dl: tokens.length });
lib/lexical-ios.js:394:  // mas em vault típico (~1k notas, ~200 tokens cada) é <50ms.
lib/lexical-ios.js:406:      // codex HIGH #2: doc.tokens é array de OBJETOS {token, tf} — não array-pair.
lib/lexical-ios.js:407:      // Antes faziamos [token] destructure que retornava undefined, quebrando IDF.
lib/lexical-ios.js:408:      for (const entry of (doc.tokens || [])) {
lib/lexical-ios.js:409:        const token = entry && entry.token;
lib/lexical-ios.js:410:        if (!token || seen.has(token)) continue;
lib/lexical-ios.js:411:        seen.add(token);
lib/lexical-ios.js:412:        df.set(token, (df.get(token) || 0) + 1);
lib/lexical-ios.js:444:module.exports._tokenizeAndStem = tokenizeAndStem;
lib/auto-indexer.js:116:    // 5) v1.11 Feature I — lexical-ios incremental (re-tokeniza só essa nota)

codex
Ponto crítico: apesar do texto de settings mencionar token, a busca literal não achou `X-Zeus-Token` nem campo de token no client. Vou abrir o restante do client e procurar por qualquer cabeçalho de auth para confirmar se isso é lacuna real ou só nome diferente.
exec
/bin/zsh -lc "nl -ba lib/zeus-http-client.js | sed -n '170,240p;320,430p;580,610p'" in /Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91
 succeeded in 0ms:
   170	  // v1.4.1 — timeoutMs (default 1500ms): probes precisam falhar rápido em URLs mortas
   171	  // para que discovery cross-device funcione sem travar Obsidian no startup.
   172	  async isAvailable(timeoutMs = 1500) {
   173	    const now = Date.now();
   174	    if (this.healthCache !== null && (now - this.healthCheckedAt) < this.HEALTH_TTL_MS) {
   175	      return this.healthCache;
   176	    }
   177	    try {
   178	      // Race a probe HTTP contra um timer — Obsidian's requestUrl não respeita AbortController,
   179	      // então usamos Promise.race como timeout externo.
   180	      const probe = this._requestUrl({
   181	        url: `${this.baseUrl}/v1/health`,
   182	        method: 'GET',
   183	        throw: false,
   184	      });
   185	      const timeout = new Promise(resolve =>
   186	        setTimeout(() => resolve({ status: 0, _timedOut: true }), timeoutMs)
   187	      );
   188	      const resp = await Promise.race([probe, timeout]);
   189	      const ok = resp && resp.status >= 200 && resp.status < 300;
   190	      this.healthCache = ok;
   191	    } catch {
   192	      this.healthCache = false;
   193	    }
   194	    this.healthCheckedAt = Date.now();
   195	    return this.healthCache;
   196	  }
   197	
   198	  // v2.0 — Inject PCC routing header into outgoing request headers.
   199	  _pccHeaders() {
   200	    if (this.pccMode === 'off') return {};
   201	    if (this.pccMode === 'auto') return { 'X-Zeus-Allow-Pcc': 'auto' };
   202	    return { 'X-Zeus-Allow-Pcc': '1' };  // opt-in
   203	  }
   204	
   205	  // v2.0 — Read PCC-routing signal from response headers.
   206	  // v1.15 — codex #5: o daemon sinaliza `X-Zeus-Pcc-Possible: 1` quando cloud era
   207	  // POSSÍVEL (heurística), não quando foi comprovadamente usado — Apple não expõe
   208	  // API de inspeção de routing.
   209	  _readPccPossible(headers) {
   210	    if (!headers) return false;
   211	    // Obsidian requestUrl returns headers as plain object; fetch returns Headers instance
   212	    const val = typeof headers.get === 'function'
   213	      ? headers.get('x-zeus-pcc-possible') || headers.get('X-Zeus-Pcc-Possible')
   214	      : (headers['x-zeus-pcc-possible'] || headers['X-Zeus-Pcc-Possible']);
   215	    const possible = val === '1' || val === 'true';
   216	    if (possible) this.pccPossibleCount++;
   217	    this.lastPccPossible = possible;
   218	    return possible;
   219	  }
   220	
   221	  // Try to load Obsidian's requestUrl from the obsidian module; fallback to fetch on Node
   222	  async _requestUrl({ url, method = 'GET', body, contentType = 'application/json', throw: throwOnError = true, signal = null }) {
   223	    let obsidian;
   224	    try { obsidian = require('obsidian'); } catch { obsidian = null; }
   225	
   226	    const pccHeaders = this._pccHeaders();
   227	
   228	    if (obsidian && obsidian.requestUrl) {
   229	      // Obsidian environment — preferred
   230	      const resp = await obsidian.requestUrl({
   231	        url, method, contentType,
   232	        headers: { 'Content-Type': contentType, ...pccHeaders },
   233	        body: typeof body === 'string' ? body : (body ? JSON.stringify(body) : undefined),
   234	        throw: throwOnError,
   235	      });
   236	      this._readPccPossible(resp.headers);
   237	      return resp;
   238	    }
   239	
   240	    // Node fallback (testing, scripts)
   320	      { _privacyPath: _privacyPath || privacyPath, privacy });
   321	  }
   322	
   323	  // v1.3.0 — asp transcribe (SpeechAnalyzer macOS 26+ ou SFSpeechRecognizer fallback)
   324	  // engine: "sa|sf|auto" (default auto); locale: BCP47 (e.g. "pt-BR", "en-US")
   325	  async aspTranscribe(absPath, locale = 'pt-BR', engine = 'auto') {
   326	    return await this._post('/v1/asp/transcribe',
   327	      { path: absPath, locale, engine },
   328	      600000  // 10min para áudios longos + asset download primeira vez
   329	    );
   330	  }
   331	
   332	  // v1.3.0 — asp vad (Voice Activity Detection rápido, pré-filtro)
   333	  async aspVad(absPath) {
   334	    return await this._post('/v1/asp/vad', { path: absPath }, 15000);
   335	  }
   336	
   337	  async embedBatch(texts, options = {}) {
   338	    // v1.5 — daemon /v1/embed só aceita single `text`. Antes da v1.5 isso caía
   339	    // no fallback `afm embed` CLI que recebia stdin batch; removido o CLI,
   340	    // implementamos batch via N chamadas sequenciais. Para vault grande, manter
   341	    // throughput aceitável (~20ms/embed no M2 Pro local) — caller costuma usar
   342	    // BATCH=20 já. Eventualmente daemon ganha `{texts:[...]}` nativo.
   343	    if (!Array.isArray(texts)) {
   344	      throw new Error('embedBatch: requer array de strings');
   345	    }
   346	    const { _privacyPath, privacyPath, privacy, ...wireOptions } = options || {};
   347	    const privacyCtx = { _privacyPath: _privacyPath || privacyPath, privacy };
   348	    const vectors = [];
   349	    let dim = 0, model = '';
   350	    for (const t of texts) {
   351	      const r = await this._post('/v1/embed', { text: t, ...wireOptions }, 30000, privacyCtx);
   352	      const v = (r.vectors && r.vectors[0]) || r.vector;
   353	      if (!Array.isArray(v)) throw new Error('embedBatch: daemon não retornou vetor para "' + (t || '').slice(0, 40) + '..."');
   354	      vectors.push(v);
   355	      if (!dim) { dim = r.dim || v.length; model = r.model || ''; }
   356	    }
   357	    return { vectors, dim, model, count: vectors.length };
   358	  }
   359	
   360	  async enrich(noteContent, notePath, vaultSummary = '') {
   361	    return await this._post('/v1/enrich', {
   362	      note_content: noteContent,
   363	      note_path: notePath,
   364	      vault_summary: vaultSummary,
   365	    }, 90000);
   366	  }
   367	
   368	  async agent(question, pattern = 'auto') {
   369	    return await this._post('/v1/agent', { question, pattern }, 180000);
   370	  }
   371	
   372	  async ocr(filePath, outputFormat = 'text', language = 'pt-BR,en') {
   373	    // v1.5 — daemon /v1/ocr handler exige `image_path` (ou `image_base64`).
   374	    // Pré-v1.5 cliente enviava `path` que caía no CLI fallback; agora precisa
   375	    // bater no contrato Swift exato. `output_format` e `language` ignorados
   376	    // server-side mas mantidos por compat futura.
   377	    const langArr = typeof language === 'string'
   378	      ? language.split(',').map(s => s.trim()).filter(Boolean)
   379	      : (Array.isArray(language) ? language : ['pt-BR', 'en-US']);
   380	    return await this._post('/v1/ocr', {
   381	      image_path: filePath,
   382	      languages: langArr,
   383	      output_format: outputFormat,
   384	    }, 120000);
   385	  }
   386	
   387	  // v1.15 — codex round 5: privacyCtx ({_privacyPath, privacy}) propaga a
   388	  // proveniência do conteúdo já extraído para o gate central em _post. Sem isso,
   389	  // texto vindo de Clientes/** escaparia por endpoints content-only (sem path no body).
   390	  async summarize(text, privacyCtx = null) {
   391	    return await this._post('/v1/summarize', { text }, 60000, privacyCtx);
   392	  }
   393	
   394	  async graphExtract(text, maxNodes = 20, maxEdges = 30, privacyCtx = null) {
   395	    return await this._post('/v1/graph/extract', { text, max_nodes: maxNodes, max_edges: maxEdges }, 60000, privacyCtx);
   396	  }
   397	
   398	  async classify(text, options, privacyCtx = null) {
   399	    return await this._post('/v1/classify', { text, options }, 60000, privacyCtx);
   400	  }
   401	
   402	  async prompt(instruction, options = {}) {
   403	    // options: { max_tokens, deterministic, prewarm, _privacyPath, privacy }
   404	    const body = {
   405	      instruction,
   406	      max_tokens: options.max_tokens || 300,
   407	      deterministic: options.deterministic !== false,
   408	    };
   409	    if (options.prewarm !== undefined) body.prewarm = options.prewarm;
   410	    return await this._post('/v1/prompt', body, options.timeoutMs || 90000,
   411	      { _privacyPath: options._privacyPath || options.privacyPath, privacy: options.privacy });
   412	  }
   413	
   414	  async visionClassify(imagePath, topN = 8) {
   415	    return await this._post('/v1/vision/classify', { path: imagePath, top_n: topN }, 30000);
   416	  }
   417	
   418	  async visionLandmarks(imagePath) {
   419	    return await this._post('/v1/vision/landmarks', { path: imagePath }, 30000);
   420	  }
   421	
   422	  // ----- v0.7 new methods — full Apple ecosystem coverage -----
   423	
   424	  async translate(text, sourceLang, targetLang) {
   425	    return await this._post('/v1/translate', { text, source_lang: sourceLang, target_lang: targetLang }, 30000);
   426	  }
   427	
   428	  async nlTag(text, scheme = 'lemma') {
   429	    return await this._post('/v1/nl/tag', { text, scheme }, 15000);
   430	  }
   580	  async health() {
   581	    const resp = await this._requestUrl({
   582	      url: `${this.baseUrl}/v1/health`,
   583	      method: 'GET',
   584	      throw: false,
   585	    });
   586	    return resp.json || { status: 'unreachable' };
   587	  }
   588	
   589	  async tools() {
   590	    const resp = await this._requestUrl({
   591	      url: `${this.baseUrl}/v1/tools`,
   592	      method: 'GET',
   593	      throw: false,
   594	    });
   595	    return (resp.json && resp.json.tools) || [];
   596	  }
   597	
   598	  // v1.9 — MobileCLIP stub opt-in (ADR-010)
   599	  //
   600	  // Sem modelo instalado em ~/Library/Application Support/Zeus/mobileclip-model/
   601	  // o daemon retorna 501 com `hint` acionável apontando para o comando do plugin
   602	  // "Zeus: instalar modelo MobileCLIP (download manual)". Runtime CoreML real
   603	  // chega em v2.0.
   604	  async mobileclipStatus() {
   605	    try {
   606	      const resp = await this._requestUrl({
   607	        url: `${this.baseUrl}/v1/mobileclip/status`,
   608	        method: 'GET',
   609	        throw: false,
   610	      });

exec
/bin/zsh -lc "rg -n -i \"auth|authorization|bearer|header|secret|password|zeus.*token|token.*zeus|daemonToken|daemon_token|localStorage.*token\" . -g '*.js' -g '*.ts' -g '*.swift' -g '*.md'" in /Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91
exec
/bin/zsh -lc "rg -n \"ZEUS_DAEMON_TOKEN|X-Zeus-Token|constant|health|cmd|bind|host|listen\" daemon bin scripts . -g '*.swift' -g '*.js' -g '*.ts' -g '*.sh' -g '*.md'" in /Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91
 succeeded in 0ms:
./APPLE_NATIVE_ROADMAP.md:7:Python bindings via ctypes para o framework Swift `FoundationModels`. Já instalado em `/opt/homebrew/lib/python3.14/site-packages/apple_fm_sdk/`. Permite migrar partes do pipeline do plugin para um worker layer Python que roda em paralelo ao daemon Swift, sem duplicar responsabilidades.
./lib/native-watcher.js:38:    // codex LOW #1: _adapterSeen criado no constructor para que o listener
./audit-final-consolidado.md:32:| 1 | **Fechar o daemon**: bind default `127.0.0.1`; exigir `--host 0.0.0.0` explícito | CRIT | Codex 1 | `fix(daemon): bind loopback-only by default` |
./audit-final-consolidado.md:33:| 2 | **Autenticar/remover `/v1/cmd`**: implementar middleware `X-Zeus-Token` (já tem spec) antes de `route()`, liberar só `/v1/health` p/ não-loopback; allowlist de comandos OU remover `/v1/cmd` do build | CRIT | Codex 2+3 | `feat(daemon): token auth middleware + lock /v1/cmd` |
./audit-final-consolidado.md:48:| 2. Sem hostname/IP em sync | ✅ | ❌ `data.json` `rogers-mac-mini` |
./audit-final-consolidado.md:54:**Causa raiz única:** estado runtime (paths absolutos, hostname auto-descoberto) sendo gravado em arquivos que sincronizam via iCloud. Ação #4 fecha as duas regressões + o WARN.
./audit-final-consolidado.md:68:> "Cortar v1.4.2-ios no canônico" é **moot** — o canônico já é v1.13.2. E **não se deve cortar nova versão ainda**: os 3 CRITs de segurança do daemon (bind aberto + `/v1/cmd` sem auth) são **release-blocking**, ainda mais com `Clientes/**` (sigiloso) no vault.
./audit-final-consolidado.md:71:1. **Sprint de hardening** — ações #1, #2, #3 (CRITs + privacy gate). Sem isso, qualquer host na tailnet executa comando no Mac mini.
./audit-final-consolidado.md:88:| CRIT #1 — bind default `127.0.0.1` | ✅ FEITO | `lsof`: default loopback-only; LAN recusa conexão |
./audit-final-consolidado.md:89:| CRIT #2/#3 — auth gate `X-Zeus-Token` (Mac + iOS) | ✅ FEITO | `curl` LAN sem token→401, token correto→200, loopback→200; ambos daemons compilam |
./audit-fase3-conformidade.md:47:| 3 | On-device-first (loopback antes de Tailscale) | ✅ CONFORMS | `main.source.js:363-484`: ordem `127.0.0.1` → `localhost` → setting → mesh; `winners.find(w=>w.loopback)` vence; só loopback é cacheado |
./audit-fase3-conformidade.md:48:| 4 | HTTP-first com spawn fallback (macOS) | ✅ CONFORMS | `daemon-lifecycle.js:96-100`: checa `isHealthy(800)` antes; só spawna se health falha; mutex anti-corrida |
./audit-fase3-conformidade.md:63:| 2 | Zero hostnames/IPs em config sincronizada | ❌ **FAIL** | `data.json:41` `"zeusDaemonUrl": "http://rogers-mac-mini:2223"`; `data/lexical-ios.jsonl` tem 2× `rogers-mac-mini`. Default do código é `127.0.0.1` — auto-probe reescreveu o hostname |
./audit-fase3-conformidade.md:70:> **Regressões: Check 1 e Check 2.** Ambas vêm do mesmo mecanismo — auto-probe/indexação **escrevendo estado runtime (paths absolutos, hostname) em arquivos que sincronizam via iCloud**. Num MacBook/iPad/iPhone recebendo via iCloud, esses paths e hostname não resolvem → quebra o on-device-first e vaza topologia.
./lib/daemon-lifecycle.js:10: *   1. /v1/health responde em http://127.0.0.1:2223 → reaproveita (LaunchAgent
./lib/daemon-lifecycle.js:14: *   3. Polling em /v1/health por até 10s. Sucesso → registra como "spawnedByUs",
./lib/daemon-lifecycle.js:34:    this.host = options.host || DEFAULT_HOST;
./lib/daemon-lifecycle.js:35:    this.url = `http://${this.host}:${this.port}`;
./lib/daemon-lifecycle.js:41:    // health-check inicial e cada uma spawnar seu daemon — colisão na porta 2223.
./lib/daemon-lifecycle.js:120:        child = spawn(bin, ['--port', String(this.port), '--host', this.host], {
./lib/daemon-lifecycle.js:153:          finish({ running: false, source: 'spawn-timeout', reason: 'sem /v1/health em 10s' });
./CHANGELOG.md:23:- **Topologia Tailscale hardcoded REMOVIDA** (`TAILSCALE_MESH` com IPs `100.108.x`/hostname `rogers-mac-mini`). Mesh peers agora vêm de `localStorage` per-device (não sincronizado), default vazio, configuráveis em Settings. `allowRemoteDaemonFallback` default `false` (strict on-device).
./CHANGELOG.md:54:- **CRIT #1 — bind loopback por padrão.** `ZeusArgs.host` default passa de `0.0.0.0` para `127.0.0.1` (`daemon/Sources/ZeusDaemonMac/main.swift`). Cross-device via Tailscale agora exige `--host 0.0.0.0` explícito + token. Verificado: `lsof` mostra `127.0.0.1` only no default.
./CHANGELOG.md:55:- **CRIT #2/#3 — auth gate `X-Zeus-Token`.** Novo middleware em `ZeusMacHTTPHandler` e `AegisHTTPHandler`: loopback é confiável (on-device-first); peers não-loopback exigem `X-Zeus-Token` == env `ZEUS_DAEMON_TOKEN` (compare constant-time). `/v1/health` fica aberto para discovery. Fail-closed: sem token configurado, todo peer remoto é recusado mesmo bound em `0.0.0.0`. Fecha o vetor de RCE não-autenticado em `/v1/cmd` na LAN/tailnet. Verificado: LAN sem token → 401, token correto → 200, loopback → 200.
./CHANGELOG.md:182:User pediu "Resolva ❌ skip (sem Swift bridge)" — último gap iOS. Solução: **adicionar 3 handlers Spotlight no AegisDaemon iOS library** (já existente em `daemon/Sources/AegisDaemon/` desde v1.4.0). Quando app host iOS (Capivara OR MetassistemaApp-iOS) embarca AegisDaemon, plugin Capacitor chama loopback `127.0.0.1:2223/v1/spotlight/{index,query,purge}` exatamente como no Mac.
./CHANGELOG.md:200:- Contrato app host: embarcar `AegisDaemon` SwiftPM, inicializar `AegisHTTPServer(port: 2223)` no AppDelegate, declarar `NSLocalNetworkUsageDescription` em Info.plist
./CHANGELOG.md:206:`httpClient.spotlightIndex/QueryNative/Purge` (v1.7) já existem. Em iOS, `discoverDaemonUrl()` testa loopback primeiro — quando AegisDaemon iOS está vivo no app host, plugin descobre automaticamente. AutoIndexer (v1.10) dispara via 15s debounce normalmente.
./CHANGELOG.md:216:| Camada | Mac | iOS Capacitor (sem app host) | iOS com app host AegisDaemon |
./CHANGELOG.md:226:Para v1.13 funcionar **plenamente em iOS**, o **app host** (`Capivara` ou `MetassistemaApp-iOS`) precisa:
./CHANGELOG.md:229:2. Inicializar `AegisHTTPServer(port: 2223, host: "127.0.0.1")` no `AppDelegate`
./CHANGELOG.md:233:**Sem app host** (Obsidian iOS standalone puro): gap permanece como `❌ skip gracioso` — plugin Capacitor não tem caminho Swift nativo, é uma limitação de Capacitor não do Zeus.
./CHANGELOG.md:239:A Roger já tem 2 apps host candidates no seu workspace (capivara/app e meta-repos/meta-metassistema-app) — ambos podem embarcar `AegisDaemon` Swift Package.
./CHANGELOG.md:626:- **`/v1/health` endpoint_count: 40** (era 37 — +3 MobileCLIP routes)
./CHANGELOG.md:887:- **LOW** — `native-watcher` listener `vault.on('modify')` usava `this._adapterSeen.get()` mas `_adapterSeen` era inicializado depois. Movido para constructor.
./CHANGELOG.md:1013:  - Probe `/v1/health` em 127.0.0.1:2223 com timeout 800ms.
./CHANGELOG.md:1015:  - Se morto → `spawn(bin/ZeusDaemonMac, ['--port','2223','--host','127.0.0.1'])` detached:false (lifecycle amarrado ao Obsidian), polling de `/v1/health` por até 10s.
./CHANGELOG.md:1022:- Doctor verifica 7 layers: macOS version, `bin/ZeusDaemonMac` (existe + executável + tamanho), codesign (adhoc/Apple), `main.js`, `manifest.json`, `package.json`, daemon HTTP `/v1/health` (FM/NL/Vision/Speech flags). Exit 0/1/2 CI-friendly.
./CHANGELOG.md:1023:- Smoke exercita endpoints críticos: `/v1/health` (3 asserts), `/v1/embed` (200 + dim=512), `/v1/tools` (count>0), `/v1/refine` (200 + non-empty). Validado 9/9 contra daemon v1.0.0 em produção.
./CHANGELOG.md:1045:- `node scripts/zeus-smoke.mjs` → 9/9 asserts (health · embed dim=512 · tools count=12 · refine non-empty)
./CHANGELOG.md:1071:### Changed — `/v1/health`
./CHANGELOG.md:1182:- **Device-adaptive automático**: daemon `/v1/health` reporta `speech_available: bool` e `endpoints[]`. Quando ausente (iOS Capacitor atual), `aspTranscribe` retorna 503 e helper apenas loga warn — sem crash do plugin. Em iPhone/iPad o pipeline audio degrada graciosamente até v1.4 implementar Speech em AegisDaemon.
./CHANGELOG.md:1321:- LaunchAgent restart via `install-mac-daemon.sh`; `/v1/health` reporta `endpoint_count: 29`, `speech_available: true`
./CHANGELOG.md:1355:- Adicionado campo `speech_available` no `/v1/health` payload
./CHANGELOG.md:1370:- **Endpoints totais agora**: 29 (era 26) — 3 novos `afm/refine`, `asp/transcribe`, `asp/vad` listados em `/v1/health.endpoints` e no default 404 case
./CHANGELOG.md:1375:- LaunchAgent `com.maiocchi.zeusdaemon` em produção: `/v1/health` retorna `endpoint_count: 29`, `speech_available: true`, `fm_available: true`
./lib/universal-fs.js:145:// Stable device id — replacement for os.hostname() heuristic
./lib/universal-fs.js:151:    if (nodeOs && typeof nodeOs.hostname === 'function') {
./lib/universal-fs.js:152:      hint = String(nodeOs.hostname()).replace(/[^a-z0-9-]/gi, '').slice(0, 12) || 'dev';
./audit-codex-round4.md:25:| 1 | **CRIT** | `daemon/.../main.swift:28` | Daemon Mac faz bind default em **`0.0.0.0`** — expõe HTTP na LAN/tailnet | ✅ confirmado: `var host = "0.0.0.0"` (comentário "aceitar conexões Tailscale") |
./audit-codex-round4.md:26:| 2 | **CRIT** | `ZeusMacHTTPHandler.swift:293` (handler `:1485`) | **`/v1/cmd` sem auth** executa comandos shell (`cat`, `ssh`, `osascript`, `curl`, `git`, `claude`); CORS `*` em `:3338` | ✅ confirmado: `route()` (linha 269) despacha direto, sem gate; handler "macOS system commands" |
./audit-codex-round4.md:27:| 3 | **CRIT** | spec `daemon-token-auth-design.md:25` vs handler `:269` | Design exige `X-Zeus-Token` para não-loopback, mas **middleware nunca foi implementado** | ✅ confirmado: 0 ocorrências de `X-Zeus-Token`/auth no handler |
./audit-codex-round4.md:31:| 7 | **HIGH** | `main.source.js:366` | **IPs Tailscale + hostname pessoal** hardcoded (`100.108.x.x`, `rogers-mac-mini.local`) | ✅ confirmado (cf. S8 Check 2) |
./audit-codex-round4.md:41:> 2. O endpoint **`/v1/cmd` executa comandos de shell** (incl. `ssh`, `curl`, `osascript`, `claude`).
./audit-codex-round4.md:44:> Resultado: **qualquer host na mesma LAN ou tailnet** do Mac mini pode `POST /v1/cmd` e executar comandos arbitrários na máquina do Roger. Como o vault contém `Clientes/**` (sigiloso), o impacto cruza o privacy gate jurídico. **Release-blocking.**
./audit-codex-round4.md:48:1. **#1** default `127.0.0.1`; exigir `--host 0.0.0.0` explícito + auth obrigatória.
./audit-codex-round4.md:49:2. **#2** remover `/v1/cmd` do build público OU proteger com token local + allowlist mínima + bloquear não-loopback.
./audit-codex-round4.md:50:3. **#3** middleware de auth antes de `route()`, liberando só `/v1/health`.
./audit-codex-round4.md:54:7. **#7** mover topologia (IPs/hostname) para config local não-sincronizada ou discovery dinâmico.
./main.source.js:362:// DaemonLifecycle, AegisDaemon embebido no host app no iOS). Retorna shape
./main.source.js:380:// v1.15 — codex #7 / S8 Check 2: a topologia de rede (IPs Tailscale, hostnames mDNS)
./main.source.js:386:const ZEUS_MESH_PEERS_KEY = 'zeus.mesh.peers';   // JSON array de URLs http://host:port, per-device
./main.source.js:426:  return /^https?:\/\/(127\.0\.0\.1|localhost|\[::1\])(:\d+)?(\/|$)/i.test(url);
./main.source.js:462://   2. localhost:2223  — alternativa de hostname para o mesmo loopback
./main.source.js:481:    push('http://localhost:2223');
./main.source.js:1832:    // v1.15 — registerDomEvent (este Modal é Component; listener limpo no close)
./main.source.js:2109:      // render do grafo; o GC limpa o listener junto com o <circle> removido.
./main.source.js:2288:      const health = await this.plugin.httpClient.health();
./main.source.js:2291:      this._addStatusRow(daemonSection, 'Status', health.status || 'unreachable', health.status === 'ok' ? 'ok' : 'err');
./main.source.js:2292:      this._addStatusRow(daemonSection, 'Platform', health.platform || '?');
./main.source.js:2293:      this._addStatusRow(daemonSection, 'Endpoints', String((health.endpoints || []).length));
./main.source.js:2295:      this._addStatusRow(daemonSection, 'NLContextualEmbedding', health.nl_available ? '✓' : '✗', health.nl_available ? 'ok' : 'err');
./main.source.js:2296:      this._addStatusRow(daemonSection, 'Vision', health.vision_available ? '✓' : '✗', health.vision_available ? 'ok' : 'err');
./main.source.js:2297:      this._addStatusRow(daemonSection, 'FoundationModels', health.fm_available ? '✓' : '✗', health.fm_available ? 'ok' : 'warn');
./main.source.js:2298:      if (health.translation_available !== undefined) {
./main.source.js:2299:        this._addStatusRow(daemonSection, 'Translation', health.translation_available ? '✓' : '✗', health.translation_available ? 'ok' : 'warn');
./main.source.js:2441:      .setDesc('URLs de daemons remotos para fallback, uma por linha (ex: http://host-ou-ip:2223). Guardado só neste device (localStorage) — nunca vai para data.json sincronizado. Cada device escolhe seus próprios peers; sem topologia hardcoded. Requer o toggle acima ligado e um token de auth válido no daemon remoto.')
./main.source.js:2444:          .setPlaceholder('http://192.168.1.50:2223\nhttp://meu-host.local:2223')
./main.source.js:3174:        const health = await this.httpClient.health();
./main.source.js:3176:        dbg('[zeus] daemon health:', health.status, '| platform:', health.platform, '| endpoints:', (health.endpoints || []).length, '| tools:', tools.length, '| url:', activeUrl);
./main.source.js:3177:        if (health.status === 'ok') {
./main.source.js:3179:          new Notice(`Zeus: daemon ${health.platform || '?'} ${isLocal ? 'LOCAL ✓' : 'REMOTE (Tailscale) ⚠'} · ${(health.endpoints || []).length} endpoints`);
./main.source.js:3323:          const health = await this.httpClient.health();
./main.source.js:3326:          new Notice(`Daemon: ${health.status || 'unknown'} · platform: ${health.platform || '?'} · ${tools.length} tools`);
./main.source.js:3327:          dbg('[zeus] HTTP daemon health:', health, 'tools:', tools);
./main.source.js:4861:      // (+ listener) é coletado junto. addEventListener é seguro e correto aqui.
./lib/passport-scheduler.js:18: *   - Initial sweep 30s after plugin load — let plugin settle, daemon health check,
./daemon/Sources/AegisDaemon/AegisHTTPServer.swift:9:// requestUrl (bypass de CORS) — embedding / enrich / agent / health.
./daemon/Sources/AegisDaemon/AegisHTTPServer.swift:28:    /// Inicia o listener HTTP em loopback (default 127.0.0.1:2223).
./daemon/Sources/AegisDaemon/AegisHTTPServer.swift:29:    public func start(host: String = "127.0.0.1", port: Int = 2223) async throws {
./daemon/Sources/AegisDaemon/AegisHTTPServer.swift:41:        let bound = try await bootstrap.bind(host: host, port: port).get()
./daemon/Sources/AegisDaemon/AegisHTTPServer.swift:43:        print("[AegisHTTPServer] 🛰️ HTTP escutando em http://\(host):\(port) — endpoints /v1/embed /v1/enrich /v1/agent /v1/health")
./main.js:114:        if (nodeOs && typeof nodeOs.hostname === "function") {
./main.js:115:          hint = String(nodeOs.hostname()).replace(/[^a-z0-9-]/gi, "").slice(0, 12) || "dev";
./main.js:913:        this.healthCache = null;
./main.js:914:        this.healthCheckedAt = 0;
./main.js:988:        this.healthCache = null;
./main.js:991:        return /^https?:\/\/(127\.0\.0\.1|localhost|\[::1\])(:\d+)?(\/|$)/i.test(this.baseUrl || "");
./main.js:1009:      // Lazy health check — cached for HEALTH_TTL_MS
./main.js:1014:        if (this.healthCache !== null && now - this.healthCheckedAt < this.HEALTH_TTL_MS) {
./main.js:1015:          return this.healthCache;
./main.js:1019:            url: `${this.baseUrl}/v1/health`,
./main.js:1028:          this.healthCache = ok;
./main.js:1030:          this.healthCache = false;
./main.js:1032:        this.healthCheckedAt = Date.now();
./main.js:1033:        return this.healthCache;
./main.js:1375:      async health() {
./main.js:1377:          url: `${this.baseUrl}/v1/health`,
./main.js:2897:        this.host = options.host || DEFAULT_HOST;
./main.js:2898:        this.url = `http://${this.host}:${this.port}`;
./main.js:3006:            child = spawn2(bin, ["--port", String(this.port), "--host", this.host], {
./main.js:3039:              finish({ running: false, source: "spawn-timeout", reason: "sem /v1/health em 10s" });
./main.js:5793:  return /^https?:\/\/(127\.0\.0\.1|localhost|\[::1\])(:\d+)?(\/|$)/i.test(url);
./main.js:5838:    push("http://localhost:2223");
./main.js:7412:      const health = await this.plugin.httpClient.health();
./main.js:7415:      this._addStatusRow(daemonSection, "Status", health.status || "unreachable", health.status === "ok" ? "ok" : "err");
./main.js:7416:      this._addStatusRow(daemonSection, "Platform", health.platform || "?");
./main.js:7417:      this._addStatusRow(daemonSection, "Endpoints", String((health.endpoints || []).length));
./main.js:7419:      this._addStatusRow(daemonSection, "NLContextualEmbedding", health.nl_available ? "\u2713" : "\u2717", health.nl_available ? "ok" : "err");
./main.js:7420:      this._addStatusRow(daemonSection, "Vision", health.vision_available ? "\u2713" : "\u2717", health.vision_available ? "ok" : "err");
./main.js:7421:      this._addStatusRow(daemonSection, "FoundationModels", health.fm_available ? "\u2713" : "\u2717", health.fm_available ? "ok" : "warn");
./main.js:7422:      if (health.translation_available !== void 0) {
./main.js:7423:        this._addStatusRow(daemonSection, "Translation", health.translation_available ? "\u2713" : "\u2717", health.translation_available ? "ok" : "warn");
./main.js:7522:    new Setting(containerEl).setName("Daemon mesh peers (local, n\xE3o sincronizado)").setDesc("URLs de daemons remotos para fallback, uma por linha (ex: http://host-ou-ip:2223). Guardado s\xF3 neste device (localStorage) \u2014 nunca vai para data.json sincronizado. Cada device escolhe seus pr\xF3prios peers; sem topologia hardcoded. Requer o toggle acima ligado e um token de auth v\xE1lido no daemon remoto.").addTextArea((t) => {
./main.js:7523:      t.setValue(_zeusGetMeshPeers().join("\n")).setPlaceholder("http://192.168.1.50:2223\nhttp://meu-host.local:2223").onChange((v) => {
./main.js:8021:          const health = await this.httpClient.health();
./main.js:8023:          dbg("[zeus] daemon health:", health.status, "| platform:", health.platform, "| endpoints:", (health.endpoints || []).length, "| tools:", tools.length, "| url:", activeUrl);
./main.js:8024:          if (health.status === "ok") {
./main.js:8026:            new Notice(`Zeus: daemon ${health.platform || "?"} ${isLocal ? "LOCAL \u2713" : "REMOTE (Tailscale) \u26A0"} \xB7 ${(health.endpoints || []).length} endpoints`);
./main.js:8180:            const health = await this.httpClient.health();
./main.js:8183:            new Notice(`Daemon: ${health.status || "unknown"} \xB7 platform: ${health.platform || "?"} \xB7 ${tools.length} tools`);
./main.js:8184:            dbg("[zeus] HTTP daemon health:", health, "tools:", tools);
./lib/zeus-http-client.js:42: *   GET  /v1/health
./lib/zeus-http-client.js:55:    this.healthCache = null;
./lib/zeus-http-client.js:56:    this.healthCheckedAt = 0;
./lib/zeus-http-client.js:57:    this.HEALTH_TTL_MS = 30000;   // re-probe daemon health every 30s
./lib/zeus-http-client.js:143:    this.healthCache = null;
./lib/zeus-http-client.js:147:    return /^https?:\/\/(127\.0\.0\.1|localhost|\[::1\])(:\d+)?(\/|$)/i.test(this.baseUrl || '');
./lib/zeus-http-client.js:169:  // Lazy health check — cached for HEALTH_TTL_MS
./lib/zeus-http-client.js:174:    if (this.healthCache !== null && (now - this.healthCheckedAt) < this.HEALTH_TTL_MS) {
./lib/zeus-http-client.js:175:      return this.healthCache;
./lib/zeus-http-client.js:181:        url: `${this.baseUrl}/v1/health`,
./lib/zeus-http-client.js:190:      this.healthCache = ok;
./lib/zeus-http-client.js:192:      this.healthCache = false;
./lib/zeus-http-client.js:194:    this.healthCheckedAt = Date.now();
./lib/zeus-http-client.js:195:    return this.healthCache;
./lib/zeus-http-client.js:580:  async health() {
./lib/zeus-http-client.js:582:      url: `${this.baseUrl}/v1/health`,
./README.md:101:│   │       ├── AegisHTTPServer.swift       ← SwiftNIO bind 127.0.0.1:2223
./daemon/Sources/AegisDaemon/MLXAppleTwinBootstrap.swift:9:/// do AegisDaemon, idealmente em `AegisDaemon.start()` (antes do listen()).
daemon/Sources/AegisDaemon/AegisHTTPServer.swift:9:// requestUrl (bypass de CORS) — embedding / enrich / agent / health.
daemon/Sources/AegisDaemon/AegisHTTPServer.swift:28:    /// Inicia o listener HTTP em loopback (default 127.0.0.1:2223).
daemon/Sources/AegisDaemon/AegisHTTPServer.swift:29:    public func start(host: String = "127.0.0.1", port: Int = 2223) async throws {
daemon/Sources/AegisDaemon/AegisHTTPServer.swift:41:        let bound = try await bootstrap.bind(host: host, port: port).get()
daemon/Sources/AegisDaemon/AegisHTTPServer.swift:43:        print("[AegisHTTPServer] 🛰️ HTTP escutando em http://\(host):\(port) — endpoints /v1/embed /v1/enrich /v1/agent /v1/health")
./audit-claude-vs-codex.md:18:| **CRIT daemon `0.0.0.0`** | **CONCORDO** (verificado) | `main.swift:28` literal. Os agentes Claude **não olharam** o bind do daemon. |
./audit-claude-vs-codex.md:19:| **CRIT `/v1/cmd` sem auth** | **CONCORDO** (verificado) | `route()` despacha sem gate; handler executa shell. Achado mais grave da rodada. |
./audit-claude-vs-codex.md:20:| **CRIT token auth não implementado** | **CONCORDO** (verificado) | Spec existe, código não. Zero refs a `X-Zeus-Token`. |
./audit-claude-vs-codex.md:50:> O S7 auditou **conformidade arquitetural** (invariantes Apple-native, ordem de fallback, degradação iOS) e concluiu "CONFORMS" em 5/5 — mas **nunca testou o endereço de bind nem a autenticação do `/v1/cmd`**. Tudo que o S7 validou (loopback-first no *cliente*) é verdade, e ainda assim o *servidor* está aberto em `0.0.0.0` sem auth. Lição: conformidade de design ≠ postura de segurança.
./audit-claude-vs-codex.md:55:| `/v1/cmd` RCE sem auth | Fora do escopo declarado dos subagentes (conformidade, não pentest) |
./audit-claude-vs-codex.md:63:1. **CRIT** — Daemon `0.0.0.0` + `/v1/cmd` sem auth + token middleware ausente → **cadeia única de RCE na tailnet**. (Codex 1+2+3)
./INSTALL.md:27:- **Daemon ausente** → plugin spawna `bin/ZeusDaemonMac --port 2223 --host 127.0.0.1` no `onload()`, monitora `/v1/health` por até 10s. Sucesso → opera normal. Falha → degrada gracioso (Notice no Settings tab) e Obsidian continua funcional.
./INSTALL.md:50:| `fm_available: false` no `/v1/health` | Apple Intelligence não ativado ou macOS < 26 | System Settings → Apple Intelligence & Siri → habilitar; aguardar download dos modelos |
daemon/Sources/AegisDaemon/MLXAppleTwinBootstrap.swift:9:/// do AegisDaemon, idealmente em `AegisDaemon.start()` (antes do listen()).
./docs/ADR-007-QuickLook-Markdown-Preview.md:21:2. macOS 14 (Sonoma) marcou `QLGenerator` como deprecated em favor de `QLPreviewExtension` (app extension embarcada em host `.app`). O caminho legacy **continua funcional** em todas as versões pós-Sonoma testadas até esta data; deprecation warning não impede instalação.
./docs/ADR-007-QuickLook-Markdown-Preview.md:22:3. O daemon Swift do Zeus (`ZeusDaemonMac`) já existe como host potencial para uma `QLPreviewExtension` futura — mas adicionar uma app extension exige assinatura de código e empacotamento que hoje o repositório não tem.
./docs/ADR-007-QuickLook-Markdown-Preview.md:34:| **QLPreviewExtension moderna** (Sonoma+) | API atual, sem deprecation warning | Exige host `.app` assinado; o daemon Swift hoje não é distribuído como `.app` empacotado. Bloqueia adoção rápida |
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:36://   GET  /v1/health           → status + disponibilidade NL/Vision/FoundationModels
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:46://   POST /v1/cmd              → comandos Aegis nativos (iOS)
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:183:    // Loopback confiável (on-device); peers não-loopback exigem X-Zeus-Token ==
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:184:    // env ZEUS_DAEMON_TOKEN. Fail-closed sem token configurado.
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:192:        guard let token = ProcessInfo.processInfo.environment["ZEUS_DAEMON_TOKEN"],
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:194:        guard let provided = head.headers.first(name: "X-Zeus-Token")
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:233:        // v1.14 — Auth gate (codex CRIT #2/#3). /v1/health aberto p/ discovery;
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:234:        // resto exige loopback OU X-Zeus-Token válido.
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:235:        if path != "/v1/health" && !Self.isAuthorized(remote: context.remoteAddress, head: head) {
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:237:                           dict: ["error": "unauthorized: non-loopback request requires valid X-Zeus-Token"])
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:262:        case (.GET, "/v1/health"):
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:329:        case (.POST, "/v1/cmd"):
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:331:        case (.GET, "/v1/cmd"):
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:332:            // Suporte a GET com query param ?cmd=...
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:367:                    "GET /v1/health", "GET /v1/tools",
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:371:                    "POST /v1/cmd",
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:389:    // MARK: - /v1/health
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:447:            "GET /v1/health", "GET /v1/tools",
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:451:            "POST /v1/cmd",
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:501:                ["name": "cmd", "endpoint": "POST /v1/cmd", "input": "cmd",
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:502:                 "output": "output", "model": "iOS native (ping/status/audit/network/storage/sysinfo/spotlight/health/contacts/calendar/reminders/photos/ls)"],
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:549:            makeTool("capivara_health", "Verifica se o dispositivo está online",
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:553:            makeTool("capivara_cmd", "Executa comando nativo no dispositivo",
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:555:                      "properties": ["cmd": ["type": "string", "description": "comando ou linguagem natural"]],
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:556:                      "required": ["cmd"]]),
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2402:    // iOS 9. AegisDaemon embarcado no app host iOS (Capivara / MetassistemaApp)
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2653:    // MARK: - /v1/cmd — executa comando Zeus nativo (cross-device via Tailscale)
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2656:        let cmd: String
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2657:        if let json = Self.parseJSON(bodyJSON), let c = json["cmd"] as? String, !c.isEmpty {
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2658:            cmd = c
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2661:            cmd = bodyJSON.trimmingCharacters(in: .whitespacesAndNewlines)
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2663:            return Response(status: .badRequest, payload: ["error": "body inválido: requer {\"cmd\": \"...\"}"])
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2668:        let output = executor.run(cmd)
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2669:        return Response(status: .ok, payload: ["output": output, "cmd": cmd])
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2678:// MARK: - Bridge síncrona para /v1/cmd (evita dependência circular com AegisCommandExecutor)
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2684:    func run(_ cmd: String) -> String {
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2685:        let args = cmd.split(separator: " ").map(String.init)
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2708:            return "iOS \(pi.operatingSystemVersionString) | \(pi.processorCount) cores | \(free/1_073_741_824)GB livre / \(total/1_073_741_824)GB total | host: \(pi.hostName)"
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2712:        case "health":
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2713:            return AegisNativeTools.healthRead(metric: args.dropFirst().first ?? "steps")
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2755:              apps iOS: spotlight, health, contacts, calendar, reminders, photos, ls
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2772:            displayLabel: process.hostName,
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2775:            capabilitiesSummary: "AegisDaemon standalone: HTTP, NL/Vision/FM quando disponíveis; integrações app-host opcionais usam fallback local."
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2783:        return "Device audit: \(process.hostName) | \(process.operatingSystemVersionString) | uptime \(Int(process.systemUptime))s"
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2787:        return "Network status: HTTP daemon ativo; diagnóstico detalhado requer integração do app host."
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2794:    static func healthRead(metric: String) -> String {
./docs/ADR-011-iOS-Spotlight-AegisDaemon-Bridge.md:10:# ADR-011 — iOS CoreSpotlight via AegisDaemon embarcado no app host
./docs/ADR-011-iOS-Spotlight-AegisDaemon-Bridge.md:24:SwiftPM em `daemon/Sources/AegisDaemon/`) embarcado pelo app host iOS
./docs/ADR-011-iOS-Spotlight-AegisDaemon-Bridge.md:35:AegisDaemon HTTP Server (embarcado no app host iOS Swift)
./docs/ADR-011-iOS-Spotlight-AegisDaemon-Bridge.md:62:SwiftPM linkada no Xcode workspace do app host.
./docs/ADR-011-iOS-Spotlight-AegisDaemon-Bridge.md:67:em iOS com AegisDaemon embarcado no app host (loopback 127.0.0.1:2223), chamadas
./docs/ADR-011-iOS-Spotlight-AegisDaemon-Bridge.md:73:## Contrato app host
./docs/ADR-011-iOS-Spotlight-AegisDaemon-Bridge.md:75:**App host (Capivara OR MetassistemaApp-iOS)** precisa:
./docs/ADR-011-iOS-Spotlight-AegisDaemon-Bridge.md:82:   let server = AegisHTTPServer(port: 2223, host: "127.0.0.1")
./docs/ADR-011-iOS-Spotlight-AegisDaemon-Bridge.md:92:**Quando app host embarca AegisDaemon**:
./docs/ADR-011-iOS-Spotlight-AegisDaemon-Bridge.md:97:**Quando app host NÃO embarca AegisDaemon** (default Obsidian iOS standalone):
./docs/ADR-011-iOS-Spotlight-AegisDaemon-Bridge.md:118:1. Maintainer compila app host com AegisDaemon Swift Package
./docs/ADR-011-iOS-Spotlight-AegisDaemon-Bridge.md:120:3. Em primeiro launch, app host inicia AegisHTTPServer em 127.0.0.1:2223
./docs/ADR-008-Leiden-Communities-JS-Port.md:16:A referência interna canônica (`~/Code/maiocchi-ia/skills/tripla-fusao/scripts/cluster.py`, 741 LOC Python) usa `igraph` + `leidenalg` (binding C do paper Traag et al. 2019). Trazer essa stack inteira para dentro de um plugin Obsidian que precisa rodar **idêntico em macOS Electron e iOS Capacitor** (vide ADR-018 — daemon HTTP Aegis-pattern) tem três problemas:
./docs/superpowers/specs/2026-05-16-daemon-token-auth-design.md:9:O daemon `zeusdaemon-mac` escuta em `0.0.0.0:2223` e expõe ~25 endpoints POST **sem autenticação** — acessíveis por qualquer host na LAN doméstica e em toda a tailnet. O estudo do plugin Zeus (2026-05-16) apontou isso como o risco mais grave.
./docs/superpowers/specs/2026-05-16-daemon-token-auth-design.md:26:| Não-loopback (LAN / tailnet) | Exige header `X-Zeus-Token` válido; senão `401`. |
./docs/superpowers/specs/2026-05-16-daemon-token-auth-design.md:27:| `GET /v1/health` | Sempre liberado — diagnóstico. |
./docs/superpowers/specs/2026-05-16-daemon-token-auth-design.md:34:- O `ZeusHttpClient` (plugin) anexa `X-Zeus-Token` nas requests não-loopback. Como loopback é isento, só callers cross-device carregam token.
./docs/superpowers/specs/2026-05-16-daemon-token-auth-design.md:41:1. **Middleware de auth** no handler HTTP (`ZeusMacHTTPHandler.swift`, `AegisHTTPHandlers.swift`), **antes** do roteamento por endpoint: detecta loopback → se não, valida `X-Zeus-Token` contra o conjunto.
./docs/superpowers/specs/2026-05-16-daemon-token-auth-design.md:55:- `GET /v1/health` de qualquer origem, sem token → `200`.
./docs/superpowers/specs/2026-05-16-camada-gemma4-design.md:91:- **ODR:** On-Demand Resources é hospedado pela App Store. Se o app iOS não é App-Store-distribuído (sideload / deploy via Xcode), o ODR padrão não se aplica — confirmar o mecanismo de download do adapter (pode exigir esquema self-hosted via Tailscale/iCloud).
./daemon/Sources/ZeusDaemonMac/main.swift:8://   GET  /v1/health    → status + plataforma + disponibilidade FM
./daemon/Sources/ZeusDaemonMac/main.swift:28:    // o LaunchAgent/auto-spawn já passam --host 127.0.0.1. Para cross-device via
./daemon/Sources/ZeusDaemonMac/main.swift:29:    // Tailscale, exigir --host 0.0.0.0 EXPLÍCITO + ZEUS_DAEMON_TOKEN no ambiente
./daemon/Sources/ZeusDaemonMac/main.swift:31:    var host: String = "127.0.0.1"
./daemon/Sources/ZeusDaemonMac/main.swift:43:            case "--host":
./daemon/Sources/ZeusDaemonMac/main.swift:44:                if i + 1 < argv.count { args.host = argv[i + 1]; i += 1 }
./daemon/Sources/ZeusDaemonMac/main.swift:52:                  ZeusDaemonMac [--port 2223] [--host 127.0.0.1] [--vault PATH]
./daemon/Sources/ZeusDaemonMac/main.swift:55:                  GET  /v1/health
./daemon/Sources/ZeusDaemonMac/main.swift:91:    serverChannel = try bootstrap.bind(host: args.host, port: args.port).wait()
./daemon/Sources/ZeusDaemonMac/main.swift:93:    FileHandle.standardError.write("[ZeusDaemonMac] FATAL: bind \(args.host):\(args.port) falhou: \(error)\n".data(using: .utf8) ?? Data())
./daemon/Sources/ZeusDaemonMac/main.swift:100:print("[ZeusDaemonMac] HTTP escutando em http://\(args.host):\(args.port)")
./daemon/Sources/ZeusDaemonMac/main.swift:101:print("[ZeusDaemonMac] endpoints: GET /v1/health · GET /v1/tools · POST /v1/embed · POST /v1/ocr · POST /v1/summarize · POST /v1/enrich · POST /v1/cmd")
./daemon/README.md:20:│  │  bind 127.0.0.1:2223 — LaunchAgent via launchctl                │  │
./daemon/README.md:26:│  │  bind 127.0.0.1:2223 — SwiftNIO ChannelInitializer             │  │
./daemon/README.md:36:- `GET /v1/health` — status + capabilities + endpoint list
./daemon/README.md:110:Primeiro que responde `200` em `/v1/health` é usado. Zero-config por device.
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:5://   GET  /v1/health
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:173:    // X-Zeus-Token igual a env ZEUS_DAEMON_TOKEN. Fail-closed: sem token
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:182:        guard let token = ProcessInfo.processInfo.environment["ZEUS_DAEMON_TOKEN"],
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:184:        guard let provided = head.headers.first(name: "X-Zeus-Token")
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:186:        return constantTimeEquals(provided, token)
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:189:    static func constantTimeEquals(_ a: String, _ b: String) -> Bool {
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:240:        // v1.14 — Auth gate (codex CRIT #2/#3). /v1/health aberto para discovery;
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:241:        // todo o resto (incl. /v1/cmd) exige loopback OU X-Zeus-Token válido.
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:242:        if path != "/v1/health" && !Self.isAuthorized(remote: context.remoteAddress, head: head) {
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:244:                           dict: ["error": "unauthorized: non-loopback request requires valid X-Zeus-Token"])
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:305:        case (.GET, "/v1/health"):
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:327:        case (.POST, "/v1/cmd"):
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:397:                    "GET /v1/health", "GET /v1/tools",
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:400:                    "POST /v1/prompt", "POST /v1/cmd",
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:422:    // MARK: - /v1/health
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:448:            "GET /v1/health", "GET /v1/tools", "GET /v1/mcp",
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:451:            "POST /v1/prompt", "POST /v1/cmd",
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:527:                ["name": "cmd", "endpoint": "POST /v1/cmd", "input": "cmd",
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:571:            tool("capivara_health",  "Status completo do \(kind)",
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:575:            tool("capivara_cmd",     "Executa comando macOS nativo (shell, brew, launchctl, osascript, pmset)",
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:577:                  "properties": ["cmd": ["type": "string", "description": "comando ou linguagem natural"]],
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:578:                  "required": ["cmd"]]),
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:1519:    // MARK: - /v1/cmd (macOS system commands)
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:1523:              let cmd = json["cmd"] as? String, !cmd.isEmpty else {
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:1525:                "error": "requer {\"cmd\": \"...\"}",
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:1532:        let tokens = cmd.split(separator: " ").map(String.init)
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:1538:        return Response(status: .ok, payload: ["output": output, "cmd": cmd, "platform": "macOS"])
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:1607:            guard !args.isEmpty else { return "Uso: ssh <host> [comando]" }
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:1608:            // Apenas comandos não-interativos (exige argumento de comando após o host)
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:1609:            guard args.count >= 2 else { return "SSH: forneça host e comando. Ex: ssh iphone.ts.net sysinfo" }
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:1610:            let host = args[0]
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:1613:                              "-o", "ConnectTimeout=5", host, remoteCmd], timeout: 15)
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:1694:        let hostname = shellRun(["/bin/hostname"], timeout: 3).trimmingCharacters(in: .newlines)
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:1700:        Host:        \(hostname)
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:2899:                let p = rawBuf.bindMemory(to: Float.self)
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:2905:                let p = rawBuf.bindMemory(to: Double.self)
daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:36://   GET  /v1/health           → status + disponibilidade NL/Vision/FoundationModels
daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:46://   POST /v1/cmd              → comandos Aegis nativos (iOS)
daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:183:    // Loopback confiável (on-device); peers não-loopback exigem X-Zeus-Token ==
daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:184:    // env ZEUS_DAEMON_TOKEN. Fail-closed sem token configurado.
daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:192:        guard let token = ProcessInfo.processInfo.environment["ZEUS_DAEMON_TOKEN"],
daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:194:        guard let provided = head.headers.first(name: "X-Zeus-Token")
daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:233:        // v1.14 — Auth gate (codex CRIT #2/#3). /v1/health aberto p/ discovery;
daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:234:        // resto exige loopback OU X-Zeus-Token válido.
daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:235:        if path != "/v1/health" && !Self.isAuthorized(remote: context.remoteAddress, head: head) {
daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:237:                           dict: ["error": "unauthorized: non-loopback request requires valid X-Zeus-Token"])
daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:262:        case (.GET, "/v1/health"):
daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:329:        case (.POST, "/v1/cmd"):
daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:331:        case (.GET, "/v1/cmd"):
daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:332:            // Suporte a GET com query param ?cmd=...
daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:367:                    "GET /v1/health", "GET /v1/tools",
daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:371:                    "POST /v1/cmd",
daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:389:    // MARK: - /v1/health
daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:447:            "GET /v1/health", "GET /v1/tools",
daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:451:            "POST /v1/cmd",
daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:501:                ["name": "cmd", "endpoint": "POST /v1/cmd", "input": "cmd",
daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:502:                 "output": "output", "model": "iOS native (ping/status/audit/network/storage/sysinfo/spotlight/health/contacts/calendar/reminders/photos/ls)"],
daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:549:            makeTool("capivara_health", "Verifica se o dispositivo está online",
daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:553:            makeTool("capivara_cmd", "Executa comando nativo no dispositivo",
daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:555:                      "properties": ["cmd": ["type": "string", "description": "comando ou linguagem natural"]],
daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:556:                      "required": ["cmd"]]),
daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2402:    // iOS 9. AegisDaemon embarcado no app host iOS (Capivara / MetassistemaApp)
daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2653:    // MARK: - /v1/cmd — executa comando Zeus nativo (cross-device via Tailscale)
daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2656:        let cmd: String
daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2657:        if let json = Self.parseJSON(bodyJSON), let c = json["cmd"] as? String, !c.isEmpty {
daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2658:            cmd = c
daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2661:            cmd = bodyJSON.trimmingCharacters(in: .whitespacesAndNewlines)
daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2663:            return Response(status: .badRequest, payload: ["error": "body inválido: requer {\"cmd\": \"...\"}"])
daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2668:        let output = executor.run(cmd)
daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2669:        return Response(status: .ok, payload: ["output": output, "cmd": cmd])
daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2678:// MARK: - Bridge síncrona para /v1/cmd (evita dependência circular com AegisCommandExecutor)
daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2684:    func run(_ cmd: String) -> String {
daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2685:        let args = cmd.split(separator: " ").map(String.init)
daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2708:            return "iOS \(pi.operatingSystemVersionString) | \(pi.processorCount) cores | \(free/1_073_741_824)GB livre / \(total/1_073_741_824)GB total | host: \(pi.hostName)"
daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2712:        case "health":
daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2713:            return AegisNativeTools.healthRead(metric: args.dropFirst().first ?? "steps")
daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2755:              apps iOS: spotlight, health, contacts, calendar, reminders, photos, ls
daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2772:            displayLabel: process.hostName,
daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2775:            capabilitiesSummary: "AegisDaemon standalone: HTTP, NL/Vision/FM quando disponíveis; integrações app-host opcionais usam fallback local."
daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2783:        return "Device audit: \(process.hostName) | \(process.operatingSystemVersionString) | uptime \(Int(process.systemUptime))s"
daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2787:        return "Network status: HTTP daemon ativo; diagnóstico detalhado requer integração do app host."
daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2794:    static func healthRead(metric: String) -> String {
daemon/Sources/ZeusDaemonMac/main.swift:8://   GET  /v1/health    → status + plataforma + disponibilidade FM
daemon/Sources/ZeusDaemonMac/main.swift:28:    // o LaunchAgent/auto-spawn já passam --host 127.0.0.1. Para cross-device via
daemon/Sources/ZeusDaemonMac/main.swift:29:    // Tailscale, exigir --host 0.0.0.0 EXPLÍCITO + ZEUS_DAEMON_TOKEN no ambiente
daemon/Sources/ZeusDaemonMac/main.swift:31:    var host: String = "127.0.0.1"
daemon/Sources/ZeusDaemonMac/main.swift:43:            case "--host":
daemon/Sources/ZeusDaemonMac/main.swift:44:                if i + 1 < argv.count { args.host = argv[i + 1]; i += 1 }
daemon/Sources/ZeusDaemonMac/main.swift:52:                  ZeusDaemonMac [--port 2223] [--host 127.0.0.1] [--vault PATH]
daemon/Sources/ZeusDaemonMac/main.swift:55:                  GET  /v1/health
daemon/Sources/ZeusDaemonMac/main.swift:91:    serverChannel = try bootstrap.bind(host: args.host, port: args.port).wait()
daemon/Sources/ZeusDaemonMac/main.swift:93:    FileHandle.standardError.write("[ZeusDaemonMac] FATAL: bind \(args.host):\(args.port) falhou: \(error)\n".data(using: .utf8) ?? Data())
daemon/Sources/ZeusDaemonMac/main.swift:100:print("[ZeusDaemonMac] HTTP escutando em http://\(args.host):\(args.port)")
daemon/Sources/ZeusDaemonMac/main.swift:101:print("[ZeusDaemonMac] endpoints: GET /v1/health · GET /v1/tools · POST /v1/embed · POST /v1/ocr · POST /v1/summarize · POST /v1/enrich · POST /v1/cmd")
daemon/QuickLook/ZeusMarkdownQuickLook/README.md:17:> A partir de macOS 14 (Sonoma), Apple recomenda [`QLPreviewExtension`](https://developer.apple.com/documentation/quicklook/qlpreviewingcontroller) (app extension embarcada num host `.app`). O caminho legacy (CFPlugIn `.qlgenerator` em `~/Library/QuickLook/`) continua **funcional** mas marcado deprecated. Esta implementação usa o caminho legacy por dois motivos:
daemon/QuickLook/ZeusMarkdownQuickLook/README.md:19:> 1. Distribuição standalone — não exige host `.app` empacotado.
./daemon/QuickLook/ZeusMarkdownQuickLook/README.md:17:> A partir de macOS 14 (Sonoma), Apple recomenda [`QLPreviewExtension`](https://developer.apple.com/documentation/quicklook/qlpreviewingcontroller) (app extension embarcada num host `.app`). O caminho legacy (CFPlugIn `.qlgenerator` em `~/Library/QuickLook/`) continua **funcional** mas marcado deprecated. Esta implementação usa o caminho legacy por dois motivos:
./daemon/QuickLook/ZeusMarkdownQuickLook/README.md:19:> 1. Distribuição standalone — não exige host `.app` empacotado.
daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:5://   GET  /v1/health
daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:173:    // X-Zeus-Token igual a env ZEUS_DAEMON_TOKEN. Fail-closed: sem token
daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:182:        guard let token = ProcessInfo.processInfo.environment["ZEUS_DAEMON_TOKEN"],
daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:184:        guard let provided = head.headers.first(name: "X-Zeus-Token")
daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:186:        return constantTimeEquals(provided, token)
daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:189:    static func constantTimeEquals(_ a: String, _ b: String) -> Bool {
daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:240:        // v1.14 — Auth gate (codex CRIT #2/#3). /v1/health aberto para discovery;
daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:241:        // todo o resto (incl. /v1/cmd) exige loopback OU X-Zeus-Token válido.
daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:242:        if path != "/v1/health" && !Self.isAuthorized(remote: context.remoteAddress, head: head) {
daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:244:                           dict: ["error": "unauthorized: non-loopback request requires valid X-Zeus-Token"])
daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:305:        case (.GET, "/v1/health"):
daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:327:        case (.POST, "/v1/cmd"):
daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:397:                    "GET /v1/health", "GET /v1/tools",
daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:400:                    "POST /v1/prompt", "POST /v1/cmd",
daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:422:    // MARK: - /v1/health
daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:448:            "GET /v1/health", "GET /v1/tools", "GET /v1/mcp",
daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:451:            "POST /v1/prompt", "POST /v1/cmd",
daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:527:                ["name": "cmd", "endpoint": "POST /v1/cmd", "input": "cmd",
daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:571:            tool("capivara_health",  "Status completo do \(kind)",
daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:575:            tool("capivara_cmd",     "Executa comando macOS nativo (shell, brew, launchctl, osascript, pmset)",
daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:577:                  "properties": ["cmd": ["type": "string", "description": "comando ou linguagem natural"]],
daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:578:                  "required": ["cmd"]]),
daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:1519:    // MARK: - /v1/cmd (macOS system commands)
daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:1523:              let cmd = json["cmd"] as? String, !cmd.isEmpty else {
daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:1525:                "error": "requer {\"cmd\": \"...\"}",
daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:1532:        let tokens = cmd.split(separator: " ").map(String.init)
daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:1538:        return Response(status: .ok, payload: ["output": output, "cmd": cmd, "platform": "macOS"])
daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:1607:            guard !args.isEmpty else { return "Uso: ssh <host> [comando]" }
daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:1608:            // Apenas comandos não-interativos (exige argumento de comando após o host)
daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:1609:            guard args.count >= 2 else { return "SSH: forneça host e comando. Ex: ssh iphone.ts.net sysinfo" }
daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:1610:            let host = args[0]
daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:1613:                              "-o", "ConnectTimeout=5", host, remoteCmd], timeout: 15)
daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:1694:        let hostname = shellRun(["/bin/hostname"], timeout: 3).trimmingCharacters(in: .newlines)
daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:1700:        Host:        \(hostname)
daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:2899:                let p = rawBuf.bindMemory(to: Float.self)
daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:2905:                let p = rawBuf.bindMemory(to: Double.self)
daemon/README.md:20:│  │  bind 127.0.0.1:2223 — LaunchAgent via launchctl                │  │
daemon/README.md:26:│  │  bind 127.0.0.1:2223 — SwiftNIO ChannelInitializer             │  │
daemon/README.md:36:- `GET /v1/health` — status + capabilities + endpoint list
daemon/README.md:110:Primeiro que responde `200` em `/v1/health` é usado. Zero-config por device.
./daemon/scripts/install-mac-daemon.sh:65:say "6/6 — aguardando 2s e verificando health"
./daemon/scripts/install-mac-daemon.sh:67:if curl --silent --fail --max-time 5 http://127.0.0.1:2223/v1/health > /tmp/zeusdaemon.health.json; then
./daemon/scripts/install-mac-daemon.sh:68:    say "OK — health respondeu:"
./daemon/scripts/install-mac-daemon.sh:69:    cat /tmp/zeusdaemon.health.json
./daemon/scripts/install-mac-daemon.sh:74:    say "AVISO — curl health falhou. Inspecione: cat /tmp/zeusdaemon.err.log"
daemon/scripts/install-mac-daemon.sh:65:say "6/6 — aguardando 2s e verificando health"
daemon/scripts/install-mac-daemon.sh:67:if curl --silent --fail --max-time 5 http://127.0.0.1:2223/v1/health > /tmp/zeusdaemon.health.json; then
daemon/scripts/install-mac-daemon.sh:68:    say "OK — health respondeu:"
daemon/scripts/install-mac-daemon.sh:69:    cat /tmp/zeusdaemon.health.json
daemon/scripts/install-mac-daemon.sh:74:    say "AVISO — curl health falhou. Inspecione: cat /tmp/zeusdaemon.err.log"

 succeeded in 165ms:
./audit-fase3-conformidade.md:22:| Campos obrigatórios presentes | ✅ PASS | id, name, version, minAppVersion, description, author |
./audit-final-consolidado.md:33:| 2 | **Autenticar/remover `/v1/cmd`**: implementar middleware `X-Zeus-Token` (já tem spec) antes de `route()`, liberar só `/v1/health` p/ não-loopback; allowlist de comandos OU remover `/v1/cmd` do build | CRIT | Codex 2+3 | `feat(daemon): token auth middleware + lock /v1/cmd` |
./audit-final-consolidado.md:68:> "Cortar v1.4.2-ios no canônico" é **moot** — o canônico já é v1.13.2. E **não se deve cortar nova versão ainda**: os 3 CRITs de segurança do daemon (bind aberto + `/v1/cmd` sem auth) são **release-blocking**, ainda mais com `Clientes/**` (sigiloso) no vault.
./audit-final-consolidado.md:78:O Zeus v1.13.2 é arquiteturalmente **sólido** nos invariantes declarados (Apple-native base inviolável, on-device-first, HTTP-first, degradação iOS — todos CONFORMS) e **limpo** de `require()` iOS desprotegidos e segredos hardcoded. O risco real **não está no design, está na superfície de rede e no vazamento de estado**: um daemon aberto sem auth com endpoint de shell, e um privacy gate incompleto que deixa conteúdo `sigiloso` escapar por endpoints não-`embed`. Esses dois eixos — corrigíveis em uma sprint focada — são o que separa o plugin de estar pronto para uso cross-device seguro.
./audit-final-consolidado.md:89:| CRIT #2/#3 — auth gate `X-Zeus-Token` (Mac + iOS) | ✅ FEITO | `curl` LAN sem token→401, token correto→200, loopback→200; ambos daemons compilam |
./audit-final-consolidado.md:98:- HIGH — remover topologia Tailscale hardcoded `TAILSCALE_MESH` + parar de gravar `zeusDaemonUrl` remoto em `data.json` (S8 Check 2). *Mitigado de fato pelo auth gate: endpoints remotos agora exigem token.*
./audit-claude-vs-codex.md:19:| **CRIT `/v1/cmd` sem auth** | **CONCORDO** (verificado) | `route()` despacha sem gate; handler executa shell. Achado mais grave da rodada. |
./audit-claude-vs-codex.md:20:| **CRIT token auth não implementado** | **CONCORDO** (verificado) | Spec existe, código não. Zero refs a `X-Zeus-Token`. |
./audit-claude-vs-codex.md:50:> O S7 auditou **conformidade arquitetural** (invariantes Apple-native, ordem de fallback, degradação iOS) e concluiu "CONFORMS" em 5/5 — mas **nunca testou o endereço de bind nem a autenticação do `/v1/cmd`**. Tudo que o S7 validou (loopback-first no *cliente*) é verdade, e ainda assim o *servidor* está aberto em `0.0.0.0` sem auth. Lição: conformidade de design ≠ postura de segurança.
./audit-claude-vs-codex.md:55:| `/v1/cmd` RCE sem auth | Fora do escopo declarado dos subagentes (conformidade, não pentest) |
./audit-claude-vs-codex.md:63:1. **CRIT** — Daemon `0.0.0.0` + `/v1/cmd` sem auth + token middleware ausente → **cadeia única de RCE na tailnet**. (Codex 1+2+3)
./MCP.md:139:A camada Claude Code / `afm` enxerga as 3 tools como JSON-RPC; nada muda do lado do agente. O daemon Zeus age como single point of authority sobre o vault.
./daemon/MDImporters/ZeusMarkdownImporter/README.md:21:| `kMDItemAuthors`      | `authors:` or `author:` |
./lib/passport-scheduler.js:14: *   - passports.jsonl é authoritative even after sync; sha-based diff é determinístico.
./audit-fase1-estrutural.md:63:**ADRs em `docs/`:** ADR-006 a ADR-011 (Spotlight, QuickLook, Leiden, MobileCLIP, iOS-Spotlight-bridge). Specs pendentes: `2026-05-16-camada-gemma4-design.md`, `2026-05-16-daemon-token-auth-design.md` (este último **não implementado** — ver Fase 2/3).
./README.md:82:│   ├── zeus-http-client.js                 ← HTTP transport + token metrics
./lib/io-queue.js:126:    // codex HIGH #4: privacy gate hard-enforced (era só comentário no header).
./audit-codex-round4.md:26:| 2 | **CRIT** | `ZeusMacHTTPHandler.swift:293` (handler `:1485`) | **`/v1/cmd` sem auth** executa comandos shell (`cat`, `ssh`, `osascript`, `curl`, `git`, `claude`); CORS `*` em `:3338` | ✅ confirmado: `route()` (linha 269) despacha direto, sem gate; handler "macOS system commands" |
./audit-codex-round4.md:27:| 3 | **CRIT** | spec `daemon-token-auth-design.md:25` vs handler `:269` | Design exige `X-Zeus-Token` para não-loopback, mas **middleware nunca foi implementado** | ✅ confirmado: 0 ocorrências de `X-Zeus-Token`/auth no handler |
./audit-codex-round4.md:48:1. **#1** default `127.0.0.1`; exigir `--host 0.0.0.0` explícito + auth obrigatória.
./audit-codex-round4.md:50:3. **#3** middleware de auth antes de `route()`, liberando só `/v1/health`.
./CHANGELOG.md:39:- **`pcc_used` → `pcc_possible`** em todo lugar (Swift Mac handler, client JS, UI, header `X-Zeus-Pcc-Used` → `X-Zeus-Pcc-Possible`). O daemon não consegue medir uso real de cloud (Apple não expõe API de inspeção); o campo agora declara honestamente "cloud era possível", não "cloud foi usado". `pcc=off` → `false` (recusa estrutural).
./CHANGELOG.md:54:- **CRIT #1 — bind loopback por padrão.** `ZeusArgs.host` default passa de `0.0.0.0` para `127.0.0.1` (`daemon/Sources/ZeusDaemonMac/main.swift`). Cross-device via Tailscale agora exige `--host 0.0.0.0` explícito + token. Verificado: `lsof` mostra `127.0.0.1` only no default.
./CHANGELOG.md:55:- **CRIT #2/#3 — auth gate `X-Zeus-Token`.** Novo middleware em `ZeusMacHTTPHandler` e `AegisHTTPHandler`: loopback é confiável (on-device-first); peers não-loopback exigem `X-Zeus-Token` == env `ZEUS_DAEMON_TOKEN` (compare constant-time). `/v1/health` fica aberto para discovery. Fail-closed: sem token configurado, todo peer remoto é recusado mesmo bound em `0.0.0.0`. Fecha o vetor de RCE não-autenticado em `/v1/cmd` na LAN/tailnet. Verificado: LAN sem token → 401, token correto → 200, loopback → 200.
./CHANGELOG.md:320:- **HIGH #3**: `lexical-ios.incremental()` com header null gravava docs sem header → search() retornava []. Extraído `_recomputeHeader()` que varre `_docs` e recompõe consistente.
./CHANGELOG.md:323:- **MED #6** (IDF stale): `_recomputeHeader()` varre `_docs` e refaz `{N, avgdl, idf}` consistente após incremental.
./CHANGELOG.md:381:**Nova lib**: `lib/lexical-ios.js` (~290 LOC). BM25 puro JS, posting list materializado em `data/lexical-ios.jsonl` (header + 1 linha por nota).
./CHANGELOG.md:601:  - `GetMetadataForFile.m` (319 LOC): parser YAML frontmatter + body H1-H3 + `[[wikilinks]]` + inline `#tags`. Popula `kMDItemTextContent`, `kMDItemTitle`, `kMDItemKeywords` (union 6 fontes), `kMDItemAuthors`, `kMDItemDescription`. ARC + `@autoreleasepool`.
./CHANGELOG.md:1394:Fechamento do ciclo PCC: o daemon Swift agora lê o header de permissão, propaga até os handlers FoundationModels (`enrich`/`summarize`/`prompt`), aplica heurística calibrada para decidir quando sinalizar uso de cloud routing, e devolve o header `X-Zeus-Pcc-Used: 1`. Plugin v1.1 já estava preparado — agora o ciclo opt-in → header outgoing → daemon decide → header response → contador da sessão funciona end-to-end.
./CHANGELOG.md:1397:- **`PccPermission` enum** em `ZeusMacHTTPHandler.swift`: `.off | .optIn | .auto` com derivação tolerante do header (`1`/`true`/`opt-in`/`auto`).
./CHANGELOG.md:1398:- **Extração do header** em `handleRequest()`: lê `X-Zeus-Allow-Pcc` (case-insensitive) e converte para `PccPermission`.
./CHANGELOG.md:1402:- **`writeJSON()`** aceita `pccUsed` e seta header `X-Zeus-Pcc-Used: 1` na response quando true. Também adiciona `Access-Control-Expose-Headers` para que o client lendo via `requestUrl` consiga inspecionar.
./CHANGELOG.md:1410:Quando Apple expuser API explícita futuramente (ex.: `GenerationOptions.allowsCloudCompute`), a heurística é substituída pela API real — assinatura externa do header permanece.
./CHANGELOG.md:1414:- `pcc_used: bool` — espelho do header (redundância intencional p/ debug fácil)
./CHANGELOG.md:1419:- CORS expandido: `Access-Control-Allow-Headers` agora inclui `X-Zeus-Allow-Pcc`; `Access-Control-Expose-Headers` expõe `X-Zeus-Pcc-Used` ao requestUrl client
./CHANGELOG.md:1423:- Nenhum dado sigiloso vaza: header é só *permissão*, daemon mantém a autoridade de roteamento, e o on-device fallback é sempre tentado primeiro pelo próprio sistema operacional Apple
./CHANGELOG.md:1435:Polish de Settings UX, métricas de token economizado visíveis no status bar, e integração client-side de Apple Cloud Private (PCC). Daemon Swift permanece em v0.5.0 — atualização do daemon para honrar o header `X-Zeus-Allow-Pcc` é trackeada como follow-up (a parte client-side fica wired e aguardando).
./CHANGELOG.md:1438:- **Token-saved metric** no status bar (`Zeus: 1245 docs · 18.3k tok saved`) — economia estimada via PIA passports compactos vs carga raw equivalente
./CHANGELOG.md:1447:- Setting **Modo PCC** com 3 opções: `off` (default, on-device only) / `opt-in` (header `X-Zeus-Allow-Pcc:1`) / `auto` (daemon decide)
./CHANGELOG.md:1451:- Helpers `_pccHeaders()` (injeta header outgoing) e `_readPccUsed()` (lê `X-Zeus-Pcc-Used` da response)
./CHANGELOG.md:1456:- Seções v1.1 e v2.0 do Settings tab com headers `<h3>` claros e descrições didáticas
./CHANGELOG.md:1462:- **PCC privacy model**: header HTTP é apenas *permissão* — daemon Swift mantém autoridade final sobre roteamento. Default `off` preserva o privacy gate original do Zeus (sigiloso nunca sai do disco local).
./CHANGELOG.md:1468:1. Ler header `X-Zeus-Allow-Pcc` em handlers `enrich`, `agent`, `graphExtract`
./CHANGELOG.md:1469:2. Configurar `SystemLanguageModel.default(allowingCloudCompute: true)` quando header presente (Swift 6.0 + macOS 26)
./docs/ADR-006-Spotlight-MDImporter-Companion.md:45:`kMDItemAuthors`, `kMDItemDescription` a partir do frontmatter YAML e da
./lib/zeus-http-client.js:70:    // a PERMISSÃO enviada no header; o daemon sinaliza POSSIBILIDADE de cloud.
./lib/zeus-http-client.js:72:    //   'off'    → no PCC permission header sent; on-device only
./lib/zeus-http-client.js:73:    //   'opt-in' → header X-Zeus-Allow-Pcc:1; Apple decides per request
./lib/zeus-http-client.js:74:    //   'auto'   → header X-Zeus-Allow-Pcc:auto; daemon flags possibility when load exceeds on-device
./lib/zeus-http-client.js:79:    this.lastPccPossible = false;      // updated from response header X-Zeus-Pcc-Possible
./lib/zeus-http-client.js:198:  // v2.0 — Inject PCC routing header into outgoing request headers.
./lib/zeus-http-client.js:199:  _pccHeaders() {
./lib/zeus-http-client.js:205:  // v2.0 — Read PCC-routing signal from response headers.
./lib/zeus-http-client.js:209:  _readPccPossible(headers) {
./lib/zeus-http-client.js:210:    if (!headers) return false;
./lib/zeus-http-client.js:211:    // Obsidian requestUrl returns headers as plain object; fetch returns Headers instance
./lib/zeus-http-client.js:212:    const val = typeof headers.get === 'function'
./lib/zeus-http-client.js:213:      ? headers.get('x-zeus-pcc-possible') || headers.get('X-Zeus-Pcc-Possible')
./lib/zeus-http-client.js:214:      : (headers['x-zeus-pcc-possible'] || headers['X-Zeus-Pcc-Possible']);
./lib/zeus-http-client.js:226:    const pccHeaders = this._pccHeaders();
./lib/zeus-http-client.js:232:        headers: { 'Content-Type': contentType, ...pccHeaders },
./lib/zeus-http-client.js:236:      this._readPccPossible(resp.headers);
./lib/zeus-http-client.js:244:        headers: { 'Content-Type': contentType, ...pccHeaders },
./lib/zeus-http-client.js:248:      this._readPccPossible(resp.headers);
./lib/zeus-http-client.js:252:      return { status: resp.status, text, json, headers: resp.headers };
./audit-codex-round5.md:28:| 6 | PCC `pcc_used` heurístico afirmado como uso real | HIGH | rename → `pcc_possible` (Swift+JS+UI+header), off=false | `ZeusMacHTTPHandler.swift`, `lib/zeus-http-client.js`, `main.source.js` |
./docs/ADR-007-QuickLook-Markdown-Preview.md:45:  - Frontmatter em header destacado (chips coloridos para `tags`, par chave-valor para `tipo`, `status`, `criado`, `atualizado`, `privacidade`).
./lib/lexical-ios.js:8: * `data/lexical-ios.jsonl` (1 linha por nota + 1 header com IDF global) e
./lib/lexical-ios.js:12: *   - PERSISTÊNCIA: header line + 1 linha por nota com {tokens:[{token,tf}], dl}
./lib/lexical-ios.js:20: *   Linha 0 (header):
./lib/lexical-ios.js:143:    this._header = null;    // { schema, N, avgdl, idf, last_built }
./lib/lexical-ios.js:172:    this._header = null;
./lib/lexical-ios.js:186:            this._header = obj;
./lib/lexical-ios.js:201:   * Persiste o índice in-memory para disco (header + 1 linha por doc).
./lib/lexical-ios.js:211:        if (this._header) lines.push(JSON.stringify(this._header));
./lib/lexical-ios.js:224:   * Build full index: itera todas notas .md, tokeniza+stem, recomputa header
./lib/lexical-ios.js:280:    this._header = {
./lib/lexical-ios.js:303:    if (!this._header || this._docs.size === 0) return [];
./lib/lexical-ios.js:309:    const avgdl = this._header.avgdl || 0;
./lib/lexical-ios.js:310:    const idfMap = this._header.idf || {};
./lib/lexical-ios.js:346:   * marcamos o header como "stale-incremental" e o consumer pode chamar
./lib/lexical-ios.js:384:    // v1.11.1 codex HIGH #3 + MED #6: se header é null, recompute full (caller
./lib/lexical-ios.js:385:    // pode ter chamado incremental SEM ter rodado build() prévio — sem header
./lib/lexical-ios.js:386:    // search() retorna []). Também usa _recomputeHeader pra IDF consistente.
./lib/lexical-ios.js:387:    this._recomputeHeader();
./lib/lexical-ios.js:392:  // v1.11.1 codex MED #6: _recomputeHeader varre _docs e refaz idf/avgdl/N
./lib/lexical-ios.js:395:  _recomputeHeader() {
./lib/lexical-ios.js:398:      this._header = { schema: 'lexical-ios-v1', N: 0, avgdl: 0, idf: {}, last_built: new Date().toISOString() };
./lib/lexical-ios.js:419:    this._header = {
./lib/lexical-ios.js:436:      avgdl: this._header ? this._header.avgdl : 0,
./lib/lexical-ios.js:437:      vocab_size: this._header && this._header.idf ? Object.keys(this._header.idf).length : 0,
./lib/lexical-ios.js:438:      last_built: this._header ? this._header.last_built : null,
./docs/ADR-008-Leiden-Communities-JS-Port.md:14:O plugin Zeus implementou em v1.8 o grafo multiplex (`lib/multiplex-graph.js`) — 8 edge types entre notas (wikilink, backlink, entity_overlap, date_overlap, folder_path, semantic_cosine, spotlight_token_bm25, co_citation) persistidos em `data/multiplex.jsonl`. O passo natural seguinte é detectar comunidades sobre esse grafo para que o usuário ganhe agrupamentos automáticos do vault — clusters de notas que dialogam entre si independentemente da hierarquia de pastas.
./lib/multiplex-graph.js:327:      console.warn('[zeus.multiplex] spotlight_token_bm25 skip:', e.message);
./docs/superpowers/specs/2026-05-16-daemon-token-auth-design.md:1:# Design — Token-auth adaptativo do daemon Zeus
./docs/superpowers/specs/2026-05-16-daemon-token-auth-design.md:26:| Não-loopback (LAN / tailnet) | Exige header `X-Zeus-Token` válido; senão `401`. |
./docs/superpowers/specs/2026-05-16-daemon-token-auth-design.md:33:- O daemon mantém um **conjunto** de tokens aceitos (não um hardcoded), lido de `~/.config/zeus/tokens` (`chmod 600`). Permite rotação e revogação de um aparelho sem afetar os outros.
./docs/superpowers/specs/2026-05-16-daemon-token-auth-design.md:34:- O `ZeusHttpClient` (plugin) anexa `X-Zeus-Token` nas requests não-loopback. Como loopback é isento, só callers cross-device carregam token.
./docs/superpowers/specs/2026-05-16-daemon-token-auth-design.md:41:1. **Middleware de auth** no handler HTTP (`ZeusMacHTTPHandler.swift`, `AegisHTTPHandlers.swift`), **antes** do roteamento por endpoint: detecta loopback → se não, valida `X-Zeus-Token` contra o conjunto.
./docs/superpowers/specs/2026-05-16-daemon-token-auth-design.md:43:3. **`ZeusHttpClient`** (`lib/zeus-http-client.js`): anexa o header nas requests não-loopback.
./daemon/QuickLook/ZeusMarkdownQuickLook/README.md:10:Companion macOS Quick Look generator do plugin Zeus para Obsidian. Renderiza preview HTML rico com tokens Anthropic (Orange `#d97757`, Lora serif, Poppins headings) ao apertar **SPACE** em um arquivo `.md` no Finder ou Spotlight. Mostra frontmatter visível com chips de tags, wikilinks clicáveis, hierarquia de headings, blocos de código com syntax-class.
./daemon/Sources/ZeusDaemonMac/main.swift:29:    // Tailscale, exigir --host 0.0.0.0 EXPLÍCITO + ZEUS_DAEMON_TOKEN no ambiente
./daemon/Sources/ZeusDaemonMac/main.swift:30:    // (o handler recusa requisições não-loopback sem token — ver isAuthorized).
./main.source.js:283:  // 'off'    = nenhum header de permissão; só on-device (default, privacy máximo)
./main.source.js:538:        'kMDItemTitle', 'kMDItemAuthors', 'kMDItemNumberOfPages',
./main.source.js:2132:    // =========== Header com título da nota ativa ===========
./main.source.js:2133:    const headerCosine = container.createDiv({ cls: 'zeus-smart-header' });
./main.source.js:2134:    headerCosine.createSpan({ cls: 'zeus-smart-title-active', text: file ? file.basename : 'Conexões' });
./main.source.js:2187:    const fmHeader = container.createDiv({ cls: 'zeus-smart-header zeus-smart-header-fm' });
./main.source.js:2188:    fmHeader.createSpan({ cls: 'zeus-smart-title', text: 'FoundationModels' });
./main.source.js:2189:    const fmBadge = fmHeader.createSpan({ cls: 'zeus-badge zeus-badge-fm', text: 'reasoning…' });
./main.source.js:2281:    const header = container.createDiv({ cls: 'zeus-status-header' });
./main.source.js:2282:    header.createEl('h3', { text: 'Zeus Engine' });
./main.source.js:2441:      .setDesc('URLs de daemons remotos para fallback, uma por linha (ex: http://host-ou-ip:2223). Guardado só neste device (localStorage) — nunca vai para data.json sincronizado. Cada device escolhe seus próprios peers; sem topologia hardcoded. Requer o toggle acima ligado e um token de auth válido no daemon remoto.')
./main.source.js:2703:      .setDesc('off = só on-device (privacy máximo, default). opt-in = client envia header X-Zeus-Allow-Pcc:1; Apple decide caso a caso. auto = client permite PCC; o daemon apenas sinaliza POSSIBILIDADE quando a carga excede on-device — o routing real é decidido pela Apple (sem API pública de controle/inspeção).')
./main.source.js:2706:        .addOption('opt-in', 'opt-in — header X-Zeus-Allow-Pcc:1')
./main.source.js:4563:          new Notice(`Zeus lexical-ios: ${r.N} notas, ${r.vocab} tokens (${r.elapsedMs}ms)`);
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:182:    // MARK: - Auth (v1.14 — codex CRIT #2/#3)
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:183:    // Loopback confiável (on-device); peers não-loopback exigem X-Zeus-Token ==
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:184:    // env ZEUS_DAEMON_TOKEN. Fail-closed sem token configurado.
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:190:    static func isAuthorized(remote: SocketAddress?, head: HTTPRequestHead) -> Bool {
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:192:        guard let token = ProcessInfo.processInfo.environment["ZEUS_DAEMON_TOKEN"],
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:194:        guard let provided = head.headers.first(name: "X-Zeus-Token")
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:195:                ?? head.headers.first(name: "x-zeus-token") else { return false }
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:225:            var headers = HTTPHeaders()
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:226:            headers.add(name: "Access-Control-Allow-Origin", value: "*")
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:227:            headers.add(name: "Access-Control-Allow-Methods", value: "GET, POST, OPTIONS")
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:228:            headers.add(name: "Access-Control-Allow-Headers", value: "Content-Type")
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:229:            self.writeRaw(context: context, status: .noContent, headers: headers, body: nil)
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:233:        // v1.14 — Auth gate (codex CRIT #2/#3). /v1/health aberto p/ discovery;
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:234:        // resto exige loopback OU X-Zeus-Token válido.
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:235:        if path != "/v1/health" && !Self.isAuthorized(remote: context.remoteAddress, head: head) {
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:236:            self.writeJSON(context: context, status: .unauthorized,
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:237:                           dict: ["error": "unauthorized: non-loopback request requires valid X-Zeus-Token"])
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2624:        var headers = HTTPHeaders()
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2625:        headers.add(name: "Content-Type", value: "application/json; charset=utf-8")
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2626:        headers.add(name: "Access-Control-Allow-Origin", value: "*")
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2632:            headers.add(name: "Content-Length", value: "\(data.count)")
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2635:            headers.add(name: "Content-Length", value: "0")
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2637:        self.writeRaw(context: context, status: status, headers: headers, body: body)
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2641:                          headers: HTTPHeaders, body: ByteBuffer?) {
./daemon/Sources/AegisDaemon/AegisHTTPHandlers.swift:2642:        let head = HTTPResponseHead(version: .init(major: 1, minor: 1), status: status, headers: headers)
./main.js:1035:      // v2.0 — Inject PCC routing header into outgoing request headers.
./main.js:1036:      _pccHeaders() {
./main.js:1041:      // v2.0 — Read PCC-routing signal from response headers.
./main.js:1045:      _readPccPossible(headers) {
./main.js:1046:        if (!headers) return false;
./main.js:1047:        const val = typeof headers.get === "function" ? headers.get("x-zeus-pcc-possible") || headers.get("X-Zeus-Pcc-Possible") : headers["x-zeus-pcc-possible"] || headers["X-Zeus-Pcc-Possible"];
./main.js:1061:        const pccHeaders = this._pccHeaders();
./main.js:1067:            headers: { "Content-Type": contentType, ...pccHeaders },
./main.js:1071:          this._readPccPossible(resp.headers);
./main.js:1077:            headers: { "Content-Type": contentType, ...pccHeaders },
./main.js:1081:          this._readPccPossible(resp.headers);
./main.js:1088:          return { status: resp.status, text, json, headers: resp.headers };
./main.js:3825:          console.warn("[zeus.multiplex] spotlight_token_bm25 skip:", e.message);
./main.js:5185:        this._header = null;
./main.js:5207:        this._header = null;
./main.js:5221:                this._header = obj;
./main.js:5235:       * Persiste o índice in-memory para disco (header + 1 linha por doc).
./main.js:5244:            if (this._header) lines.push(JSON.stringify(this._header));
./main.js:5256:       * Build full index: itera todas notas .md, tokeniza+stem, recomputa header
./main.js:5306:        this._header = {
./main.js:5328:        if (!this._header || this._docs.size === 0) return [];
./main.js:5333:        const avgdl = this._header.avgdl || 0;
./main.js:5334:        const idfMap = this._header.idf || {};
./main.js:5366:       * marcamos o header como "stale-incremental" e o consumer pode chamar
./main.js:5399:        this._recomputeHeader();
./main.js:5403:      // v1.11.1 codex MED #6: _recomputeHeader varre _docs e refaz idf/avgdl/N
./main.js:5406:      _recomputeHeader() {
./main.js:5409:          this._header = { schema: "lexical-ios-v1", N: 0, avgdl: 0, idf: {}, last_built: (/* @__PURE__ */ new Date()).toISOString() };
./main.js:5428:        this._header = {
./main.js:5444:          avgdl: this._header ? this._header.avgdl : 0,
./main.js:5445:          vocab_size: this._header && this._header.idf ? Object.keys(this._header.idf).length : 0,
./main.js:5446:          last_built: this._header ? this._header.last_built : null
./main.js:5684:  // 'off'    = nenhum header de permissão; só on-device (default, privacy máximo)
./main.js:5894:        "kMDItemAuthors",
./main.js:7277:    const headerCosine = container.createDiv({ cls: "zeus-smart-header" });
./main.js:7278:    headerCosine.createSpan({ cls: "zeus-smart-title-active", text: file ? file.basename : "Conex\xF5es" });
./main.js:7323:    const fmHeader = container.createDiv({ cls: "zeus-smart-header zeus-smart-header-fm" });
./main.js:7324:    fmHeader.createSpan({ cls: "zeus-smart-title", text: "FoundationModels" });
./main.js:7325:    const fmBadge = fmHeader.createSpan({ cls: "zeus-badge zeus-badge-fm", text: "reasoning\u2026" });
./main.js:7407:    const header = container.createDiv({ cls: "zeus-status-header" });
./main.js:7408:    header.createEl("h3", { text: "Zeus Engine" });
./main.js:7522:    new Setting(containerEl).setName("Daemon mesh peers (local, n\xE3o sincronizado)").setDesc("URLs de daemons remotos para fallback, uma por linha (ex: http://host-ou-ip:2223). Guardado s\xF3 neste device (localStorage) \u2014 nunca vai para data.json sincronizado. Cada device escolhe seus pr\xF3prios peers; sem topologia hardcoded. Requer o toggle acima ligado e um token de auth v\xE1lido no daemon remoto.").addTextArea((t) => {
./main.js:7671:    new Setting(containerEl).setName("Mostrar tokens economizados no status bar").setDesc('Exibe formato "Zeus: 1245 docs \xB7 18.3k tok saved". Baseline configur\xE1vel abaixo.').addToggle((t) => t.setValue(this.plugin.settings.showTokenSavedInStatusBar).onChange(async (v) => {
./main.js:7702:    new Setting(containerEl).setName("Modo PCC").setDesc("off = s\xF3 on-device (privacy m\xE1ximo, default). opt-in = client envia header X-Zeus-Allow-Pcc:1; Apple decide caso a caso. auto = client permite PCC; o daemon apenas sinaliza POSSIBILIDADE quando a carga excede on-device \u2014 o routing real \xE9 decidido pela Apple (sem API p\xFAblica de controle/inspe\xE7\xE3o).").addDropdown((d) => d.addOption("off", "off \u2014 s\xF3 on-device (default)").addOption("opt-in", "opt-in \u2014 header X-Zeus-Allow-Pcc:1").addOption("auto", "auto \u2014 daemon decide").setValue(this.plugin.settings.pccMode).onChange(async (v) => {
./main.js:9413:            new Notice(`Zeus lexical-ios: ${r.N} notas, ${r.vocab} tokens (${r.elapsedMs}ms)`);
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:171:    // MARK: - Auth (v1.14 — codex CRIT #2/#3)
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:173:    // X-Zeus-Token igual a env ZEUS_DAEMON_TOKEN. Fail-closed: sem token
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:180:    static func isAuthorized(remote: SocketAddress?, head: HTTPRequestHead) -> Bool {
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:182:        guard let token = ProcessInfo.processInfo.environment["ZEUS_DAEMON_TOKEN"],
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:184:        guard let provided = head.headers.first(name: "X-Zeus-Token")
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:185:                ?? head.headers.first(name: "x-zeus-token") else { return false }
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:232:            var headers = HTTPHeaders()
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:233:            headers.add(name: "Access-Control-Allow-Origin", value: "*")
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:234:            headers.add(name: "Access-Control-Allow-Methods", value: "GET, POST, OPTIONS")
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:235:            headers.add(name: "Access-Control-Allow-Headers", value: "Content-Type")
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:236:            self.writeRaw(context: context, status: .noContent, headers: headers, body: nil)
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:240:        // v1.14 — Auth gate (codex CRIT #2/#3). /v1/health aberto para discovery;
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:241:        // todo o resto (incl. /v1/cmd) exige loopback OU X-Zeus-Token válido.
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:242:        if path != "/v1/health" && !Self.isAuthorized(remote: context.remoteAddress, head: head) {
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:243:            self.writeJSON(context: context, status: .unauthorized,
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:244:                           dict: ["error": "unauthorized: non-loopback request requires valid X-Zeus-Token"])
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:248:        // v1.2 — PCC permission: extrair header X-Zeus-Allow-Pcc da request.
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:251:        let pccHeaderValue = head.headers.first(name: "X-Zeus-Allow-Pcc")
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:252:            ?? head.headers.first(name: "x-zeus-allow-pcc")
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:253:        let pccPermission = PccPermission.fromHeader(pccHeaderValue)
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:273:        // Quando true, response inclui header X-Zeus-Pcc-Possible:1.
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:286:    // v1.2 — PCC permission modes derived from request header `X-Zeus-Allow-Pcc`.
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:295:        static func fromHeader(_ value: String?) -> PccPermission {
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:995:    // Privacy gate inalterado: respeita header X-Zeus-Allow-Pcc da request.
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:1630:            guard !safe.isEmpty else { return "Uso: curl <url> [-H header]" }
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:3389:        var headers = HTTPHeaders()
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:3390:        headers.add(name: "Content-Type", value: "application/json; charset=utf-8")
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:3391:        headers.add(name: "Access-Control-Allow-Origin", value: "*")
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:3392:        headers.add(name: "Access-Control-Allow-Methods", value: "GET, POST, OPTIONS")
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:3393:        headers.add(name: "Access-Control-Allow-Headers", value: "Content-Type, X-Zeus-Allow-Pcc")
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:3394:        headers.add(name: "Access-Control-Expose-Headers", value: "X-Zeus-Pcc-Possible")
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:3399:            headers.add(name: "X-Zeus-Pcc-Possible", value: "1")
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:3406:            headers.add(name: "Content-Length", value: "\(data.count)")
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:3409:            headers.add(name: "Content-Length", value: "0")
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:3411:        self.writeRaw(context: context, status: status, headers: headers, body: body)
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:3415:                          headers: HTTPHeaders, body: ByteBuffer?) {
./daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:3416:        let head = HTTPResponseHead(version: .init(major: 1, minor: 1), status: status, headers: headers)

exec
/bin/zsh -lc "nl -ba daemon/Sources/ZeusDaemonMac/main.swift | sed -n '24,60p'" in /Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91
 succeeded in 0ms:
    24	// MARK: - Argument parsing
    25	
    26	struct ZeusArgs {
    27	    // v1.14 — default loopback-only (codex CRIT #1). Bind seguro por padrão:
    28	    // o LaunchAgent/auto-spawn já passam --host 127.0.0.1. Para cross-device via
    29	    // Tailscale, exigir --host 0.0.0.0 EXPLÍCITO + ZEUS_DAEMON_TOKEN no ambiente
    30	    // (o handler recusa requisições não-loopback sem token — ver isAuthorized).
    31	    var host: String = "127.0.0.1"
    32	    var port: Int = 2223
    33	    var vaultPath: String? = nil
    34	
    35	    static func parse(_ argv: [String]) -> ZeusArgs {
    36	        var args = ZeusArgs()
    37	        var i = 1
    38	        while i < argv.count {
    39	            let a = argv[i]
    40	            switch a {
    41	            case "--port":
    42	                if i + 1 < argv.count, let p = Int(argv[i + 1]) { args.port = p; i += 1 }
    43	            case "--host":
    44	                if i + 1 < argv.count { args.host = argv[i + 1]; i += 1 }
    45	            case "--vault":
    46	                if i + 1 < argv.count { args.vaultPath = argv[i + 1]; i += 1 }
    47	            case "--help", "-h":
    48	                print("""
    49	                ZeusDaemonMac — Zeus on-device intelligence daemon (macOS)
    50	
    51	                Usage:
    52	                  ZeusDaemonMac [--port 2223] [--host 127.0.0.1] [--vault PATH]
    53	
    54	                Endpoints:
    55	                  GET  /v1/health
    56	                  GET  /v1/tools
    57	                  POST /v1/embed      {"text": "..."}
    58	                  POST /v1/ocr        {"image_path": "..."} or {"image_base64": "..."}
    59	                  POST /v1/summarize  {"text": "...", "max_tokens": 500}
    60	                  POST /v1/enrich     {"note_content": "...", "note_path": "..."}

exec
/bin/zsh -lc "nl -ba lib/io-queue.js | sed -n '1,170p'" in /Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91
exec
/bin/zsh -lc "nl -ba daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift | sed -n '171,246p'" in /Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91
 succeeded in 0ms:
   171	    // MARK: - Auth (v1.14 — codex CRIT #2/#3)
   172	    // Loopback é confiável (on-device-first). Requisições não-loopback exigem
   173	    // X-Zeus-Token igual a env ZEUS_DAEMON_TOKEN. Fail-closed: sem token
   174	    // configurado, todo peer remoto é recusado mesmo se bound em 0.0.0.0.
   175	    static func isLoopback(_ addr: SocketAddress?) -> Bool {
   176	        guard let ip = addr?.ipAddress else { return false }
   177	        return ip == "127.0.0.1" || ip == "::1" || ip == "::ffff:127.0.0.1"
   178	    }
   179	
   180	    static func isAuthorized(remote: SocketAddress?, head: HTTPRequestHead) -> Bool {
   181	        if isLoopback(remote) { return true }
   182	        guard let token = ProcessInfo.processInfo.environment["ZEUS_DAEMON_TOKEN"],
   183	              !token.isEmpty else { return false }
   184	        guard let provided = head.headers.first(name: "X-Zeus-Token")
   185	                ?? head.headers.first(name: "x-zeus-token") else { return false }
   186	        return constantTimeEquals(provided, token)
   187	    }
   188	
   189	    static func constantTimeEquals(_ a: String, _ b: String) -> Bool {
   190	        let ab = Array(a.utf8), bb = Array(b.utf8)
   191	        if ab.count != bb.count { return false }
   192	        var diff: UInt8 = 0
   193	        for i in 0..<ab.count { diff |= ab[i] ^ bb[i] }
   194	        return diff == 0
   195	    }
   196	
   197	    func channelRead(context: ChannelHandlerContext, data: NIOAny) {
   198	        switch self.unwrapInboundIn(data) {
   199	        case .head(let head):
   200	            self.requestHead = head
   201	            self.bodyBuffer = context.channel.allocator.buffer(capacity: 0)
   202	        case .body(var chunk):
   203	            if self.bodyBuffer == nil {
   204	                self.bodyBuffer = context.channel.allocator.buffer(capacity: chunk.readableBytes)
   205	            }
   206	            self.bodyBuffer?.writeBuffer(&chunk)
   207	        case .end:
   208	            self.handleRequest(context: context)
   209	        }
   210	    }
   211	
   212	    private func handleRequest(context: ChannelHandlerContext) {
   213	        guard let head = self.requestHead else {
   214	            self.writeJSON(context: context, status: .badRequest, dict: ["error": "missing request head"])
   215	            return
   216	        }
   217	
   218	        let bodyString: String
   219	        if var buf = self.bodyBuffer, buf.readableBytes > 0 {
   220	            bodyString = buf.readString(length: buf.readableBytes) ?? ""
   221	        } else {
   222	            bodyString = ""
   223	        }
   224	
   225	        self.requestHead = nil
   226	        self.bodyBuffer = nil
   227	
   228	        let path = head.uri.split(separator: "?").first.map(String.init) ?? head.uri
   229	        let method = head.method
   230	
   231	        if method == .OPTIONS {
   232	            var headers = HTTPHeaders()
   233	            headers.add(name: "Access-Control-Allow-Origin", value: "*")
   234	            headers.add(name: "Access-Control-Allow-Methods", value: "GET, POST, OPTIONS")
   235	            headers.add(name: "Access-Control-Allow-Headers", value: "Content-Type")
   236	            self.writeRaw(context: context, status: .noContent, headers: headers, body: nil)
   237	            return
   238	        }
   239	
   240	        // v1.14 — Auth gate (codex CRIT #2/#3). /v1/health aberto para discovery;
   241	        // todo o resto (incl. /v1/cmd) exige loopback OU X-Zeus-Token válido.
   242	        if path != "/v1/health" && !Self.isAuthorized(remote: context.remoteAddress, head: head) {
   243	            self.writeJSON(context: context, status: .unauthorized,
   244	                           dict: ["error": "unauthorized: non-loopback request requires valid X-Zeus-Token"])
   245	            return
   246	        }

 succeeded in 0ms:
     1	/*
     2	 * io-queue.js — fila iCloud-mediada para Mac consumir tarefas geradas em iOS.
     3	 *
     4	 * v1.11.0 — Feature H (closes "Mac consumes iOS deferred work" gap):
     5	 * quando o iOS gera uma intent de indexação (passport/embed/spotlight) mas
     6	 * NÃO tem daemon FM nativo, enfileira a tarefa em `data/ios-queue/<sha>.json`.
     7	 * O Mac (que tem daemon FM) consome periodicamente (15min via PassportScheduler
     8	 * OU manual via comando) e processa.
     9	 *
    10	 * Codex critique: NÃO usar arquivo único com read-modify-write para a fila —
    11	 * adapterWriteAtomic via rename NÃO é lock forte cross-device em iCloud.
    12	 * Em vez disso: 1 task = 1 arquivo (`data/ios-queue/<sha-of-task>.json`).
    13	 * Idempotência via SHA do task payload (mesmo task → mesmo arquivo → único).
    14	 *
    15	 * Concurrency Mac-side: usa DistributedCoordinator para claim/release por
    16	 * `path` (o lock canônico já existente). Após processar com sucesso, deleta o
    17	 * task file. Em falha, mantém o file — próximo sweep retenta.
    18	 *
    19	 * Schema do task:
    20	 *   {
    21	 *     path: string (vault-relative),
    22	 *     sha: string (sha do conteúdo da nota no momento do enqueue),
    23	 *     type: 'passport' | 'embed' | 'spotlight',
    24	 *     payload: object (opcional, type-específico),
    25	 *     enqueued_at: ISO timestamp,
    26	 *     enqueued_by: deviceId,
    27	 *   }
    28	 *
    29	 * Identidade do task file:
    30	 *   sha-of-task = sha256(JSON.stringify({path, sha, type}))
    31	 *   → idempotente: re-enqueue do mesmo (path, sha, type) é no-op (overwrite
    32	 *     do mesmo conteúdo).
    33	 *
    34	 * Garantia eventual consistency:
    35	 *   - iCloud sync de `data/ios-queue/*.json` leva 5-30s.
    36	 *   - Mac consume() roda 15min ou on-demand → eventual.
    37	 *   - Para sigiloso/confidencial: tasks que tocam notas Clientes/** NÃO devem
    38	 *     ser enfileiradas (Privacy Gate). Caller é responsável.
    39	 */
    40	
    41	'use strict';
    42	
    43	const universal = require('./universal-fs');
    44	
    45	const QUEUE_DIR_NAME = 'ios-queue';
    46	const VALID_TYPES = new Set(['passport', 'embed', 'spotlight']);
    47	
    48	class IoQueue {
    49	  /**
    50	   * @param {*} plugin Zeus plugin instance
    51	   */
    52	  constructor(plugin) {
    53	    this.plugin = plugin;
    54	  }
    55	
    56	  /**
    57	   * codex HIGH #4 — privacy gate hard-enforced.
    58	   * Recusa paths sigilosos (Clientes/**, ou marcados como privacy:'sigiloso' no
    59	   * payload). Conforme ~/Code/claude-config/rules/juridico.md: Clientes/** é
    60	   * SIGILOSO por default, NÃO pode ir para nenhum caminho cloud — incluindo
    61	   * io-queue que persiste em iCloud sync.
    62	   * @param {string} path — vault-relative
    63	   * @param {object} [payload]
    64	   * @returns {boolean} true se path é privado e NÃO pode ser enfileirado
    65	   */
    66	  static isPrivatePath(path, payload) {
    67	    if (!path) return false;
    68	    // Hard rule: Clientes/** sempre sigiloso
    69	    if (/^Clientes\//i.test(path)) return true;
    70	    // Payload explicit override (caller pode marcar como sigiloso)
    71	    if (payload && payload.privacy && /sigiloso/i.test(String(payload.privacy))) return true;
    72	    // Custom patterns futuros podem ser adicionados aqui
    73	    return false;
    74	  }
    75	
    76	  get _adapter() {
    77	    return this.plugin.app.vault.adapter;
    78	  }
    79	
    80	  get queueDir() {
    81	    return universal.joinPath(this.plugin.manifest.dir, 'data', QUEUE_DIR_NAME);
    82	  }
    83	
    84	  async _ensureDir() {
    85	    await universal.adapterMkdir(this._adapter, universal.joinPath(this.plugin.manifest.dir, 'data'));
    86	    await universal.adapterMkdir(this._adapter, this.queueDir);
    87	  }
    88	
    89	  /**
    90	   * SHA do payload identitário do task — garante idempotência.
    91	   * Stringify usa keys ordenadas para evitar variação por ordem de inserção.
    92	   * @param {{path:string, sha:string, type:string}} task
    93	   * @returns {Promise<string>} hex short (16 chars) — colisão prática ~zero
    94	   */
    95	  async _taskSha(task) {
    96	    const canonical = JSON.stringify({
    97	      path: task.path || '',
    98	      sha: task.sha || '',
    99	      type: task.type || '',
   100	    });
   101	    const hex = await universal.sha256Hex(canonical);
   102	    return hex.slice(0, 16);
   103	  }
   104	
   105	  async _taskFilePath(task) {
   106	    const sha = await this._taskSha(task);
   107	    return universal.joinPath(this.queueDir, sha + '.json');
   108	  }
   109	
   110	  /**
   111	   * Enfileira um task. Idempotente: mesmo (path, sha, type) → mesmo file.
   112	   *
   113	   * @param {{path:string, sha:string, type:string, payload?:object}} task
   114	   * @returns {Promise<{enqueued: boolean, taskSha: string, file: string, reason?: string}>}
   115	   */
   116	  async enqueue(task) {
   117	    if (!task || typeof task !== 'object') {
   118	      return { enqueued: false, reason: 'task inválido' };
   119	    }
   120	    if (!task.path || typeof task.path !== 'string') {
   121	      return { enqueued: false, reason: 'path ausente' };
   122	    }
   123	    if (!task.type || !VALID_TYPES.has(task.type)) {
   124	      return { enqueued: false, reason: `type inválido (esperado: ${[...VALID_TYPES].join('|')})` };
   125	    }
   126	    // codex HIGH #4: privacy gate hard-enforced (era só comentário no header).
   127	    // Paths em Clientes/** (sigiloso default per claude-config/juridico.md) ou
   128	    // marcados como `payload.privacy === 'sigiloso'` NÃO podem ir pra fila —
   129	    // io-queue persiste em iCloud sync (data/ios-queue/*.json), o que viola o
   130	    // privacy gate sigiloso (não-cloud) do plugin Zeus.
   131	    if (IoQueue.isPrivatePath(task.path, task.payload)) {
   132	      return { enqueued: false, reason: 'privacy-gate: path sigiloso, não enfileirado' };
   133	    }
   134	    if (!task.sha) {
   135	      // Sha do conteúdo é central pra idempotência; aceita sem (caller pode não
   136	      // tê-lo), mas marca explicitamente.
   137	      task.sha = '';
   138	    }
   139	    await this._ensureDir();
   140	    const taskSha = await this._taskSha(task);
   141	    const file = universal.joinPath(this.queueDir, taskSha + '.json');
   142	
   143	    const deviceId = (this.plugin.coordinator && this.plugin.coordinator.deviceId) || 'unknown';
   144	    const fullTask = {
   145	      path: task.path,
   146	      sha: task.sha,
   147	      type: task.type,
   148	      payload: task.payload || null,
   149	      enqueued_at: task.enqueued_at || new Date().toISOString(),
   150	      enqueued_by: task.enqueued_by || deviceId,
   151	      task_sha: taskSha,
   152	    };
   153	    await universal.adapterWriteAtomic(this._adapter, file, JSON.stringify(fullTask));
   154	    return { enqueued: true, taskSha, file };
   155	  }
   156	
   157	  /**
   158	   * Lista todos os tasks pendentes na fila.
   159	   * @returns {Promise<Array<object>>}
   160	   */
   161	  async list() {
   162	    await this._ensureDir();
   163	    const listing = await universal.adapterList(this._adapter, this.queueDir);
   164	    const entries = (listing && listing.files) || [];
   165	    const out = [];
   166	    for (const f of entries) {
   167	      if (!f.endsWith('.json')) continue;
   168	      try {
   169	        const raw = await universal.adapterRead(this._adapter, f);
   170	        const task = JSON.parse(raw);

exec
/bin/zsh -lc 'rg -n "class EmbedRelay|iosEmbedRelayEnabled|tryEmbed|embedRelay|privacy|_privacyPath|isPrivatePath" lib main.source.js' in /Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91
 succeeded in 0ms:
main.source.js:283:  // 'off'    = nenhum header de permissão; só on-device (default, privacy máximo)
main.source.js:314:  iosEmbedRelayEnabled: true,
main.source.js:1239:      // v1.15 — codex round 5: passa filePath como privacyCtx para o gate central
main.source.js:1244:        [stripped, 20, 30, { _privacyPath: filePath }],
main.source.js:2703:      .setDesc('off = só on-device (privacy máximo, default). opt-in = client envia header X-Zeus-Allow-Pcc:1; Apple decide caso a caso. auto = client permite PCC; o daemon apenas sinaliza POSSIBILIDADE quando a carga excede on-device — o routing real é decidido pela Apple (sem API pública de controle/inspeção).')
main.source.js:2998:    this.embedRelay = new EmbedIosLib.EmbedRelay(this);
main.source.js:3867:          if (remote && IoQueue.isPrivatePath(rel)) {
main.source.js:3868:            console.warn('[zeus] real-time embed blocked by privacy gate:', rel, this.httpClient.baseUrl);
main.source.js:3871:          const resp = await this.httpClient.embed(content.slice(0, 4000), { _privacyPath: rel });
main.source.js:3930:          if (remote && IoQueue.isPrivatePath(rel)) {
main.source.js:3931:            console.warn('[zeus] audio embed blocked by privacy gate:', rel, this.httpClient.baseUrl);
main.source.js:3934:          const resp = await this.httpClient.embed(tr.text.slice(0, 4000), { _privacyPath: rel });
main.source.js:4606:          if (this.embedRelay) {
main.source.js:4607:            const probe = await this.embedRelay.tryEmbed('zeus iOS embed probe');
main.source.js:4639:            '  4. iosEmbedRelayEnabled: true (default)',
lib/io-queue.js:57:   * codex HIGH #4 — privacy gate hard-enforced.
lib/io-queue.js:58:   * Recusa paths sigilosos (Clientes/**, ou marcados como privacy:'sigiloso' no
lib/io-queue.js:66:  static isPrivatePath(path, payload) {
lib/io-queue.js:71:    if (payload && payload.privacy && /sigiloso/i.test(String(payload.privacy))) return true;
lib/io-queue.js:126:    // codex HIGH #4: privacy gate hard-enforced (era só comentário no header).
lib/io-queue.js:128:    // marcados como `payload.privacy === 'sigiloso'` NÃO podem ir pra fila —
lib/io-queue.js:130:    // privacy gate sigiloso (não-cloud) do plugin Zeus.
lib/io-queue.js:131:    if (IoQueue.isPrivatePath(task.path, task.payload)) {
lib/io-queue.js:132:      return { enqueued: false, reason: 'privacy-gate: path sigiloso, não enfileirado' };
lib/embed-ios.js:197:class EmbedRelay {
lib/embed-ios.js:207:  async tryEmbed(text, options = {}) {
lib/zeus-http-client.js:150:  _isPrivatePath(path, payload = {}) {
lib/zeus-http-client.js:156:    if (payload && payload.privacy && /sigiloso/i.test(String(payload.privacy))) return true;
lib/zeus-http-client.js:161:    const privacyPath = options._privacyPath || options.privacyPath || options.path || null;
lib/zeus-http-client.js:162:    if (!this._isLoopbackBaseUrl() && this._isPrivatePath(privacyPath, options)) {
lib/zeus-http-client.js:164:        `privacy-gate: ${endpoint} recusou enviar conteúdo sigiloso para daemon remoto (${this.baseUrl})`,
lib/zeus-http-client.js:258:  async _post(endpoint, body, timeoutMs = 60000, privacyCtx = null) {
lib/zeus-http-client.js:259:    // v1.14 — privacy gate CENTRALIZADO (codex HIGH #4). Cobre TODOS os endpoints,
lib/zeus-http-client.js:261:    // ou de privacyCtx explícito (caso /v1/embed, cujo body é texto já extraído).
lib/zeus-http-client.js:262:    const privacyPath = (privacyCtx && (privacyCtx._privacyPath || privacyCtx.privacyPath))
lib/zeus-http-client.js:266:    const privacyTag = (privacyCtx && privacyCtx.privacy)
lib/zeus-http-client.js:267:      || (body && typeof body === 'object' ? body.privacy : null);
lib/zeus-http-client.js:268:    this._assertRawContentAllowed(endpoint, { _privacyPath: privacyPath, privacy: privacyTag });
lib/zeus-http-client.js:306:    const { _privacyPath, privacyPath, privacy, ...wireOptions } = options || {};
lib/zeus-http-client.js:307:    // privacy gate aplicado centralmente em _post via privacyCtx (body é texto, sem path)
lib/zeus-http-client.js:310:      { _privacyPath: _privacyPath || privacyPath, privacy });
lib/zeus-http-client.js:315:  // v1.15 — codex round 5: aceita _privacyPath/privacy no options para o gate ver a
lib/zeus-http-client.js:318:    const { _privacyPath, privacyPath, privacy, ...wireOptions } = options || {};
lib/zeus-http-client.js:320:      { _privacyPath: _privacyPath || privacyPath, privacy });
lib/zeus-http-client.js:346:    const { _privacyPath, privacyPath, privacy, ...wireOptions } = options || {};
lib/zeus-http-client.js:347:    const privacyCtx = { _privacyPath: _privacyPath || privacyPath, privacy };
lib/zeus-http-client.js:351:      const r = await this._post('/v1/embed', { text: t, ...wireOptions }, 30000, privacyCtx);
lib/zeus-http-client.js:387:  // v1.15 — codex round 5: privacyCtx ({_privacyPath, privacy}) propaga a
lib/zeus-http-client.js:390:  async summarize(text, privacyCtx = null) {
lib/zeus-http-client.js:391:    return await this._post('/v1/summarize', { text }, 60000, privacyCtx);
lib/zeus-http-client.js:394:  async graphExtract(text, maxNodes = 20, maxEdges = 30, privacyCtx = null) {
lib/zeus-http-client.js:395:    return await this._post('/v1/graph/extract', { text, max_nodes: maxNodes, max_edges: maxEdges }, 60000, privacyCtx);
lib/zeus-http-client.js:398:  async classify(text, options, privacyCtx = null) {
lib/zeus-http-client.js:399:    return await this._post('/v1/classify', { text, options }, 60000, privacyCtx);
lib/zeus-http-client.js:403:    // options: { max_tokens, deterministic, prewarm, _privacyPath, privacy }
lib/zeus-http-client.js:411:      { _privacyPath: options._privacyPath || options.privacyPath, privacy: options.privacy });

exec
/bin/zsh -lc "nl -ba lib/embed-ios.js | sed -n '1,270p'" in /Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91
 succeeded in 0ms:
     1	/*
     2	 * embed-ios.js — v1.12 embed-relay + iOS embed schema versionado.
     3	 *
     4	 * Codex audit v1.12 aprovou estratégia híbrida two-tier:
     5	 *
     6	 *   CAMADA 1 (default ON) — Relay Mac via Tailscale/loopback HTTP daemon
     7	 *     iOS chama daemon Mac (qualquer device com NLContextualEmbedding nativa) e
     8	 *     persiste resultado 512-dim em data/embeddings.jsonl (mesmo formato Mac).
     9	 *     Quality: Apple-native pt-BR otimizada. Latência: 100-500ms via Tailscale.
    10	 *     Funciona quando Mac está acessível (Tailscale mesh OR mesma LAN).
    11	 *
    12	 *   CAMADA 2 (opt-in labs, default OFF) — transformers.js + multilingual-e5-small
    13	 *     ~118MB lazy-fetch via huggingface.co em primeiro use. Cache via Browser
    14	 *     Cache API (NÃO IndexedDB — codex notou: Transformers.js usa env.useBrowserCache).
    15	 *     Persiste em data/embeddings-ios.jsonl (384-dim, schema separado).
    16	 *     Quality: e5 multilingual ~80% NLContextualEmbedding pt-BR. Latência:
    17	 *     500-1500ms via WASM no WKWebView.
    18	 *     v1.12 ENTREGA: stub + comando "instalar modelo" (copia instruções pro
    19	 *     clipboard estilo MobileCLIP v1.9). Integração runtime transformers.js
    20	 *     deferida pra ADR-011 v1.13 labs (precisa bundle config + CSP audit real).
    21	 *
    22	 * SCHEMA versionado (codex MED — separação rigorosa Mac×iOS):
    23	 *   embeddings.jsonl  — 512-dim NLContextualEmbedding (Apple Mac/iOS daemon)
    24	 *   embeddings-ios.jsonl — 384-dim multilingual-e5-small (transformers.js iOS)
    25	 *
    26	 *   Cada linha em ambos:
    27	 *     {
    28	 *       schema: 'zeus-embeddings-v1',
    29	 *       path: 'note/path.md',
    30	 *       sha: '...',
    31	 *       mtime: 1700000000000,
    32	 *       title: '...',
    33	 *       model_id: 'apple-nlcontextual-pt-BR' | 'Xenova/multilingual-e5-small',
    34	 *       model_revision: '...',         // commit/tag do modelo
    35	 *       dim: 512 | 384,                // recusa carregar se mismatch
    36	 *       device_class: 'mac' | 'ios' | 'ipad',
    37	 *       text_sha: '...',               // hash do texto embedado (não do file)
    38	 *       source: 'daemon-mac' | 'daemon-ios-relay' | 'transformers-ios',
    39	 *       created_at: '2026-05-20T...',
    40	 *       vec: [Float, ...],
    41	 *     }
    42	 *
    43	 * RRF cross-dim: HybridSearch fuse() usa rank position (não cosine cross-dim),
    44	 * portanto seguro adicionar embeddings-ios como 7º retriever desde que cada
    45	 * device leia SÓ seu próprio jsonl (loader recusa dim mismatch).
    46	 */
    47	
    48	'use strict';
    49	
    50	const universal = require('./universal-fs');
    51	
    52	const EMBED_IOS_FILE = 'embeddings-ios.jsonl';
    53	const EMBED_IOS_DIM = 384;          // multilingual-e5-small
    54	const EMBED_IOS_MODEL = 'Xenova/multilingual-e5-small';
    55	const EMBED_MAC_DIM = 512;          // NLContextualEmbedding
    56	const EMBED_MAC_MODEL = 'apple-nlcontextual-pt-BR';
    57	
    58	class EmbedIos {
    59	  constructor(plugin) {
    60	    this.plugin = plugin;
    61	    this._entries = new Map();        // path → entry
    62	    this._loaded = false;
    63	    this._writePromise = null;        // mutex
    64	  }
    65	
    66	  get _adapter() { return this.plugin.app.vault.adapter; }
    67	  get dataPath() { return universal.joinPath(this.plugin.manifest.dir, 'data'); }
    68	  get jsonlPath() { return universal.joinPath(this.dataPath, EMBED_IOS_FILE); }
    69	
    70	  async load() {
    71	    if (this._loaded) return;
    72	    if (!(await universal.adapterExists(this._adapter, this.jsonlPath))) {
    73	      this._loaded = true;
    74	      return;
    75	    }
    76	    try {
    77	      const raw = await universal.adapterRead(this._adapter, this.jsonlPath);
    78	      for (const line of raw.split('\n').filter(Boolean)) {
    79	        try {
    80	          const obj = JSON.parse(line);
    81	          // codex MED: loader recusa dim mismatch (não truncar silente).
    82	          if (!obj || !obj.path) continue;
    83	          if (obj.dim !== EMBED_IOS_DIM) {
    84	            console.warn('[zeus.embed-ios] skip linha dim mismatch:', obj.path, 'dim=', obj.dim);
    85	            continue;
    86	          }
    87	          if (!Array.isArray(obj.vec) || obj.vec.length !== EMBED_IOS_DIM) {
    88	            console.warn('[zeus.embed-ios] skip linha vec inválida:', obj.path);
    89	            continue;
    90	          }
    91	          this._entries.set(obj.path, obj);
    92	        } catch (e) { /* skip malformed */ }
    93	      }
    94	    } catch (e) {
    95	      console.warn('[zeus.embed-ios] load failed:', e.message);
    96	    }
    97	    this._loaded = true;
    98	  }
    99	
   100	  // Get entry por path (iOS-local 384-dim).
   101	  async get(path) {
   102	    await this.load();
   103	    return this._entries.get(path) || null;
   104	  }
   105	
   106	  // Persist atomic. Mutex serializa concorrentes (v1.8.1 pattern).
   107	  async _persist() {
   108	    if (this._writePromise) await this._writePromise.catch(() => {});
   109	    this._writePromise = (async () => {
   110	      try {
   111	        await universal.adapterMkdir(this._adapter, this.dataPath);
   112	        const lines = [];
   113	        for (const e of this._entries.values()) lines.push(JSON.stringify(e));
   114	        await universal.adapterWriteAtomic(this._adapter, this.jsonlPath, lines.join('\n'));
   115	      } finally {
   116	        this._writePromise = null;
   117	      }
   118	    })();
   119	    return this._writePromise;
   120	  }
   121	
   122	  // Upsert entry. Validate schema + dim.
   123	  async upsert(entry) {
   124	    await this.load();
   125	    if (!entry || !entry.path) throw new Error('embed-ios.upsert: path obrigatório');
   126	    if (entry.dim !== EMBED_IOS_DIM) throw new Error(`embed-ios.upsert: dim ${entry.dim} ≠ ${EMBED_IOS_DIM}`);
   127	    if (!Array.isArray(entry.vec) || entry.vec.length !== EMBED_IOS_DIM) {
   128	      throw new Error('embed-ios.upsert: vec inválido');
   129	    }
   130	    // Garante schema fields obrigatórios
   131	    entry.schema = entry.schema || 'zeus-embeddings-v1';
   132	    entry.model_id = entry.model_id || EMBED_IOS_MODEL;
   133	    entry.created_at = entry.created_at || new Date().toISOString();
   134	    entry.source = entry.source || 'transformers-ios';
   135	    this._entries.set(entry.path, entry);
   136	    await this._persist();
   137	    return { upserted: entry.path, total: this._entries.size };
   138	  }
   139	
   140	  // Removes entry (note deleted/renamed)
   141	  async remove(path) {
   142	    await this.load();
   143	    if (this._entries.has(path)) {
   144	      this._entries.delete(path);
   145	      await this._persist();
   146	      return { removed: true };
   147	    }
   148	    return { removed: false };
   149	  }
   150	
   151	  // Stats
   152	  async stats() {
   153	    await this.load();
   154	    return {
   155	      schema: 'zeus-embeddings-v1',
   156	      file: this.jsonlPath,
   157	      model_id: EMBED_IOS_MODEL,
   158	      dim: EMBED_IOS_DIM,
   159	      count: this._entries.size,
   160	      runtime_installed: await this._modelInstalled(),
   161	    };
   162	  }
   163	
   164	  // Check se transformers.js + modelo estão instalados (cache local)
   165	  async _modelInstalled() {
   166	    // v1.12: detecção stub — checa se ~/.zeus-ios-embed-model/ existe no
   167	    // application support (futuro v1.13 labs gravará aqui após primeiro fetch).
   168	    // Por enquanto sempre retorna false (sem runtime instalado).
   169	    return false;
   170	  }
   171	
   172	  /**
   173	   * Embed um texto via transformers.js (lazy-load).
   174	   * v1.12 ENTREGA: stub que retorna instrução acionável quando runtime ausente.
   175	   * v1.13 labs implementará lazy-import xenova/transformers + modelo fetch.
   176	   */
   177	  async embedText(text) {
   178	    if (!(await this._modelInstalled())) {
   179	      throw new Error(
   180	        'embed-ios runtime não instalado. Rode comando "Zeus: instalar modelo iOS embed". '
   181	        + 'v1.12 ENTREGA é stub; runtime transformers.js completo em v1.13 labs (ADR-011).',
   182	      );
   183	    }
   184	    // TODO v1.13: const { pipeline } = await import('@xenova/transformers');
   185	    //             const ext = await pipeline('feature-extraction', EMBED_IOS_MODEL, { quantized: true });
   186	    //             const out = await ext('query: ' + text, { pooling: 'mean', normalize: true });
   187	    //             return Array.from(out.data);
   188	    throw new Error('embed-ios.embedText: runtime não implementado em v1.12');
   189	  }
   190	}
   191	
   192	// =========================================================================
   193	// EmbedRelay — camada 1 (preferida): tenta daemon HTTP via Tailscale/loopback.
   194	// Quando bem-sucedido, persiste no embeddings.jsonl Mac canônico (512-dim).
   195	// =========================================================================
   196	
   197	class EmbedRelay {
   198	  constructor(plugin) {
   199	    this.plugin = plugin;
   200	  }
   201	
   202	  /**
   203	   * Tenta embed via daemon HTTP (loopback Mac OU Tailscale relay iOS→Mac).
   204	   * Sucesso → retorna {ok: true, vec, dim, model, source}
   205	   * Falha → retorna {ok: false, reason}  (não lança — gracioso)
   206	   */
   207	  async tryEmbed(text, options = {}) {
   208	    if (!this.plugin.httpClient) return { ok: false, reason: 'no-httpClient' };
   209	    if (!text || text.length < 2) return { ok: false, reason: 'text-too-short' };
   210	    try {
   211	      const available = await this.plugin.httpClient.isAvailable(1500);
   212	      if (!available) return { ok: false, reason: 'daemon-unreachable' };
   213	      const r = await this.plugin.httpClient.embed(text, options);
   214	      const vec = (r && r.vectors && r.vectors[0]) || (r && r.vector) || null;
   215	      if (!Array.isArray(vec) || vec.length !== EMBED_MAC_DIM) {
   216	        return { ok: false, reason: `dim-mismatch: ${vec ? vec.length : 'null'}` };
   217	      }
   218	      return {
   219	        ok: true,
   220	        vec,
   221	        dim: EMBED_MAC_DIM,
   222	        model: (r && r.model) || EMBED_MAC_MODEL,
   223	        source: 'daemon-relay',
   224	      };
   225	    } catch (e) {
   226	      return { ok: false, reason: (e.message || String(e)).slice(0, 100) };
   227	    }
   228	  }
   229	}
   230	
   231	module.exports = EmbedIos;
   232	module.exports.EmbedRelay = EmbedRelay;
   233	module.exports.EMBED_IOS_DIM = EMBED_IOS_DIM;
   234	module.exports.EMBED_IOS_MODEL = EMBED_IOS_MODEL;
   235	module.exports.EMBED_MAC_DIM = EMBED_MAC_DIM;
   236	module.exports.EMBED_MAC_MODEL = EMBED_MAC_MODEL;

exec
/bin/zsh -lc "nl -ba main.source.js | sed -n '1228,1255p;1634,1662p;1700,1716p'" in /Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91
 succeeded in 0ms:
  1228	class ZeusGraphExtractor {
  1229	  constructor(plugin) {
  1230	    this.plugin = plugin;
  1231	  }
  1232	
  1233	  async extract(filePath) {
  1234	    // HTTP-first: daemon graphExtract() works on iOS too. v0.11 uses vault.adapter
  1235	    // for reading (platform-agnostic) — no Node fs dependency.
  1236	    try {
  1237	      const content = await this.plugin.app.vault.adapter.read(filePath);
  1238	      const stripped = content.replace(/^---\n[\s\S]*?\n---\n/, '').slice(0, 6000);
  1239	      // v1.15 — codex round 5: passa filePath como privacyCtx para o gate central
  1240	      // ver a proveniência do conteúdo (graphExtract envia texto, não path).
  1241	      const r = await tryDaemonOrSpawn(
  1242	        this.plugin,
  1243	        'graphExtract',
  1244	        [stripped, 20, 30, { _privacyPath: filePath }],
  1245	        ['graph-extract', '--max-nodes', '20', '--max-edges', '30'],
  1246	        stripped,
  1247	        60000
  1248	      );
  1249	      if (r.source === 'daemon') {
  1250	        return r.result;
  1251	      }
  1252	      return JSON.parse(r.result);
  1253	    } catch (e) {
  1254	      throw new Error(`graph-extract: ${e.message.slice(0, 200)}`);
  1255	    }
  1634	    if (this.inFlight.has(filePath)) return this.inFlight.get(filePath);
  1635	
  1636	    const promise = (async () => {
  1637	      try {
  1638	        const absVault = this.plugin.vaultRoot;
  1639	        // Read note content for daemon path — vault.adapter works on Mac and iOS.
  1640	        let noteContent = '';
  1641	        try {
  1642	          noteContent = await universal.adapterRead(this.plugin.app.vault.adapter, filePath);
  1643	        } catch (readErr) {
  1644	          console.warn('[zeus] enrich read failed', filePath, readErr.message);
  1645	        }
  1646	        const r = await tryDaemonOrSpawn(
  1647	          this.plugin,
  1648	          'enrich',
  1649	          [noteContent, filePath, ''],   // (noteContent, notePath, vaultSummary)
  1650	          ['enrich', filePath, '--vault', absVault, '--prewarm', '--deterministic'],
  1651	          null,
  1652	          this.plugin.settings.enrichTimeoutMs
  1653	        );
  1654	        let parsed;
  1655	        if (r.source === 'daemon') {
  1656	          parsed = r.result;
  1657	        } else {
  1658	          try { parsed = JSON.parse(r.result); } catch (jsonErr) {
  1659	            console.warn('[zeus] enrich non-JSON output', String(r.result).slice(0, 200));
  1660	            return null;
  1661	          }
  1662	        }
  1700	    ];
  1701	    if (onProgress) onProgress('FoundationModels processando (pode levar 30-60s)…');
  1702	    const r = await tryDaemonOrSpawn(
  1703	      this.plugin,
  1704	      'agent',
  1705	      [question, this.plugin.settings.agentPattern],
  1706	      args,
  1707	      null,
  1708	      180000
  1709	    );
  1710	    if (r.source === 'daemon') {
  1711	      const v = r.result;
  1712	      // Daemon may return { answer, text, output }; pick whichever
  1713	      return String((v && (v.answer || v.text || v.output || v.response)) || JSON.stringify(v));
  1714	    }
  1715	    return r.result;
  1716	  }

exec
/bin/zsh -lc "rg -n \"embedRelay|tryEmbed\\(|iosEmbedRelayEnabled|EmbedRelay\" main.source.js lib/auto-indexer.js lib -g '*.js'" in /Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91
 succeeded in 0ms:
main.source.js:314:  iosEmbedRelayEnabled: true,
main.source.js:2998:    this.embedRelay = new EmbedIosLib.EmbedRelay(this);
main.source.js:4606:          if (this.embedRelay) {
main.source.js:4607:            const probe = await this.embedRelay.tryEmbed('zeus iOS embed probe');
main.source.js:4639:            '  4. iosEmbedRelayEnabled: true (default)',
lib/embed-ios.js:193:// EmbedRelay — camada 1 (preferida): tenta daemon HTTP via Tailscale/loopback.
lib/embed-ios.js:197:class EmbedRelay {
lib/embed-ios.js:207:  async tryEmbed(text, options = {}) {
lib/embed-ios.js:232:module.exports.EmbedRelay = EmbedRelay;

exec
/bin/zsh -lc "nl -ba main.source.js | sed -n '760,785p;580,606p'" in /Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91
 succeeded in 0ms:
   580	    const cached = this.loadFromCache(sha);
   581	    if (cached) return cached;
   582	
   583	    const plugin = this.plugin;
   584	    const topN = plugin.settings.avClassifyTopN;
   585	
   586	    const tasks = [
   587	      // aocr (text in image) — HTTP-first via daemon ocr()
   588	      tryDaemonOrSpawn(
   589	        plugin,
   590	        'ocr',
   591	        [absPath, 'text', 'pt-BR,en'],
   592	        ['ocr', absPath, '-o', 'text'],
   593	        null,
   594	        60000
   595	      ).then(r => {
   596	        if (r.source === 'daemon') {
   597	          const text = (r.result && (r.result.text || r.result.ocr || '')) || '';
   598	          return { aocr: String(text).trim() };
   599	        }
   600	        return { aocr: String(r.result || '').trim() };
   601	      }).catch(e => ({ aocr: '', aocrError: e.message.slice(0, 80) })),
   602	
   603	      // av classify (top-N categories) — HTTP-first via daemon visionClassify()
   604	      tryDaemonOrSpawn(
   605	        plugin,
   606	        'visionClassify',
   760	
   761	    // IMAGES: full multi-modal (aocr + av classify + av landmarks + acs metadata)
   762	    if (isImage && this.plugin.settings.avImageFeatures) {
   763	      try {
   764	        const features = await this.plugin.av.extractImageFeatures(absPath, sha);
   765	        return this.plugin.av.synthesizeIndexableText(features, path.basename(absPath));
   766	      } catch (e) {
   767	        console.warn('[zeus] av extract failed for', absPath, e.message);
   768	        // fall through to plain OCR
   769	      }
   770	    }
   771	
   772	    // Plain aocr path (PDF or fallback for images without av features) — HTTP-first
   773	    const cachePath = path.join(this.dataPath, OCR_CACHE_DIR, sha + '.txt');
   774	    if (fs.existsSync(cachePath)) return fs.readFileSync(cachePath, 'utf8');
   775	
   776	    const extractText = (r) => {
   777	      if (r.source === 'daemon') {
   778	        const v = r.result;
   779	        return String((v && (v.text || v.ocr || v.content)) || '');
   780	      }
   781	      return String(r.result || '');
   782	    };
   783	
   784	    try {
   785	      const spawnArgs = ['ocr', absPath, '-o', 'text', '-l', 'pt-BR,en'];

exec
/bin/zsh -lc "rg -n \"function .*abs|absPath|adapter.getResourcePath|base64|image_base64|ocr\\(\" main.source.js lib -g '*.js'" in /Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91
 succeeded in 0ms:
lib/hierarchical.js:375:    const absPath = path.isAbsolute(filePath) ? filePath : path.join(vaultRoot, filePath);
lib/hierarchical.js:378:      text = fs.readFileSync(absPath, 'utf8');
lib/hierarchical.js:380:      throw new Error('processLargeDoc: cannot read ' + absPath + ' — ' + e.message);
lib/passport-index.js:117:    // v1.15 — S8 Check 1: o daemon recebe absPath (Mac) e ecoa passport.path
main.source.js:523:async function acsMetadata(absPath) {
main.source.js:526:    const child = spawn('/usr/bin/mdls', ['-plist', '-', absPath], { stdio: ['ignore', 'pipe', 'pipe'] });
main.source.js:579:  async extractImageFeatures(absPath, sha) {
main.source.js:587:      // aocr (text in image) — HTTP-first via daemon ocr()
main.source.js:591:        [absPath, 'text', 'pt-BR,en'],
main.source.js:592:        ['ocr', absPath, '-o', 'text'],
main.source.js:607:        [absPath, topN],
main.source.js:608:        ['vision', 'classify', absPath, '--top-n', String(topN)],
main.source.js:623:        [absPath],
main.source.js:624:        ['vision', 'landmarks', absPath],
main.source.js:638:      acsMetadata(absPath).then(meta => ({ acsMetadata: meta })),
main.source.js:753:  async readFileContent(absPath, ext) {
main.source.js:754:    if (ext === 'md') return fs.readFileSync(absPath, 'utf8');
main.source.js:756:    const sha = sha256(absPath + ':' + fs.statSync(absPath).mtimeMs);
main.source.js:764:        const features = await this.plugin.av.extractImageFeatures(absPath, sha);
main.source.js:765:        return this.plugin.av.synthesizeIndexableText(features, path.basename(absPath));
main.source.js:767:        console.warn('[zeus] av extract failed for', absPath, e.message);
main.source.js:785:      const spawnArgs = ['ocr', absPath, '-o', 'text', '-l', 'pt-BR,en'];
main.source.js:790:        [absPath, 'text', 'pt-BR,en'],
main.source.js:806:            [absPath, 'text', 'pt-BR,en'],
main.source.js:807:            ['ocr', absPath, '-o', 'text', '-l', 'pt-BR,en'],
main.source.js:815:        } catch (e2) { console.warn('[zeus] aocr fallback also failed', absPath, e2.message); }
main.source.js:817:      console.warn('[zeus] aocr failed for', absPath, e.message);
main.source.js:3089:                let absPath = t.path;
main.source.js:3091:                  absPath = this.vaultRoot.replace(/\/$/, '') + '/' + t.path;
main.source.js:3099:                  await this.passport.buildOne(absPath, []);
main.source.js:3906:          const absPath = nodePath ? nodePath.join(basePath, rel) : basePath + '/' + rel;
main.source.js:3910:            const vad = await this.httpClient.aspVad(absPath);
main.source.js:3921:          const tr = await this.httpClient.aspTranscribe(absPath, locale, engine);
main.source.js:4510:              let absPath = t.path;
main.source.js:4512:                absPath = this.vaultRoot.replace(/\/$/, '') + '/' + t.path;
main.source.js:4519:                await this.passport.buildOne(absPath, []);
lib/daemon-lifecycle.js:73:  _prepareBinary(absPath) {
lib/daemon-lifecycle.js:76:      try { fs.chmodSync(absPath, 0o755); } catch {}
lib/daemon-lifecycle.js:80:      try { execFileSync('/usr/bin/xattr', ['-d', 'com.apple.quarantine', absPath], { stdio: 'ignore' }); }
lib/zeus-http-client.js:325:  async aspTranscribe(absPath, locale = 'pt-BR', engine = 'auto') {
lib/zeus-http-client.js:327:      { path: absPath, locale, engine },
lib/zeus-http-client.js:333:  async aspVad(absPath) {
lib/zeus-http-client.js:334:    return await this._post('/v1/asp/vad', { path: absPath }, 15000);
lib/zeus-http-client.js:372:  async ocr(filePath, outputFormat = 'text', language = 'pt-BR,en') {
lib/zeus-http-client.js:373:    // v1.5 — daemon /v1/ocr handler exige `image_path` (ou `image_base64`).
lib/auto-indexer.js:183:        // o absPath continua sendo o caminho preferido para o daemon.
lib/auto-indexer.js:184:        let absPath = path;
lib/auto-indexer.js:186:          absPath = this.plugin.vaultRoot.replace(/\/$/, '') + '/' + path;
lib/auto-indexer.js:188:        const passport = await p.buildOne(absPath, []);

exec
/bin/zsh -lc "nl -ba lib/passport-index.js | sed -n '96,138p'" in /Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91
exec
/bin/zsh -lc "nl -ba lib/auto-indexer.js | sed -n '170,205p'" in /Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91
 succeeded in 0ms:
    96	      // (Mac AutoIndexer), tenta extrair vault-relative removendo o vaultRoot.
    97	      const relForRead = this._vaultRelative(filePath);
    98	      if (relForRead && await universal.adapterExists(this._adapter, relForRead)) {
    99	        const content = await universal.adapterRead(this._adapter, relForRead);
   100	        currentSha = await universal.sha256Hex(content);
   101	      }
   102	    } catch (e) {
   103	      console.warn('[zeus][passport] sha precompute failed for', filePath, e.message);
   104	    }
   105	    let passport;
   106	    try {
   107	      passport = await this.plugin.httpClient.passportExtract(filePath, domainOptions);
   108	    } catch (e) {
   109	      // Daemon respondeu isAvailable mas /v1/passport/extract falhou — caímos
   110	      // para local mesmo assim, mantendo a promessa de "indexação never blocks".
   111	      console.warn('[zeus][passport] daemon extract failed, fallback to ios-local:', e.message);
   112	      return await this._buildOneLocal(filePath);
   113	    }
   114	    if (!passport || !passport.path) {
   115	      throw new Error(`PassportIndex.buildOne: resposta inválida para ${filePath}`);
   116	    }
   117	    // v1.15 — S8 Check 1: o daemon recebe absPath (Mac) e ecoa passport.path
   118	    // absoluto. Coage para vault-relative ANTES de virar chave do cache/JSONL,
   119	    // garantindo que o key do map case com o .path persistido (sem duplicatas).
   120	    passport.path = this._vaultRelative(passport.path);
   121	    // Stamp staleness-tracking fields (overwrite daemon-provided when we have local SHA).
   122	    if (currentSha) passport.sha = currentSha;
   123	    if (this.plugin.coordinator && this.plugin.coordinator.deviceId) {
   124	      passport.extracted_by = this.plugin.coordinator.deviceId;
   125	    }
   126	    passport.extracted_at = new Date().toISOString();
   127	    // Persist incrementally
   128	    const map = await this.loadAll();
   129	    map.set(passport.path, passport);
   130	    await this.saveAll(map);
   131	    // Mirror passport metadata into manifest for fast staleness scans.
   132	    try { await this._updateManifestEntry(passport); } catch (e) {
   133	      console.warn('[zeus][passport] manifest mirror failed:', e.message);
   134	    }
   135	    this._lastBuiltAt = new Date().toISOString();
   136	    return passport;
   137	  }
   138	

 succeeded in 0ms:
   170	  async _runPassport(path) {
   171	    const p = this.plugin.passport;
   172	    if (!p) return { skipped: 'no-passport' };
   173	    // v1.10.2 fix: usa PassportIndex.buildOne (que chama daemon + persiste em
   174	    // passports.jsonl + atualiza manifest). Antes do fix, AutoIndexer chamava
   175	    // httpClient.passportBatchExtract direto e descartava o retorno — passports
   176	    // não eram gravados em disk.
   177	    if (typeof p.buildOne === 'function') {
   178	      try {
   179	        // v1.10.3: vault.on('create').file.path é VAULT-RELATIVE
   180	        // ("00 Templates/Foo.md"). Daemon /v1/passport/extract exige path
   181	        // ABSOLUTE. Sem conversão, daemon retorna "não foi possível ler".
   182	        // v1.11 Feature E: buildOne agora tem fallback ios-local automático;
   183	        // o absPath continua sendo o caminho preferido para o daemon.
   184	        let absPath = path;
   185	        if (path && !path.startsWith('/') && this.plugin.vaultRoot) {
   186	          absPath = this.plugin.vaultRoot.replace(/\/$/, '') + '/' + path;
   187	        }
   188	        const passport = await p.buildOne(absPath, []);
   189	        // v1.11.1 codex HIGH #2: quando passport.source==='ios-local' E device é
   190	        // iOS, enfileira REPROCESS via FM no Mac. Antes esse path só rodava em
   191	        // erro raro; agora roda em CADA passport iOS (preventivamente) — Mac
   192	        // reprocessa com qualidade FM quando online.
   193	        try {
   194	          if (passport && passport.source === 'ios-local'
   195	              && this.plugin.ioQueue && this.plugin.coordinator
   196	              && this.plugin.coordinator.deviceId
   197	              && /ios|ipad/i.test(this.plugin.coordinator.deviceId)) {
   198	            const relPath = path && path.startsWith('/') && this.plugin.vaultRoot
   199	              ? path.slice(this.plugin.vaultRoot.length).replace(/^\/+/, '')
   200	              : path;
   201	            await this.plugin.ioQueue.enqueue({
   202	              path: relPath,
   203	              sha: passport.sha || '',
   204	              type: 'passport',
   205	              payload: { reason: 'ios-local-needs-fm-refine' },

exec
/bin/zsh -lc "nl -ba main.source.js | sed -n '4498,4528p'" in /Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91
 succeeded in 0ms:
  4498	          if (tasks.length === 0) {
  4499	            n.hide();
  4500	            new Notice('Zeus: fila vazia — nada a consumir');
  4501	            return;
  4502	          }
  4503	          let consumed = 0, failed = 0;
  4504	          for (const task of tasks) {
  4505	            const r = await this.ioQueue.consume(task, async (t) => {
  4506	              if (t.type !== 'passport') return { ok: true, alreadyDone: true };
  4507	              if (!this.passport || typeof this.passport.buildOne !== 'function') {
  4508	                return { ok: false, error: 'passport API indisponível' };
  4509	              }
  4510	              let absPath = t.path;
  4511	              if (t.path && !t.path.startsWith('/') && this.vaultRoot) {
  4512	                absPath = this.vaultRoot.replace(/\/$/, '') + '/' + t.path;
  4513	              }
  4514	              try {
  4515	                const existing = await this.passport.getPassport(t.path);
  4516	                if (existing && existing.sha === t.sha && existing.source !== 'ios-local') {
  4517	                  return { ok: true, alreadyDone: true };
  4518	                }
  4519	                await this.passport.buildOne(absPath, []);
  4520	                return { ok: true };
  4521	              } catch (e) {
  4522	                return { ok: false, error: e.message };
  4523	              }
  4524	            });
  4525	            if (r.consumed) consumed++; else failed++;
  4526	          }
  4527	          n.hide();
  4528	          new Notice(`Zeus: ${consumed} consumidos, ${failed} falhas (de ${tasks.length})`);

codex
**Recomendação: C, com B agora.**  
Para “funcionar no iOS agora” com menor risco, o caminho realista é **relay Tailscale para o Mac mini**, mantendo JS local para BM25/Leiden/multiplex/cosine e tratando A como projeto posterior. A não entrega agora: exige app host, MLX/Gemma, pesos grandes, assinatura, deploy em dois devices e risco térmico em A14/A16.

**Mas “100%” precisa ser honesto:** B entrega **100% dos recursos remotos não-sigilosos enquanto Mac + Tailscale estiverem online**. Não entrega offline, não entrega sigiloso via daemon remoto, e não transforma iPhone/iPad A14/A16 em devices Apple Intelligence.

**Segurança do relay**  
Reabrir o daemon é coerente **só se for uma exceção explícita e estreita**, não rollback do hardening:

- Preferir bind no **IP Tailscale do Mac** ou interface restrita, não `0.0.0.0`.
- `ZEUS_DAEMON_TOKEN` forte obrigatório.
- Tailscale ACL permitindo **somente iPhone/iPad → Mac:2223**.
- Ideal: `/v1/cmd` recusado para não-loopback mesmo com token, porque o blast radius é alto.

O servidor Mac está endurecido: default loopback em [daemon/Sources/ZeusDaemonMac/main.swift](/Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91/daemon/Sources/ZeusDaemonMac/main.swift:27), auth por `X-Zeus-Token` para não-loopback em [ZeusMacHTTPHandler.swift](/Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91/daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:180), `/v1/health` aberto e resto protegido em [ZeusMacHTTPHandler.swift](/Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91/daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:240).

**Furo no código atual do plugin**  
O repo **não confirma** o fato “plugin envia token”. Achei discovery/mesh/fallback, mas **não achei `X-Zeus-Token` no client**.

O que existe:

- `allowRemoteDaemonFallback` default `false`: [main.source.js](/Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91/main.source.js:254)
- mesh peers em `localStorage`: [main.source.js](/Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91/main.source.js:386)
- discovery local primeiro, mesh só se opt-in: [main.source.js](/Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91/main.source.js:469)
- settings expõem peers/fallback, mas só mencionam token em texto: [main.source.js](/Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91/main.source.js:2439)
- `ZeusHttpClient` manda apenas `Content-Type` + PCC headers, **sem auth token**: [lib/zeus-http-client.js](/Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91/lib/zeus-http-client.js:222)

Consequência: `/v1/health` remoto responde, discovery escolhe o peer, mas `/v1/tools`/POSTs podem cair em `401`. Antes de chamar B “sólido”, falta adicionar token per-device no plugin e injetar `X-Zeus-Token` em requests não-loopback.

**O que quebra no B**

- `Clientes/**`: bloqueado para daemon remoto. Real-time embed simplesmente não roda; há `console.warn` e retorno silencioso em [main.source.js](/Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91/main.source.js:3866). O gate central também recusa endpoints raw-content remotos em [lib/zeus-http-client.js](/Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91/lib/zeus-http-client.js:160).
- OCR/áudio/vision por path podem falhar: o Mac não lê paths sandbox do iOS. O client manda `image_path`/`path`, não base64: [lib/zeus-http-client.js](/Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91/lib/zeus-http-client.js:372).
- Offline sem Mac/Tailscale: ficam só lanes JS e embeddings já sincronizados.
- FoundationModels generativo local no iPhone/iPad: impossível nesses devices sem app host alternativo.

**Arquitetura final**  
Implementar **C**:

1. **Agora:** B endurecido: Mac daemon bound ao Tailscale, token no plugin, ACL estreita, fallback remoto opt-in por device.
2. **Depois:** A apenas se o requisito real for **offline total no iOS**, aceitando custo de app host + Gemma/MLX.

Sem app host, é inerentemente impossível prometer: **iOS offline + nota sigilosa + embed/generativo novo**. Isso precisa aparecer na UX como “bloqueado por privacidade/offline”, não como “100%”.
tokens used
202.412
**Recomendação: C, com B agora.**  
Para “funcionar no iOS agora” com menor risco, o caminho realista é **relay Tailscale para o Mac mini**, mantendo JS local para BM25/Leiden/multiplex/cosine e tratando A como projeto posterior. A não entrega agora: exige app host, MLX/Gemma, pesos grandes, assinatura, deploy em dois devices e risco térmico em A14/A16.

**Mas “100%” precisa ser honesto:** B entrega **100% dos recursos remotos não-sigilosos enquanto Mac + Tailscale estiverem online**. Não entrega offline, não entrega sigiloso via daemon remoto, e não transforma iPhone/iPad A14/A16 em devices Apple Intelligence.

**Segurança do relay**  
Reabrir o daemon é coerente **só se for uma exceção explícita e estreita**, não rollback do hardening:

- Preferir bind no **IP Tailscale do Mac** ou interface restrita, não `0.0.0.0`.
- `ZEUS_DAEMON_TOKEN` forte obrigatório.
- Tailscale ACL permitindo **somente iPhone/iPad → Mac:2223**.
- Ideal: `/v1/cmd` recusado para não-loopback mesmo com token, porque o blast radius é alto.

O servidor Mac está endurecido: default loopback em [daemon/Sources/ZeusDaemonMac/main.swift](/Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91/daemon/Sources/ZeusDaemonMac/main.swift:27), auth por `X-Zeus-Token` para não-loopback em [ZeusMacHTTPHandler.swift](/Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91/daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:180), `/v1/health` aberto e resto protegido em [ZeusMacHTTPHandler.swift](/Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91/daemon/Sources/ZeusDaemonMac/ZeusMacHTTPHandler.swift:240).

**Furo no código atual do plugin**  
O repo **não confirma** o fato “plugin envia token”. Achei discovery/mesh/fallback, mas **não achei `X-Zeus-Token` no client**.

O que existe:

- `allowRemoteDaemonFallback` default `false`: [main.source.js](/Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91/main.source.js:254)
- mesh peers em `localStorage`: [main.source.js](/Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91/main.source.js:386)
- discovery local primeiro, mesh só se opt-in: [main.source.js](/Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91/main.source.js:469)
- settings expõem peers/fallback, mas só mencionam token em texto: [main.source.js](/Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91/main.source.js:2439)
- `ZeusHttpClient` manda apenas `Content-Type` + PCC headers, **sem auth token**: [lib/zeus-http-client.js](/Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91/lib/zeus-http-client.js:222)

Consequência: `/v1/health` remoto responde, discovery escolhe o peer, mas `/v1/tools`/POSTs podem cair em `401`. Antes de chamar B “sólido”, falta adicionar token per-device no plugin e injetar `X-Zeus-Token` em requests não-loopback.

**O que quebra no B**

- `Clientes/**`: bloqueado para daemon remoto. Real-time embed simplesmente não roda; há `console.warn` e retorno silencioso em [main.source.js](/Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91/main.source.js:3866). O gate central também recusa endpoints raw-content remotos em [lib/zeus-http-client.js](/Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91/lib/zeus-http-client.js:160).
- OCR/áudio/vision por path podem falhar: o Mac não lê paths sandbox do iOS. O client manda `image_path`/`path`, não base64: [lib/zeus-http-client.js](/Users/rogermaiocchi/Code/zeus-obsidian-plugin/.claude/worktrees/gifted-poitras-209e91/lib/zeus-http-client.js:372).
- Offline sem Mac/Tailscale: ficam só lanes JS e embeddings já sincronizados.
- FoundationModels generativo local no iPhone/iPad: impossível nesses devices sem app host alternativo.

**Arquitetura final**  
Implementar **C**:

1. **Agora:** B endurecido: Mac daemon bound ao Tailscale, token no plugin, ACL estreita, fallback remoto opt-in por device.
2. **Depois:** A apenas se o requisito real for **offline total no iOS**, aceitando custo de app host + Gemma/MLX.

Sem app host, é inerentemente impossível prometer: **iOS offline + nota sigilosa + embed/generativo novo**. Isso precisa aparecer na UX como “bloqueado por privacidade/offline”, não como “100%”.
