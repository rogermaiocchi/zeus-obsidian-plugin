{
  "name": "zeus-obsidian-plugin",
  "version": "1.4.3",
  "private": true,
  "description": "Build setup for the Zeus Obsidian plugin — bundles main.source.js + lib/*.js into a single main.js for Obsidian mobile (iOS).",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "check": "node scripts/check-project.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
